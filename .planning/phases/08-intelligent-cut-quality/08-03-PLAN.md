---
phase: 08-intelligent-cut-quality
plan: 03
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - tests/test_review.py
  - tests/test_ui_app.py
autonomous: true

must_haves:
  truths:
    - "Review stage editorial_action derivation works correctly: protected->keep, disfluency->remove, LLM-safe hedge->remove, LLM-review hedge->review"
    - "Streamlit filler cards display context snippet, pause badges, protection lock, and LLM reason when data is present"
    - "Legacy edit_plan.json without Phase 8 fields loads without error"
  artifacts:
    - path: "tests/test_review.py"
      provides: "Tests for editorial_action derivation covering all category/protected/triage combinations"
      min_lines: 20
    - path: "tests/test_ui_app.py"
      provides: "Tests for filler card data helper covering Phase 8 display fields"
      min_lines: 10
  key_links:
    - from: "src/podcast_pipeline/stages/review.py"
      to: "src/podcast_pipeline/models/edit_plan.py"
      via: "review populates FillerCutRange fields from enriched data + triage results"
      pattern: "protected|llm_safe_to_remove|editorial_action"
---

<objective>
Verify existing review/UI wiring is correct and all editorial_action paths work as expected.

Purpose: Research audit confirms plans 01-03 are compatible with no ecosystem blockers. This plan runs existing tests and adds any missing coverage for the editorial_action derivation logic and Streamlit filler card display.
Output: Verified review and UI tests with complete editorial_action path coverage.
</objective>

<execution_context>
@/home/kngpnn/.claude/get-shit-done/workflows/execute-plan.md
@/home/kngpnn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-intelligent-cut-quality/08-RESEARCH.md
@.planning/phases/08-intelligent-cut-quality/08-01-SUMMARY.md
@.planning/phases/08-intelligent-cut-quality/08-02-SUMMARY.md
@src/podcast_pipeline/models/edit_plan.py
@src/podcast_pipeline/stages/review.py
@src/podcast_pipeline/ui/app.py
@tests/test_review.py
@tests/test_ui_app.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify review editorial_action logic and add missing path coverage</name>
  <files>tests/test_review.py</files>
  <action>
**Step A -- Run existing review tests:**

Run `uv run pytest tests/test_review.py -q -k "editorial_action or phase8 or backward_compat or filler"`. If all pass, the review stage logic is correct.

**Step B -- Verify editorial_action coverage:**

Check that existing tests cover ALL four derivation paths from the spec:
1. `protected=True` -> `editorial_action="keep"`
2. `category="disfluency"` -> `editorial_action="remove"`
3. Hedge with `triage.safe_to_remove=True` -> `editorial_action="remove"`
4. Hedge with `triage.safe_to_remove=False` or no triage -> `editorial_action="review"`

If any path is not covered by existing tests, add a test for it. If all four paths are covered, this step is a no-op.

**Step C -- Verify explicit user decision override:**

Confirm there is a test that verifies explicit filler_decisions override the computed default_action. If missing, add one that creates a disfluency filler (would normally be "remove"), sets an explicit filler_decision of "keep", and asserts the final action is "keep".

Do NOT rewrite passing tests. Only add missing coverage.
  </action>
  <verify>uv run pytest tests/test_review.py -q -k "editorial_action or phase8 or backward_compat or filler"</verify>
  <done>All editorial_action derivation paths verified: protected->keep, disfluency->remove, LLM-safe->remove, LLM-review->review, explicit override works.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Streamlit filler card Phase 8 display fields</name>
  <files>tests/test_ui_app.py</files>
  <action>
**Step A -- Run existing UI tests:**

Run `uv run pytest tests/test_ui_app.py -q -k "filler_card or context or pause or protected or llm_reason"`. If all pass, the UI wiring is correct.

**Step B -- Check for missing test coverage:**

Verify that tests cover:
1. Filler card with context_before/context_after populated
2. Filler card with pause_before_ms > 0 showing timing badges
3. Filler card with protected=True showing lock/keep default
4. Filler card with llm_reason populated
5. Filler card with all Phase 8 fields absent (graceful degradation)

If any scenario is not tested, add it. If all are covered, this is a no-op.

Do NOT modify the actual UI code (src/podcast_pipeline/ui/app.py) unless a test reveals a concrete bug.
  </action>
  <verify>uv run pytest tests/test_ui_app.py -q -k "filler_card or context or pause or protected or llm_reason"</verify>
  <done>UI filler card tests verify context, pause badges, protection lock, LLM reason display, and graceful degradation for absent Phase 8 data.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_review.py tests/test_ui_app.py -q`
- `uv run ruff check src/podcast_pipeline/stages/review.py src/podcast_pipeline/ui/app.py`
</verification>

<success_criteria>
- All editorial_action derivation paths tested and passing.
- UI filler card Phase 8 display fields verified with tests.
- Legacy edit_plan.json and filler_cuts.json load without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/08-intelligent-cut-quality/08-03-SUMMARY.md`
</output>
