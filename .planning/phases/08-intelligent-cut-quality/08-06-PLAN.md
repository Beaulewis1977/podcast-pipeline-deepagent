---
phase: 08-intelligent-cut-quality
plan: 06
type: execute
wave: 4
depends_on: ["03", "05"]
files_modified:
  - scripts/smoke_test_gpu_rife.py
  - docs/phase8-operator-guide.md
  - tests/test_pipeline.py
  - tests/test_render.py
autonomous: true

must_haves:
  truths:
    - "GPU smoke test references torch 2.10.x baseline (not >=2.7) and cu128 index"
    - "GPU smoke test calls RifeBridge.generate() and prints 'RIFE OK' on success"
    - "Operator guide documents gemini-3-flash-lite as default triage model and explicitly forbids reasoning/CoT models (o1, gemini-3-pro, etc.)"
    - "Operator guide references current dependency baselines: silero-vad 6.2, librosa 0.11, opencv-headless 4.13, torch 2.10.x"
    - "Operator guide correctly states RIFE 4.26 exists but 4.25 is recommended"
    - "Legacy filler_cuts.json and edit_plan.json without Phase 8 fields load and render correctly"
    - "Render with all Phase 8 passes disabled produces identical output to Phase 7 baseline"
  artifacts:
    - path: "scripts/smoke_test_gpu_rife.py"
      provides: "GPU smoke test with current version references and upstream-compatible RIFE bridge call"
      min_lines: 40
    - path: "docs/phase8-operator-guide.md"
      provides: "Operator guide with current versions, forbidden model list, and RIFE 4.26 correction"
      min_lines: 80
    - path: "tests/test_pipeline.py"
      provides: "Integration tests for triage chain and legacy compat"
      min_lines: 40
    - path: "tests/test_render.py"
      provides: "Regression tests for Phase 8 disabled = Phase 7 baseline"
      min_lines: 20
  key_links:
    - from: "scripts/smoke_test_gpu_rife.py"
      to: "src/podcast_pipeline/utils/rife_bridge.py"
      via: "Smoke test creates RifeBridge and generates real bridge frames on GPU hardware"
      pattern: "RifeBridge|generate|RIFE OK"
    - from: "docs/phase8-operator-guide.md"
      to: "src/podcast_pipeline/config/settings.py"
      via: "Operator guide documents config field names and defaults that match settings.py"
      pattern: "llm_triage_model|gemini-3-flash-lite|rife_enabled"
---

<objective>
Update smoke test and operator guide to current baselines, add integration tests and legacy compat regressions.

Purpose: Lock Phase 8 with correct version references, explicit model constraints in documentation, and regression tests proving legacy compatibility and disabled-feature correctness.
Output: Updated smoke test + operator guide + integration tests + legacy compat regressions.
</objective>

<execution_context>
@/home/kngpnn/.claude/get-shit-done/workflows/execute-plan.md
@/home/kngpnn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-intelligent-cut-quality/08-RESEARCH.md
@.planning/phases/08-intelligent-cut-quality/08-03-SUMMARY.md
@.planning/phases/08-intelligent-cut-quality/08-05-SUMMARY.md
@scripts/smoke_test_gpu_rife.py
@docs/phase8-operator-guide.md
@tests/test_pipeline.py
@tests/test_render.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update smoke test and operator guide to current baselines</name>
  <files>scripts/smoke_test_gpu_rife.py
docs/phase8-operator-guide.md</files>
  <action>
**Step A -- Update `scripts/smoke_test_gpu_rife.py` version references:**

The existing smoke test has a well-structured helper function design (`_check_torch`, `_check_opencv`, `_resolve_rife_script`, `main`). Do NOT restructure it. Make these targeted updates:

1. Update the module docstring header from `torch>=2.7 with cu128` to `torch==2.10.* with cu128` (current tested baseline per research)
2. Update the `opencv-python-headless>=4.9` reference in the docstring to `opencv-python-headless>=4.13`
3. In `_check_torch()`, update the install hint from `uv pip install torch torchaudio --index-url ...` to `uv pip install "torch==2.10.*" "torchaudio==2.10.*" --index-url https://download.pytorch.org/whl/cu128` (pinned baseline per research)
4. Verify the smoke test still uses `RifeBridge` from the fixed module (it should -- the API contract is unchanged)

Do NOT restructure the helper functions or change the overall script logic.

**Step B -- Update `docs/phase8-operator-guide.md`:**

Make these targeted updates to the existing operator guide:

1. **Triage model section:** Update to explicitly state:
   - Default: `gemini-3-flash-lite`
   - Alternative: `claude-haiku-4-5`
   - **Explicitly forbidden:** reasoning/CoT models (`o1`, `o1-mini`, `o3-mini`, `gemini-3-pro`, `gemini-3-pro-preview`, `gemini-3-pro-image-preview`). These are too slow/expensive for per-filler classification. The triage stage needs sub-second responses per batch, not deep reasoning.

2. **Dependency versions:** Update any references to match current baselines:
   - `silero-vad>=6.2,<7` (was >=6.1)
   - `librosa>=0.11,<1` (was >=0.10)
   - `opencv-python-headless>=4.13,<5` (was >=4.9)
   - `torch==2.10.*` / `torchaudio==2.10.*` baseline (was >=2.7)

3. **RIFE model note:** Change any "4.26 does not exist" text to "4.26 exists but 4.25 is the recommended default (4.26 may produce artifacts on some content types)"

4. **GPU install command:** Update to `uv pip install "torch==2.10.*" "torchaudio==2.10.*" --index-url https://download.pytorch.org/whl/cu128`

Do NOT rewrite the entire guide. Only update the specific sections with outdated information.
  </action>
  <verify>uv run ruff check scripts/smoke_test_gpu_rife.py && grep -q "2.10" scripts/smoke_test_gpu_rife.py && grep -q "forbidden\|reasoning\|CoT" docs/phase8-operator-guide.md && grep -q "6.2" docs/phase8-operator-guide.md && echo "Version references and model constraints updated"</verify>
  <done>Smoke test references torch 2.10.x baseline; operator guide documents forbidden reasoning models and current dependency versions; RIFE 4.26 note corrected.</done>
</task>

<task type="auto">
  <name>Task 2: Add integration tests and legacy compatibility regressions</name>
  <files>tests/test_pipeline.py
tests/test_render.py</files>
  <action>
**Step A -- Add/verify integration tests in `tests/test_pipeline.py`:**

Check if these tests already exist. If they do and pass, skip. If missing, add them:

1. `test_legacy_filler_cuts_load_without_phase8_fields`: Create a filler_cuts.json with only `{start, end, word, confidence}` (no category, protected, pause fields). Load through model deserialization. Assert all Phase 8 fields default safely (category="disfluency", protected=False, etc.) and no errors raised.

2. `test_legacy_edit_plan_load_without_phase8_fields`: Create an edit_plan.json with FillerCutRange entries lacking llm_safe_to_remove, llm_reason, protected fields. Load via EditPlan model. Assert model validates and fields default correctly.

3. `test_triage_to_review_chain_editorial_actions`: Create a mock scenario with:
   - 1 disfluency filler ("um") -> expect editorial_action="remove"
   - 1 protected hedge ("like" with 400ms pause) -> expect editorial_action="keep"
   - 1 LLM-safe hedge ("you know", triage safe_to_remove=True) -> expect editorial_action="remove"
   - 1 LLM-review hedge ("basically", triage safe_to_remove=False) -> expect editorial_action="review"
   Wire through the review stage logic (mock file reads). Assert each filler gets correct editorial_action.

Mark GPU-requiring tests with `@pytest.mark.integration`.

**Step B -- Add/verify regression tests in `tests/test_render.py`:**

Check if these tests already exist. If they do and pass, skip. If missing, add them:

4. `test_render_phase8_disabled_matches_phase7`: Create a SmoothingConfig with all Phase 8 features disabled (de_breathing_enabled=False, noise_floor_match_enabled=False, pose_match_enabled=False, rife_enabled=False). Assert the render pipeline produces the same behavior as Phase 7 (no extra passes called).

5. `test_render_skips_debreathing_when_disabled`: SmoothingConfig with de_breathing_enabled=False -> verify VAD detect_breath_extension is never called.

6. `test_render_skips_noise_floor_when_disabled`: SmoothingConfig with noise_floor_match_enabled=False -> verify librosa-based functions are never called.

7. `test_render_skips_pose_match_when_disabled`: SmoothingConfig with pose_match_enabled=False -> verify cv2/pose functions are never called.

Use monkeypatching or mock to verify the skip behavior without requiring actual GPU deps.
  </action>
  <verify>uv run pytest tests/test_pipeline.py tests/test_render.py -q -k "legacy or phase8 or editorial_action or disabled" -m "not integration"</verify>
  <done>Integration tests verify triage->review->render chain; legacy artifacts load cleanly; disabled Phase 8 matches Phase 7 baseline.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_pipeline.py tests/test_render.py -q -m "not integration"`
- `uv run ruff check scripts/smoke_test_gpu_rife.py docs/phase8-operator-guide.md`
- `test -f docs/phase8-operator-guide.md && wc -l docs/phase8-operator-guide.md`
</verification>

<success_criteria>
- GPU smoke test references torch 2.10.x baseline and cu128 install.
- Operator guide documents gemini-3-flash-lite default and explicitly forbids reasoning/CoT models.
- Operator guide references current dependency baselines (silero-vad 6.2, librosa 0.11, opencv 4.13, torch 2.10).
- Operator guide correctly states RIFE 4.26 exists but 4.25 recommended.
- Legacy artifacts without Phase 8 fields load and render correctly (regression tested).
- Phase 8 disabled = Phase 7 baseline (regression tested).
- End-to-end triage->review->render chain produces correct editorial actions.
</success_criteria>

<output>
After completion, create `.planning/phases/08-intelligent-cut-quality/08-06-SUMMARY.md`
</output>
