---
phase: 08-intelligent-cut-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/podcast_pipeline/config/settings.py
autonomous: true

must_haves:
  truths:
    - "FillerConfig.llm_triage_model defaults to 'gemini-3-flash-lite' (NOT 'gpt-4o-mini')"
    - "No OpenAI model names appear as default values anywhere in Phase 8 filler/triage config"
    - "All other Phase 8 config fields (disfluencies, hedge_words, protect_pause_threshold_ms, etc.) remain correct"
  artifacts:
    - path: "src/podcast_pipeline/config/settings.py"
      provides: "FillerConfig with llm_triage_model defaulting to gemini-3-flash-lite"
      contains: 'llm_triage_model.*gemini-3-flash-lite'
  key_links:
    - from: "src/podcast_pipeline/config/settings.py"
      to: "src/podcast_pipeline/stages/analyze.py"
      via: "analyze stage reads llm_triage_model from FillerConfig to select triage provider"
      pattern: "llm_triage_model|config\\.fillers"
---

<objective>
Fix FillerConfig.llm_triage_model default from 'gpt-4o-mini' to 'gemini-3-flash-lite' and verify all other Phase 8 config fields.

Purpose: Research doc identifies this as a HIGH severity bug. The spec says gemini-3-flash-lite or claude-haiku-4-5 -- NOT OpenAI models. The project's primary provider is Gemini, so gemini-3-flash-lite is the correct default.
Output: Single-line fix in settings.py, plus verification that all other Phase 8 config is correct.
</objective>

<execution_context>
@/home/kngpnn/.claude/get-shit-done/workflows/execute-plan.md
@/home/kngpnn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-intelligent-cut-quality/08-RESEARCH.md
@src/podcast_pipeline/config/settings.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix llm_triage_model default to gemini-3-flash-lite</name>
  <files>src/podcast_pipeline/config/settings.py</files>
  <action>
In `src/podcast_pipeline/config/settings.py`, find the `FillerConfig` class and locate:

```python
llm_triage_model: str = "gpt-4o-mini"
```

Change it to:

```python
llm_triage_model: str = "gemini-3-flash-lite"
```

This is a one-line change. Do NOT modify any other fields or logic in FillerConfig. The rest of the config is correct as verified by the 08-RESEARCH.md compatibility audit.

Rationale (from research): "The spec explicitly says gemini-3-flash-lite or claude-haiku-4-5. Do NOT use OpenAI models." The project's primary provider is Gemini, so gemini-3-flash-lite is the correct default.
  </action>
  <verify>uv run python -c "from podcast_pipeline.config.settings import FillerConfig; c = FillerConfig(); assert c.llm_triage_model == 'gemini-3-flash-lite', f'Expected gemini-3-flash-lite, got {c.llm_triage_model}'; print('OK: llm_triage_model =', c.llm_triage_model)"</verify>
  <done>FillerConfig.llm_triage_model defaults to 'gemini-3-flash-lite' instead of 'gpt-4o-mini'.</done>
</task>

<task type="auto">
  <name>Task 2: Verify all Phase 8 config fields are correct</name>
  <files>src/podcast_pipeline/config/settings.py</files>
  <action>
Run verification commands to confirm the remaining Phase 8 FillerConfig and SmoothingConfig fields are correct as-is. Do NOT change anything unless a verification fails.

Verify these conditions hold (all confirmed correct in 08-RESEARCH.md):
1. FillerConfig has disfluencies, hedge_words, custom_words, words sub-lists
2. FillerConfig.disfluencies defaults to ["um", "uh", "hmm", "er", "ah"]
3. FillerConfig.hedge_words defaults to ["like", "you know", "basically", "actually", "so"]
4. FillerConfig.protect_pause_threshold_ms defaults to 300.0
5. FillerConfig.enable_llm_triage defaults to True
6. FillerConfig.llm_triage_max_context_words defaults to 5
7. SmoothingConfig.rife_enabled defaults to False
8. SmoothingConfig.pose_match_enabled defaults to True
9. SmoothingConfig.de_breathing_enabled defaults to True
10. SmoothingConfig.noise_floor_match_enabled defaults to True

Run these as assertions. If any fail, fix that specific field. If all pass, this task is a no-op confirmation.
  </action>
  <verify>uv run python -c "
from podcast_pipeline.config.settings import FillerConfig, SmoothingConfig
c = FillerConfig()
assert c.disfluencies == ['um', 'uh', 'hmm', 'er', 'ah']
assert c.hedge_words == ['like', 'you know', 'basically', 'actually', 'so']
assert c.protect_pause_threshold_ms == 300.0
assert c.enable_llm_triage is True
assert c.llm_triage_max_context_words == 5
s = SmoothingConfig()
assert s.rife_enabled is False
assert s.pose_match_enabled is True
assert s.de_breathing_enabled is True
assert s.noise_floor_match_enabled is True
print('All Phase 8 config fields verified correct')
"</verify>
  <done>All existing Phase 8 config fields confirmed correct -- no additional changes needed.</done>
</task>

</tasks>

<verification>
- `uv run python -c "from podcast_pipeline.config.settings import FillerConfig; c = FillerConfig(); assert c.llm_triage_model == 'gemini-3-flash-lite'"`
- `uv run ruff check src/podcast_pipeline/config/settings.py`
- `uv run mypy src/podcast_pipeline/config/settings.py`
</verification>

<success_criteria>
- FillerConfig.llm_triage_model is 'gemini-3-flash-lite' (not 'gpt-4o-mini').
- All other Phase 8 config fields have correct defaults per spec and research.
- No ruff or mypy errors introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/08-intelligent-cut-quality/08-01-SUMMARY.md`
</output>
