---
phase: 02-research-+-viral-integration
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/podcast_pipeline/research/youtube.py
  - tests/test_research_keywords_cache.py
autonomous: true

must_haves:
  truths:
    - "Keyword recommendations prioritize meaningful n-grams and filter low-signal stopwords."
    - "Repeated research queries reuse cached API responses inside TTL to avoid quota spikes."
  artifacts:
    - path: src/podcast_pipeline/research/youtube.py
      provides: "Weighted n-gram keyword extractor and TTL cache layer"
      min_lines: 90
    - path: tests/test_research_keywords_cache.py
      provides: "Cache and keyword quality regression tests"
      min_lines: 70
  key_links:
    - from: src/podcast_pipeline/research/youtube.py
      to: suggested_keywords
      via: "weighted keyword extraction pipeline"
      pattern: "suggested_keywords"
    - from: src/podcast_pipeline/research/youtube.py
      to: search_videos
      via: "cache lookup before API request"
      pattern: "cache"
---

<objective>
Improve keyword quality and harden YouTube query behavior with cache protection.

Purpose: Deliver actionable keyword seeds while reducing repeated API cost.
Output: Weighted keyword extraction and TTL-backed research query caching.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-research-+-viral-integration/02-RESEARCH.md
@.planning/phases/02-research-+-viral-integration/02-01-PLAN.md

# Key files
@src/podcast_pipeline/research/youtube.py
@tests/test_research.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add normalized TTL cache for research API calls</name>
  <files>src/podcast_pipeline/research/youtube.py</files>
  <action>
Implement query-result caching keyed by normalized request parameters (`query`, `order`, `max_results`, date window). Cache `search_videos` and stats lookups with explicit TTL and safe serialization so repeated analyze runs avoid duplicate YouTube calls during cache validity.
  </action>
  <verify>python -m pytest tests/test_research_keywords_cache.py -q</verify>
  <done>Repeated equivalent queries hit cache instead of issuing additional API requests within TTL.</done>
</task>

<task type="auto">
  <name>Task 2: Replace naive keyword extraction with weighted n-grams + stopwords</name>
  <files>src/podcast_pipeline/research/youtube.py</files>
  <action>
Refactor `get_trending_keywords` to tokenize titles/descriptions with regex, filter stopwords/noise tokens, compute weighted unigram/bigram/trigram scores, and return top keywords deterministically. Bias toward recurring multi-word phrases relevant for titles/thumbnails.
  </action>
  <verify>python -m pytest tests/test_research_keywords_cache.py -q</verify>
  <done>Top keyword results avoid filler terms and include recurring high-signal phrases.</done>
</task>

<task type="auto">
  <name>Task 3: Add cache and keyword quality tests</name>
  <files>tests/test_research_keywords_cache.py</files>
  <action>
Add tests that validate cache hit/miss behavior, TTL expiry handling, and keyword ranking quality on controlled sample titles/descriptions. Include assertions that stopwords are excluded and multi-word phrases can outrank weak unigrams.
  </action>
  <verify>python -m pytest tests/test_research_keywords_cache.py -q</verify>
  <done>Regression tests guard cache behavior and keyword ranking semantics.</done>
</task>

</tasks>

<verification>
- API call count is reduced for repeated equivalent queries.
- Suggested keywords reflect weighted n-gram relevance with stopword filtering.
</verification>

<success_criteria>
- Cache layer prevents avoidable quota spikes
- Keyword extraction output is materially more actionable
- Behavior is locked by deterministic tests
</success_criteria>

<output>
After completion, create `.planning/phases/02-research-+-viral-integration/02-03-SUMMARY.md`
</output>
