---
phase: 02-research-+-viral-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/podcast_pipeline/research/youtube.py
  - tests/test_research_metrics.py
autonomous: true

must_haves:
  truths:
    - "Research artifacts include engagement-rate and velocity metrics for analyzed videos."
    - "Research artifacts include competition scoring and recommended posting windows."
  artifacts:
    - path: src/podcast_pipeline/research/youtube.py
      provides: "Enriched research metrics and insight fields"
      min_lines: 80
    - path: tests/test_research_metrics.py
      provides: "Deterministic tests for metrics and posting-pattern logic"
      min_lines: 60
  key_links:
    - from: src/podcast_pipeline/research/youtube.py
      to: ResearchResult.insights
      via: "research_topic -> _generate_insights"
      pattern: "_generate_insights"
    - from: src/podcast_pipeline/research/youtube.py
      to: trending_videos[*]
      via: "per-video metric enrichment"
      pattern: "engagement|velocity"
---

<objective>
Upgrade YouTube research output with meaningful performance metrics and competition/posting insights.

Purpose: Ensure Phase 2 recommendations are grounded in measurable engagement momentum and publish-time behavior.
Output: Enriched `ResearchResult` fields and test coverage for metric correctness.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-research-+-viral-integration/02-RESEARCH.md

# Key files
@src/podcast_pipeline/research/youtube.py
@tests/test_research.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add per-video engagement and velocity metrics</name>
  <files>src/podcast_pipeline/research/youtube.py</files>
  <action>
Add helper logic that computes per-video metrics from search/stat payloads: engagement rate, hours since publish, and velocity-per-hour. Parse `published_at` safely (invalid values should degrade to defaults, not crash). Persist these computed values in each video row before aggregate insights are computed.
  </action>
  <verify>python -m pytest tests/test_research_metrics.py -q</verify>
  <done>Each analyzed video entry includes deterministic engagement and velocity fields used for later ranking.</done>
</task>

<task type="auto">
  <name>Task 2: Add competition score and posting-pattern outputs</name>
  <files>src/podcast_pipeline/research/youtube.py</files>
  <action>
Extend `ResearchResult`/`insights` generation with competition and posting analytics: topic-level competition score, engagement benchmarks, and best posting time windows derived from `published_at` distributions. Keep the outputs JSON-serializable and stable for UI rendering.
  </action>
  <verify>python -m pytest tests/test_research_metrics.py -q</verify>
  <done>`research.json` schema contains competition and posting insights that can be consumed by Analyze/UI without extra parsing glue.</done>
</task>

<task type="auto">
  <name>Task 3: Add targeted tests for metric and insight calculations</name>
  <files>tests/test_research_metrics.py</files>
  <action>
Create focused tests for metric calculations, zero/hidden-like edge cases, and posting-pattern summaries. Validate that competition score and best posting windows are present and bounded in expected ranges.
  </action>
  <verify>python -m pytest tests/test_research_metrics.py -q</verify>
  <done>Regression tests fail if engagement, velocity, competition, or posting-time calculations drift unexpectedly.</done>
</task>

</tasks>

<verification>
- `research.json` includes engagement, velocity, competition, and posting-time analytics.
- Metric helpers handle sparse stats and malformed publish timestamps safely.
</verification>

<success_criteria>
- Research artifacts contain the new phase-required analytics fields
- Insights are deterministic and test-covered
- No regressions in existing research behavior
</success_criteria>

<output>
After completion, create `.planning/phases/02-research-+-viral-integration/02-01-SUMMARY.md`
</output>
