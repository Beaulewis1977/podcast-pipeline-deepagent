---
phase: 02-research-+-viral-integration
plan: 04
type: execute
wave: 2
depends_on: ["02"]
files_modified:
  - src/podcast_pipeline/stages/analyze.py
  - tests/test_analyze_ranking.py
autonomous: true

must_haves:
  truths:
    - "Analyze stage produces clip rankings based on combined AI and detector scores."
    - "Combined clip scores are bounded to 10 and include score-component explainability."
  artifacts:
    - path: src/podcast_pipeline/stages/analyze.py
      provides: "Re-ranking pipeline and combined score payload"
      min_lines: 80
    - path: tests/test_analyze_ranking.py
      provides: "Analyze-stage ranking and score-cap regression tests"
      min_lines: 70
  key_links:
    - from: src/podcast_pipeline/stages/analyze.py
      to: src/podcast_pipeline/research/viral_detector.py
      via: "detector.score_clip used in re-ranking"
      pattern: "score_clip"
    - from: src/podcast_pipeline/stages/analyze.py
      to: analysis/viral_signals.json
      via: "persisted combined ranking payload"
      pattern: "viral_signals.json"
---

<objective>
Re-rank AI-proposed clips using detector signals and persist explainable combined scoring artifacts.

Purpose: Ensure viral recommendations reflect both model output and transcript-level signal quality.
Output: Sorted combined clip rankings in analyze artifacts and protected test coverage.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-research-+-viral-integration/02-RESEARCH.md
@.planning/phases/02-research-+-viral-integration/02-02-PLAN.md

# Key files
@src/podcast_pipeline/stages/analyze.py
@src/podcast_pipeline/research/viral_detector.py
@tests/test_integration.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add combined-score clip re-ranking in AnalyzeStage</name>
  <files>src/podcast_pipeline/stages/analyze.py</files>
  <action>
After detector scoring, compute combined score per clip using provider score + detector score with documented weights, cap final score at 10, and sort selected clip list by combined score descending. Preserve original provider values in the payload for transparency.
  </action>
  <verify>python -m pytest tests/test_analyze_ranking.py -q</verify>
  <done>Analyze artifacts expose stable combined rankings and clip order changes when detector evidence differs.</done>
</task>

<task type="auto">
  <name>Task 2: Persist explainable score components in viral artifact output</name>
  <files>src/podcast_pipeline/stages/analyze.py</files>
  <action>
Update `viral_signals.json` structure to include `ai_score`, `detector_score`, `combined_score`, and concise reasons per clip while preserving backward-compatible fields consumed by the UI. Ensure schema remains JSON-safe.
  </action>
  <verify>python -m pytest tests/test_analyze_ranking.py -q</verify>
  <done>Score breakdown is persisted per clip and can be rendered directly without extra recomputation.</done>
</task>

<task type="auto">
  <name>Task 3: Add analyze-stage ranking tests</name>
  <files>tests/test_analyze_ranking.py</files>
  <action>
Add tests for ordering by combined score, hard cap behavior, and fallback behavior when provider or detector scores are missing. Use fixtures/mocks so tests do not require network calls.
  </action>
  <verify>python -m pytest tests/test_analyze_ranking.py -q</verify>
  <done>Re-ranking behavior is deterministic and guarded against future regression.</done>
</task>

</tasks>

<verification>
- Analyze output clip order reflects combined AI + detector scoring.
- Combined scores never exceed defined bounds and include explainability fields.
</verification>

<success_criteria>
- Clip ranking is materially informed by detector signals
- Score components are persisted for downstream UI visibility
- Ranking logic is test-covered and deterministic
</success_criteria>

<output>
After completion, create `.planning/phases/02-research-+-viral-integration/02-04-SUMMARY.md`
</output>
