---
phase: 06-audio-video-enhancement-podcast-video-platform
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/podcast_pipeline/stages/render.py
  - src/podcast_pipeline/config/settings.py
  - config.yaml
  - tests/test_render.py
autonomous: true

must_haves:
  truths:
    - "Video color correction uses verified FFmpeg filters (`normalize`, `grayworld`, optional `eq`) with no undocumented filter names"
    - "Color correction is fully opt-in and does not alter existing aspect-ratio/crop/export behavior when disabled"
    - "Enhancement-only phase boundary is preserved (no cut-graph/crossfade/filler-control modifications)"
  artifacts:
    - path: "src/podcast_pipeline/stages/render.py"
      provides: "Color-correction filter builder integrated into video filter pipeline"
      min_lines: 140
    - path: "src/podcast_pipeline/config/settings.py"
      provides: "Typed color-correction settings and parameter validation"
      min_lines: 40
    - path: "config.yaml"
      provides: "Operator-facing defaults for color correction tuning"
      min_lines: 20
    - path: "tests/test_render.py"
      provides: "Regression tests for enabled/disabled color filter behavior and canonical filter names"
      min_lines: 90
  key_links:
    - from: "src/podcast_pipeline/config/settings.py"
      to: "src/podcast_pipeline/stages/render.py"
      via: "color-correction config drives canonical FFmpeg filter chain generation"
      pattern: "color_correction|normalize|grayworld|eq"
    - from: "src/podcast_pipeline/stages/render.py"
      to: "tests/test_render.py"
      via: "tests assert filter ordering and no-op behavior when disabled"
      pattern: "_build_color_correction_filters|_build_video_filters"
---

<objective>
Implement low-risk video color normalization/correction as an optional enhancement path.

Purpose: Improve visual consistency for webcam footage without changing render-core edit behavior.
Output: Canonical FFmpeg color filter chain + typed config + regression tests.
</objective>

<execution_note>
Wave 2 sequencing note: `06-03` is dependency-parallel with `06-02` but both plans touch `src/podcast_pipeline/stages/render.py` and `tests/test_render.py`. Prefer sequential execution in one branch (or isolated worktrees with an explicit merge step) to reduce conflicts.
</execution_note>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-audio-video-enhancement-podcast-video-platform/06-RESEARCH.md
@.planning/phases/06-audio-video-enhancement-podcast-video-platform/06-01-PLAN.md
@src/podcast_pipeline/stages/render.py
@src/podcast_pipeline/config/settings.py
@config.yaml
@tests/test_render.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add canonical color-correction filter builder</name>
  <files>src/podcast_pipeline/stages/render.py</files>
  <action>Implement `_build_color_correction_filters()` using verified FFmpeg filters (`normalize`, `grayworld`, and optional `eq` adjustments) and wire it into the video filter chain only when enabled. Keep filter order deterministic and compatible with existing scale/crop/pad flow.</action>
  <verify>uv run pytest tests/test_render.py -q -k "color_correction and (normalize or grayworld or eq)"</verify>
  <done>Color correction emits only canonical, documented filter names and integrates safely into render flow.</done>
</task>

<task type="auto">
  <name>Task 2: Add typed color-correction config and defaults</name>
  <files>src/podcast_pipeline/config/settings.py
config.yaml</files>
  <action>Define typed settings for color correction (`enabled`, `normalize`, `grayworld`, and bounded `eq` tuning params). Keep defaults conservative and disabled to preserve current output behavior until explicitly enabled.</action>
  <verify>uv run pytest tests/test_render.py -q -k "color_config and (defaults or bounds)"</verify>
  <done>Operators can safely tune color correction via validated config without hidden behavior changes.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests for color-path safety and scope boundaries</name>
  <files>tests/test_render.py</files>
  <action>Add tests for enabled/disabled color filter chain generation, canonical filter usage, and no impact on edit-plan/cut-graph behavior when color correction is toggled. Ensure tests explicitly reject unsupported legacy names (`autowhite`, `autolevels`).</action>
  <verify>uv run pytest tests/test_render.py -q -k "color and (disabled_noop or canonical_filters or no_edit_graph_impact)"</verify>
  <done>Color correction behavior is deterministic, safe, and insulated from Phase 7 editing concerns.</done>
</task>

</tasks>

<verification>
Video checks: color filters are canonical and opt-in, existing geometry/export logic is preserved, and enhancement scope stays isolated.
</verification>

<success_criteria>
- Color-correction path uses only verified FFmpeg filter names.
- Disabled defaults preserve existing render outputs and edit behavior.
- Regression tests prevent reintroduction of undocumented filters or scope bleed.
</success_criteria>

<output>
After completion, create `.planning/phases/06-audio-video-enhancement-podcast-video-platform/06-03-SUMMARY.md`
</output>
