---
phase: 06-audio-video-enhancement-podcast-video-platform
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/podcast_pipeline/stages/render.py
  - pyproject.toml
  - tests/test_render.py
autonomous: true

must_haves:
  truths:
    - "Audio enhancement chain uses FFmpeg-native de-essing (`deesser`) plus final click-safety cleanup (`adeclick`) instead of custom split-band DSP logic"
    - "Optional dereverb path is gated by config and dependency availability, with deterministic behavior when disabled or unavailable"
    - "Phase 6 does not introduce Demucs or other heavy archived/unmaintained dereverb dependencies"
  artifacts:
    - path: "src/podcast_pipeline/stages/render.py"
      provides: "Integrated de-esser + adeclick audio chain and optional dereverb preprocessing with guarded fallbacks"
      min_lines: 200
    - path: "pyproject.toml"
      provides: "Optional dependency contract for lightweight dereverb path"
      min_lines: 5
    - path: "tests/test_render.py"
      provides: "Regression coverage for de-esser and dereverb behavior under enabled/disabled/missing-dependency conditions"
      min_lines: 120
  key_links:
    - from: "src/podcast_pipeline/config/settings.py"
      to: "src/podcast_pipeline/stages/render.py"
      via: "deesser/dereverb config drives enhancement path selection and fallback handling"
      pattern: "deesser|dereverb|enabled"
    - from: "pyproject.toml"
      to: "src/podcast_pipeline/stages/render.py"
      via: "optional dependency path consumed only when dereverb is enabled"
      pattern: "optional-dependencies|noisereduce"
---

<objective>
Implement Phase 6 audio enhancement behavior with low-risk, FFmpeg-first defaults.

Purpose: Improve speech harshness/noise handling without destabilizing render core behavior.
Output: Native de-esser integration + optional dereverb path + regression coverage.
</objective>

<execution_note>
Wave 2 sequencing note: `06-02` and `06-03` are dependency-parallel but both modify `src/podcast_pipeline/stages/render.py` and `tests/test_render.py`. Execute sequentially in one branch (or in isolated worktrees with an explicit merge step) to avoid conflict churn.
</execution_note>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-audio-video-enhancement-podcast-video-platform/06-RESEARCH.md
@.planning/phases/06-audio-video-enhancement-podcast-video-platform/06-01-PLAN.md
@src/podcast_pipeline/stages/render.py
@pyproject.toml
@tests/test_render.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate FFmpeg-native de-esser and final click-safety into audio chain</name>
  <files>src/podcast_pipeline/stages/render.py
tests/test_render.py</files>
  <action>Add a dedicated de-esser filter builder and ensure final click-safety (`adeclick`) is explicitly integrated in chain order. Preserve deterministic ordering (dialog cleanup -> de-esser -> loudness/limiter -> adeclick safety) and keep behavior unchanged when the corresponding enhancements are disabled.</action>
  <verify>uv run pytest tests/test_render.py -q -k "deesser or adeclick or audio_chain_ordering"</verify>
  <done>De-esser and click-safety settings map cleanly to FFmpeg filter arguments with deterministic ordering and safe disabled behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Implement optional lightweight dereverb path with guarded fallback</name>
  <files>src/podcast_pipeline/stages/render.py
pyproject.toml
tests/test_render.py</files>
  <action>Add optional dereverb preprocessing using `noisereduce` as a lightweight path, gated by config (`enabled: false` default). Keep behavior deterministic when dependency is missing (clear warning + safe skip or explicit controlled failure based on configured policy). Do not add Demucs integration in Phase 6.</action>
  <verify>uv run pytest tests/test_render.py -q -k "dereverb and (disabled or noisereduce or fallback)"</verify>
  <done>Dereverb path is opt-in, robust when dependency is unavailable, and absent from baseline render flow.</done>
</task>

<task type="auto">
  <name>Task 3: Add audio enhancement regression coverage and guardrails</name>
  <files>tests/test_render.py</files>
  <action>Add tests that validate chain ordering (including `adeclick` placement), argument generation, and no-op behavior for disabled settings. Include edge-case tests ensuring enhancement toggles do not break existing platform render outputs or stage status truthfulness.</action>
  <verify>uv run pytest tests/test_render.py -q -k "audio_enhancement and (ordering or no_op or platform_safe)"</verify>
  <done>Audio enhancements are test-locked for predictable behavior across baseline and opt-in paths.</done>
</task>

</tasks>

<verification>
Audio checks: de-esser path is FFmpeg-native, dereverb is optional and resilient, baseline behavior is preserved when enhancements are off.
</verification>

<success_criteria>
- Native de-esser and final click-safety (`adeclick`) are configurable and correctly integrated into render audio chain.
- Optional dereverb is safe, dependency-aware, and disabled by default.
- Tests prove no regressions in baseline render behavior when enhancements are off.
</success_criteria>

<output>
After completion, create `.planning/phases/06-audio-video-enhancement-podcast-video-platform/06-02-SUMMARY.md`
</output>
