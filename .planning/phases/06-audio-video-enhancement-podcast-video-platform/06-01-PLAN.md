---
phase: 06-audio-video-enhancement-podcast-video-platform
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/podcast_pipeline/config/settings.py
  - config.yaml
  - src/podcast_pipeline/stages/render.py
  - tests/test_render.py
autonomous: true

must_haves:
  truths:
    - "Phase 6 enhancement configuration is explicit and typed, with a deliberate default policy: conservative de-esser enabled, heavy enhancements opt-in"
    - "Render performs capability checks for enabled FFmpeg filters and fails fast with actionable diagnostics when the local binary lacks required filters"
    - "Phase 6 scope remains enhancement-only (no crossfade/word-boundary/filler-decision render-core rewrites)"
  artifacts:
    - path: "src/podcast_pipeline/config/settings.py"
      provides: "Typed enhancement config models and validation for de-esser, dereverb, and color correction"
      min_lines: 120
    - path: "config.yaml"
      provides: "Operator-visible defaults for enhancement toggles and tuning parameters"
      min_lines: 40
    - path: "src/podcast_pipeline/stages/render.py"
      provides: "FFmpeg filter-capability probe and fail-fast guard rails"
      min_lines: 120
    - path: "tests/test_render.py"
      provides: "Regression coverage for config defaults and capability-check failure semantics"
      min_lines: 80
  key_links:
    - from: "config.yaml"
      to: "src/podcast_pipeline/config/settings.py"
      via: "typed enhancement sections parsed by `load_config()`"
      pattern: "deesser|dereverb|color_correction"
    - from: "src/podcast_pipeline/config/settings.py"
      to: "src/podcast_pipeline/stages/render.py"
      via: "enabled enhancement flags drive runtime FFmpeg filter capability checks"
      pattern: "enabled|_ensure_filter_capabilities"
---

<objective>
Define the enhancement configuration contract and enforce runtime capability guards before enabling any new filter behavior.

Purpose: Prevent ambiguous config and late FFmpeg runtime surprises.
Output: Typed config sections + render preflight checks + deterministic tests.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-audio-video-enhancement-podcast-video-platform/06-RESEARCH.md
@docs/plans/2026-02-19-audio-video-enhancement-plan.md
@src/podcast_pipeline/config/settings.py
@config.yaml
@src/podcast_pipeline/stages/render.py
@tests/test_render.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed enhancement config models with safe defaults</name>
  <files>src/podcast_pipeline/config/settings.py
config.yaml</files>
  <action>Add typed settings for `deesser`, `dereverb`, and `color_correction` with explicit `enabled` toggles and validated parameter ranges. Align defaults with the updated phase plan: `deesser.enabled: true` (conservative baseline), `dereverb.enabled: false`, `color_correction.enabled: false`. Keep Demucs out of scope for Phase 6.</action>
  <verify>uv run pytest tests/test_render.py -q -k "config and (deesser or dereverb or color_correction)"</verify>
  <done>Enhancement behavior is fully controlled by typed config and default policy is explicit and test-locked (`deesser` on, heavier options off).</done>
</task>

<task type="auto">
  <name>Task 2: Add FFmpeg filter capability preflight for enabled enhancements</name>
  <files>src/podcast_pipeline/stages/render.py
tests/test_render.py</files>
  <action>Implement a render-stage helper that checks local FFmpeg filter availability (for enabled paths only) using `ffmpeg -filters`/probe output and fails fast with actionable error messages. Require: `deesser`, `grayworld`, `normalize`, `adeclick`, and any additional enabled enhancement filters. Cache probe results per process to avoid repeated subprocess overhead.</action>
  <verify>uv run pytest tests/test_render.py -q -k "ffmpeg_filter_capability or fail_fast"</verify>
  <done>Missing required filters are reported before render execution with explicit remediation guidance.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests for scope boundary and defaults</name>
  <files>tests/test_render.py</files>
  <action>Add tests that assert Phase 6 configuration does not activate crossfade/edit-core behaviors and that enhancement settings remain no-op when disabled. Ensure tests lock the enhancement-only scope boundary so later work cannot silently mix Phase 7 concerns into Phase 6 execution.</action>
  <verify>uv run pytest tests/test_render.py -q -k "phase6_scope_boundary or enhancement_disabled_noop"</verify>
  <done>Test suite guards against scope creep and accidental behavioral drift.</done>
</task>

</tasks>

<verification>
Config and preflight checks: enhancement sections parse, required filters are validated when enabled, and default policy is deterministic across config/tests.
</verification>

<success_criteria>
- Enhancement config models are typed, validated, and documented in defaults.
- Render fails fast on missing required FFmpeg filters for enabled options.
- Scope boundary tests prevent crossfade/edit-core work from entering Phase 6.
</success_criteria>

<output>
After completion, create `.planning/phases/06-audio-video-enhancement-podcast-video-platform/06-01-SUMMARY.md`
</output>
