---
phase: 06.1-video-marketing-copy-thumbnail-visual-selection
plan: 03
type: execute
wave: 2
depends_on: ["01"]
gap_closure: false
files_modified:
  - src/podcast_pipeline/models/analysis.py
  - src/podcast_pipeline/providers/base.py
  - src/podcast_pipeline/stages/review.py
  - tests/test_models.py
  - tests/test_review.py
  - tests/test_providers.py
autonomous: true

must_haves:
  truths:
    - "Thumbnail candidates carry explicit virality metadata (`virality_score`, recommendation context)"
    - "Review state supports ranked multi-selection (`selected_thumbnails`) with legacy compatibility via `selected_thumbnail` mirror"
    - "Backward compatibility for existing review_state payloads is preserved"
  artifacts:
    - path: "src/podcast_pipeline/models/analysis.py"
      provides: "Thumbnail candidate schema supports virality metadata"
      min_lines: 25
    - path: "src/podcast_pipeline/providers/base.py"
      provides: "Prompt schema requests virality-aware thumbnail candidate fields"
      min_lines: 35
    - path: "src/podcast_pipeline/stages/review.py"
      provides: "ReviewDecisions supports ordered 1..3 thumbnail selections with compatibility shim"
      min_lines: 45
    - path: "tests/test_models.py"
      provides: "Model tests for thumbnail virality schema"
      min_lines: 25
    - path: "tests/test_review.py"
      provides: "Review-state normalization tests for selected_thumbnails compatibility"
      min_lines: 40
    - path: "tests/test_providers.py"
      provides: "Provider parse tests for thumbnail virality fields"
      min_lines: 25
  key_links:
    - from: "src/podcast_pipeline/providers/base.py"
      to: "src/podcast_pipeline/models/analysis.py"
      via: "Provider thumbnail JSON schema matches `ThumbnailCandidate` fields"
      pattern: "thumbnail_frames|virality_score|viral"
    - from: "src/podcast_pipeline/stages/review.py"
      to: "src/podcast_pipeline/models/analysis.py"
      via: "Review defaults/selectors map to analysis thumbnail indexes while preserving legacy scalar"
      pattern: "selected_thumbnails|selected_thumbnail"
---

<objective>
Establish the thumbnail MVP data contract: virality-aware candidates plus ranked multi-selection state with backward compatibility.

Purpose: Ensure later UI/render work is built on stable, testable contracts instead of ad hoc state mutations.
Output: Updated schema + provider prompt + review model normalization with regressions.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-video-marketing-copy-thumbnail-visual-selection/06.1-RESEARCH.md
@src/podcast_pipeline/models/analysis.py
@src/podcast_pipeline/providers/base.py
@src/podcast_pipeline/stages/review.py
@tests/test_models.py
@tests/test_review.py
@tests/test_providers.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend thumbnail candidate schema with virality metadata</name>
  <files>src/podcast_pipeline/models/analysis.py
tests/test_models.py</files>
  <action>Add virality-focused fields to `ThumbnailCandidate` (for example `virality_score`, `viral_style`, and score source metadata) with bounded validation and safe defaults. Add model tests for valid/invalid payload handling.</action>
  <verify>uv run pytest tests/test_models.py -q -k "ThumbnailCandidate and virality"</verify>
  <done>`ThumbnailCandidate` supports virality metadata and validation rejects malformed score payloads.</done>
</task>

<task type="auto">
  <name>Task 2: Update provider thumbnail schema to request viral-style signals</name>
  <files>src/podcast_pipeline/providers/base.py
tests/test_providers.py</files>
  <action>Extend `_build_prompt()` thumbnail schema/instructions so providers return virality metadata per candidate and explicitly include at least one strong recommendation signal when candidates exist.</action>
  <verify>uv run pytest tests/test_providers.py -q -k "thumbnail and provider"</verify>
  <done>Provider prompt contract and parsing path accept the new thumbnail virality fields.</done>
</task>

<task type="auto">
  <name>Task 3: Evolve review decisions to ranked 1..3 selection with legacy mirror</name>
  <files>src/podcast_pipeline/stages/review.py
tests/test_review.py</files>
  <action>Add ordered `selected_thumbnails: list[int]` to `ReviewDecisions` (unique, max 3), normalize old payloads by hydrating from `selected_thumbnail`, and keep `selected_thumbnail` synchronized as the primary selection mirror.</action>
  <verify>uv run pytest tests/test_review.py -q -k "selected_thumbnail or selected_thumbnails"</verify>
  <done>Old and new review-state payloads validate, with deterministic primary thumbnail mapping preserved.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_models.py tests/test_providers.py tests/test_review.py -q`
</verification>

<success_criteria>
- Thumbnail schema includes virality metadata fields with validation.
- Provider prompt schema requests matching thumbnail virality output.
- Review decisions support ordered 1..3 thumbnail selections with backward-compatible scalar mirroring.
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-video-marketing-copy-thumbnail-visual-selection/06.1-03-SUMMARY.md`
</output>
