---
phase: 06.1-video-marketing-copy-thumbnail-visual-selection
plan: 01
type: execute
wave: 1
depends_on: []
gap_closure: false
files_modified:
  - src/podcast_pipeline/models/analysis.py
  - src/podcast_pipeline/providers/base.py
  - src/podcast_pipeline/providers/kimi.py
  - src/podcast_pipeline/stages/analyze.py
  - tests/test_models.py
  - tests/test_providers.py
autonomous: true

must_haves:
  truths:
    - "Marketing schema covers the full supported marketing platform matrix: `youtube`, `spotify`, `spotify_video`, `apple`, `apple_video`, `tiktok`, `instagram`, `linkedin`, `twitter`, `facebook`"
    - "Provider prompt contract explicitly requires LLM-generated, viral-style but professional copy for every marketing platform key"
    - "When research artifacts exist, prompt context includes trend signals (`analysis/research.json`, `analysis/viral_signals.json`) so copy can align with current topic momentum"
    - "Prompt contract includes explicit platform-tailored title/description/hashtag expectations (not generic one-size copy)"
    - "Regression tests lock schema/prompt parity so marketing keys cannot silently drift or disappear"
  artifacts:
    - path: "src/podcast_pipeline/models/analysis.py"
      provides: "`MarketingCopy` includes complete cross-platform marketing keys, including `spotify_video` and `apple_video`"
      min_lines: 35
    - path: "src/podcast_pipeline/providers/base.py"
      provides: "Base prompt includes full marketing schema + explicit LLM quality instructions per platform"
      min_lines: 70
    - path: "src/podcast_pipeline/providers/kimi.py"
      provides: "Kimi provider prompt path remains schema-aligned with base contract"
      min_lines: 10
    - path: "src/podcast_pipeline/stages/analyze.py"
      provides: "Analyze stage passes available research/trend context into provider prompt path"
      min_lines: 40
    - path: "tests/test_models.py"
      provides: "Model-level contract tests for complete marketing platform key coverage"
      min_lines: 30
    - path: "tests/test_providers.py"
      provides: "Provider prompt tests validate platform coverage + viral-professional LLM instruction clauses"
      min_lines: 45
  key_links:
    - from: "src/podcast_pipeline/providers/base.py"
      to: "src/podcast_pipeline/models/analysis.py"
      via: "Provider marketing JSON keys exactly match `MarketingCopy` fields for all supported marketing platforms"
      pattern: "youtube|spotify|spotify_video|apple|apple_video|tiktok|instagram|linkedin|twitter|facebook"
    - from: "src/podcast_pipeline/providers/kimi.py"
      to: "src/podcast_pipeline/providers/base.py"
      via: "Provider request format remains aligned to base schema contract"
      pattern: "_build_prompt|marketing"
    - from: "src/podcast_pipeline/stages/analyze.py"
      to: "src/podcast_pipeline/providers/base.py"
      via: "Analyze stage injects optional trend/research context into prompt build path before provider call"
      pattern: "research|viral_signals|prompt"
---

<objective>
Lock an end-to-end LLM marketing contract for all supported platforms at the model/provider boundary.

Purpose: Prevent schema drift and guarantee the provider is asked for high-level viral-but-professional copy for every supported platform, including Spotify/Apple video variants.
Output: Model + prompt contract updates and regressions proving full-platform key retention and LLM instruction quality coverage.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-video-marketing-copy-thumbnail-visual-selection/06.1-RESEARCH.md
@prompts/001-phase-6-1-video-podcast-marketing-copy.md
@src/podcast_pipeline/models/analysis.py
@src/podcast_pipeline/providers/base.py
@src/podcast_pipeline/providers/kimi.py
@src/podcast_pipeline/stages/analyze.py
@tests/test_models.py
@tests/test_providers.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand `MarketingCopy` into a full platform matrix contract</name>
  <files>src/podcast_pipeline/models/analysis.py
tests/test_models.py</files>
  <action>Update `MarketingCopy` so contract keys map to the complete supported marketing surface (`youtube`, `spotify`, `spotify_video`, `apple`, `apple_video`, `tiktok`, `instagram`, `linkedin`, `twitter`, `facebook`) with additive defaults. Add model tests proving all keys validate and survive model dump/load cycles.</action>
  <verify>uv run pytest tests/test_models.py -q -k "MarketingCopy or AnalysisResult or marketing"</verify>
  <done>Model contract exposes complete platform coverage and tests fail on any missing/drifting marketing key.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild provider marketing prompt contract for high-quality LLM output</name>
  <files>src/podcast_pipeline/providers/base.py
src/podcast_pipeline/providers/kimi.py</files>
  <action>Update the prompt schema/instructions so the LLM must generate platform-specific marketing payloads for every supported marketing key, including `spotify_video` and `apple_video`. Add explicit style constraints: high-impact/viral framing without clickbait, professional tone, and platform-tailored field expectations for titles/descriptions/hashtags. Extend prompt-build contract to accept optional trend context input (keywords, trending hooks, competitive angle) when available.</action>
  <verify>uv run pytest tests/test_providers.py -q -k "provider and (prompt or parse or marketing)"</verify>
  <done>Provider prompt contract requests complete, platform-tailored, trend-aware LLM-authored copy (not generic or omitted sections).</done>
</task>

<task type="auto">
  <name>Task 3: Wire trend-context prompt input and add regressions</name>
  <files>src/podcast_pipeline/stages/analyze.py
tests/test_providers.py
tests/test_models.py</files>
  <action>Wire analyze flow to load and pass trend context from available research artifacts into provider prompt generation without breaking no-research runs. Add regressions asserting: (1) provider prompt marketing schema includes every required platform key, (2) prompt includes explicit viral-professional quality directives, (3) trend context injection is used when artifacts exist, and (4) validated provider payloads preserve all keys through `AnalysisResult` model validation.</action>
  <verify>uv run pytest tests/test_models.py tests/test_providers.py -q</verify>
  <done>Regression suite fails if platform coverage, quality directives, or trend-context prompt wiring regress.</done>
</task>

</tasks>

<verification>
Run focused contract checks:
- `uv run pytest tests/test_models.py tests/test_providers.py -q`
</verification>

<success_criteria>
- `MarketingCopy` includes all supported marketing platform keys, including `spotify_video` and `apple_video`.
- Provider prompt schema requests marketing payloads for the full platform matrix.
- Provider prompt includes explicit high-level viral-but-professional LLM copy directives.
- Analyze stage passes research/trend context into prompt generation when available, with safe fallback when unavailable.
- Regression tests lock prompt/schema parity, trend-context wiring, and cross-platform key retention.
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-video-marketing-copy-thumbnail-visual-selection/06.1-01-SUMMARY.md`
</output>
