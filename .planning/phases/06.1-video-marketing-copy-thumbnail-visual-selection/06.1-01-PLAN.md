---
phase: 06.1-video-marketing-copy-thumbnail-visual-selection
plan: 01
type: execute
wave: 1
depends_on: []
gap_closure: false
files_modified:
  - src/podcast_pipeline/models/analysis.py
  - src/podcast_pipeline/providers/base.py
  - src/podcast_pipeline/stages/render.py
  - src/podcast_pipeline/ui/app.py
  - tests/test_render.py
autonomous: true

must_haves:
  truths:
    - "Marketing copy for `spotify_video` and `apple_video` survives provider output -> model validation -> analysis artifact persistence"
    - "Render marketing document includes explicit Spotify Video and Apple Podcasts Video sections"
    - "Streamlit marketing editor exposes editable sections for `spotify_video` and `apple_video`"
    - "Regression coverage locks the new marketing-copy keys and rendering behavior"
  artifacts:
    - path: "src/podcast_pipeline/models/analysis.py"
      provides: "`MarketingCopy` model fields for `spotify_video` and `apple_video`"
      min_lines: 20
    - path: "src/podcast_pipeline/providers/base.py"
      provides: "Provider prompt schema requests `spotify_video` and `apple_video` copy payloads"
      min_lines: 40
    - path: "src/podcast_pipeline/stages/render.py"
      provides: "Marketing document platform list includes new video platforms"
      min_lines: 40
    - path: "src/podcast_pipeline/ui/app.py"
      provides: "Marketing editor platform list includes new video platforms"
      min_lines: 60
    - path: "tests/test_render.py"
      provides: "Tests proving marketing-doc sections and model round-trip key retention"
      min_lines: 80
  key_links:
    - from: "src/podcast_pipeline/providers/base.py"
      to: "src/podcast_pipeline/models/analysis.py"
      via: "AI response keys align with Pydantic model fields"
      pattern: "spotify_video|apple_video"
    - from: "src/podcast_pipeline/models/analysis.py"
      to: "src/podcast_pipeline/stages/render.py"
      via: "Validated `analysis.json` marketing keys rendered into marketing doc sections"
      pattern: "marketing|spotify_video|apple_video"
    - from: "src/podcast_pipeline/models/analysis.py"
      to: "src/podcast_pipeline/ui/app.py"
      via: "Validated marketing keys displayed in Streamlit editor"
      pattern: "render_marketing_editor|spotify_video|apple_video"
---

<objective>
Close the end-to-end marketing-copy gap for video podcast platforms by adding `spotify_video` and `apple_video` across model schema, provider prompt schema, render docs, and Streamlit editor.

Purpose: Ensure video platform publishing workflows receive platform-specific copy suggestions and operator edit surfaces.
Output: Code + tests proving `spotify_video` and `apple_video` flow through analysis, render, and UI layers.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@prompts/001-phase-6-1-video-podcast-marketing-copy.md
@config.yaml
@src/podcast_pipeline/models/analysis.py
@src/podcast_pipeline/providers/base.py
@src/podcast_pipeline/providers/kimi.py
@src/podcast_pipeline/stages/render.py
@src/podcast_pipeline/ui/app.py
@tests/test_render.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `spotify_video` and `apple_video` fields to marketing model</name>
  <files>src/podcast_pipeline/models/analysis.py</files>
  <action>Update `MarketingCopy` to include `spotify_video` and `apple_video` PlatformMarketing fields with default factories, keeping audio/video variant ordering adjacent.</action>
  <verify>uv run pytest tests/test_render.py -q -k "marketing and model"</verify>
  <done>Model validation preserves `spotify_video` and `apple_video` keys instead of silently dropping them.</done>
</task>

<task type="auto">
  <name>Task 2: Extend provider prompt marketing schema for video platforms</name>
  <files>src/podcast_pipeline/providers/base.py
src/podcast_pipeline/providers/kimi.py</files>
  <action>Update provider prompt schema examples so `marketing` explicitly includes `spotify_video` and `apple_video` entries with platform-appropriate descriptions.</action>
  <verify>uv run pytest tests/ -q -k "provider and marketing"</verify>
  <done>Provider prompts instruct models to generate video-specific marketing copy for Spotify and Apple variants.</done>
</task>

<task type="auto">
  <name>Task 3: Extend render marketing-doc platform sections</name>
  <files>src/podcast_pipeline/stages/render.py</files>
  <action>Update `_generate_marketing_doc()` platform section list to include Spotify Video and Apple Podcasts Video adjacent to audio counterparts.</action>
  <verify>uv run pytest tests/test_render.py -q -k "marketing_doc and (spotify_video or apple_video)"</verify>
  <done>Generated marketing docs include explicit sections for both new video platform keys.</done>
</task>

<task type="auto">
  <name>Task 4: Extend Streamlit marketing editor platform list</name>
  <files>src/podcast_pipeline/ui/app.py</files>
  <action>Update `render_marketing_editor()` platform list to show Spotify Video and Apple Video editor sections without changing existing UI flow semantics.</action>
  <verify>uv run pytest tests/test_ui.py tests/test_ui_app.py -q -k "marketing and video"</verify>
  <done>Operators can review/edit marketing copy for both new video platforms in Streamlit.</done>
</task>

<task type="auto">
  <name>Task 5: Add regression tests for model retention and marketing-doc coverage</name>
  <files>tests/test_render.py</files>
  <action>Add tests asserting `_generate_marketing_doc()` emits `spotify_video` and `apple_video` sections and `AnalysisResult.model_validate()` round-trips those keys through model dump.</action>
  <verify>uv run pytest tests/test_render.py -v</verify>
  <done>Regression suite locks end-to-end key retention and doc output behavior for new video marketing platforms.</done>
</task>

</tasks>

<verification>
Run full quality checks before completion:
- `uv run pytest tests/test_render.py -v`
- `uv run pytest tests/`
- `uv run ruff check .`
- `uv run ruff format --check .`
- `uv run mypy src/`
</verification>

<success_criteria>
- `MarketingCopy` includes `spotify_video` and `apple_video` defaults.
- Provider prompt schema requests video-platform marketing payloads.
- Render marketing doc emits Spotify Video and Apple Podcasts Video sections.
- Streamlit marketing editor includes Spotify Video and Apple Video sections.
- Regression tests cover model round-trip and marketing-doc section rendering for both new keys.
- Full test/lint/type verification passes.
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-video-marketing-copy-thumbnail-visual-selection/06.1-01-SUMMARY.md`
</output>
