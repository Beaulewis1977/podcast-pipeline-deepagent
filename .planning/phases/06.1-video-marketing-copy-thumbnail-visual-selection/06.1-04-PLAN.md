---
phase: 06.1-video-marketing-copy-thumbnail-visual-selection
plan: 04
type: execute
wave: 3
depends_on: ["02", "03"]
gap_closure: false
files_modified:
  - src/podcast_pipeline/stages/analyze.py
  - src/podcast_pipeline/ui/app.py
  - tests/test_ui.py
  - tests/test_ui_app.py
  - tests/test_pipeline.py
autonomous: true

must_haves:
  truths:
    - "Analyze stage materializes thumbnail frame images for review so UI interactions do not trigger heavy extraction work"
    - "Streamlit thumbnail selector displays visual frame previews and supports ranked selection of 1..3 thumbnails"
    - "Selection order is persisted deterministically and remains compatible with existing review state flows"
  artifacts:
    - path: "src/podcast_pipeline/stages/analyze.py"
      provides: "Analyze-time thumbnail extraction to `intermediate/thumbnails` with deterministic naming"
      min_lines: 80
    - path: "src/podcast_pipeline/ui/app.py"
      provides: "Visual thumbnail grid UI with ranked multi-select persistence"
      min_lines: 120
    - path: "tests/test_ui.py"
      provides: "UI helper tests for ranked thumbnail selection updates"
      min_lines: 30
    - path: "tests/test_ui_app.py"
      provides: "UI app behavior tests for visual selector and persistence"
      min_lines: 30
    - path: "tests/test_pipeline.py"
      provides: "Pipeline-level defaults/compatibility checks for thumbnail selection state"
      min_lines: 20
  key_links:
    - from: "src/podcast_pipeline/stages/analyze.py"
      to: "src/podcast_pipeline/ui/app.py"
      via: "UI reads extracted frame paths and metadata emitted by analyze stage"
      pattern: "intermediate/thumbnails|thumbnail_frames"
    - from: "src/podcast_pipeline/ui/app.py"
      to: "src/podcast_pipeline/stages/review.py"
      via: "UI save/update flows persist ordered `selected_thumbnails` and primary mirror"
      pattern: "save_review_decisions|selected_thumbnails|selected_thumbnail"
---

<objective>
Implement thumbnail visual-selection MVP behavior in analyze and Streamlit review flows.

Purpose: Replace text-only thumbnail cards with fast visual previews and ranked multi-select behavior.
Output: Analyze-time frame artifacts + Streamlit visual selection UX + persistence regressions.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-video-marketing-copy-thumbnail-visual-selection/06.1-RESEARCH.md
@src/podcast_pipeline/stages/analyze.py
@src/podcast_pipeline/ui/app.py
@src/podcast_pipeline/stages/review.py
@tests/test_ui.py
@tests/test_ui_app.py
@tests/test_pipeline.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add analyze-stage thumbnail frame materialization</name>
  <files>src/podcast_pipeline/stages/analyze.py</files>
  <action>After successful analysis payload generation, extract candidate frames from `intermediate/proxy.mp4` into `intermediate/thumbnails/` using deterministic names and persist path metadata for UI consumption. Keep behavior additive and fail-soft when no candidates exist.</action>
  <verify>uv run pytest tests/test_pipeline.py -q -k "thumbnail"</verify>
  <done>Analyze stage emits stable thumbnail frame artifacts and metadata that UI can read without on-demand FFmpeg extraction.</done>
</task>

<task type="auto">
  <name>Task 2: Replace text-only selector with visual grid + ranked multi-select</name>
  <files>src/podcast_pipeline/ui/app.py</files>
  <action>Refactor `render_thumbnail_selector()` to display frame images using `st.image(..., width=\"stretch\")` within `st.columns`, show virality/recommendation indicators, and implement ranked selection behavior capped at 3 picks (first selected is primary).</action>
  <verify>uv run pytest tests/test_ui.py tests/test_ui_app.py -q -k "thumbnail"</verify>
  <done>UI shows visual frame cards and supports ordered 1..3 selection with clear primary/alternate semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Persist ranked selection with backward-compatible mirror</name>
  <files>src/podcast_pipeline/ui/app.py
tests/test_ui.py
tests/test_ui_app.py
tests/test_pipeline.py</files>
  <action>Update thumbnail selection update/save paths so review-state writes preserve both `selected_thumbnails` and mirrored `selected_thumbnail`, including deterministic ordering and dedupe behavior across reruns and existing jobs.</action>
  <verify>uv run pytest tests/test_ui.py tests/test_ui_app.py tests/test_pipeline.py -q -k "selected_thumbnail"</verify>
  <done>Selection persistence is deterministic, reload-safe, and backward compatible with existing scalar-only review state.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_ui.py tests/test_ui_app.py tests/test_pipeline.py -q`
</verification>

<success_criteria>
- Analyze stage materializes thumbnail frame assets before review UI loads.
- Streamlit selector is visual (image-based), not text-only.
- Ranked 1..3 selection persists and remains compatible with existing review-state contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-video-marketing-copy-thumbnail-visual-selection/06.1-04-SUMMARY.md`
</output>
