---
phase: 06.1-video-marketing-copy-thumbnail-visual-selection
plan: 05
type: execute
wave: 4
depends_on: ["03", "04"]
gap_closure: false
files_modified:
  - src/podcast_pipeline/config/settings.py
  - config.yaml
  - src/podcast_pipeline/stages/render.py
  - tests/test_render.py
  - README.md
autonomous: true

must_haves:
  truths:
    - "For every selected export target that requires thumbnail assets, render generates and validates compliant thumbnail outputs before reporting success"
    - "Thumbnail compliance failures are fail-fast and reported with target-specific diagnostics"
    - "MVP uses one primary thumbnail plus optional alternates; explicit per-platform thumbnail assignment UX remains deferred"
  artifacts:
    - path: "src/podcast_pipeline/config/settings.py"
      provides: "Typed thumbnail compliance spec model(s) per export target"
      min_lines: 80
    - path: "config.yaml"
      provides: "Operator-visible default thumbnail spec constraints"
      min_lines: 40
    - path: "src/podcast_pipeline/stages/render.py"
      provides: "Thumbnail export + per-target compliance validation and fail-fast errors"
      min_lines: 140
    - path: "tests/test_render.py"
      provides: "Regression tests for thumbnail compliance success/failure across selected targets"
      min_lines: 120
    - path: "README.md"
      provides: "Operator-facing docs for thumbnail MVP behavior and deferred per-platform mapping"
      min_lines: 20
  key_links:
    - from: "src/podcast_pipeline/stages/review.py"
      to: "src/podcast_pipeline/stages/render.py"
      via: "`selected_thumbnails` and `export_platforms` drive thumbnail output generation/validation"
      pattern: "selected_thumbnails|export_platforms"
    - from: "src/podcast_pipeline/config/settings.py"
      to: "src/podcast_pipeline/stages/render.py"
      via: "Configured target thumbnail constraints are enforced during render"
      pattern: "thumbnail|spec|validation"
---

<objective>
Enforce thumbnail compliance per selected export target and finalize MVP export contract.

Purpose: Guarantee export-ready thumbnail artifacts and prevent silent success when a selected target cannot be satisfied.
Output: Configurable target constraints, render-time validation, regression tests, and explicit MVP/deferred boundary docs.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-video-marketing-copy-thumbnail-visual-selection/06.1-RESEARCH.md
@src/podcast_pipeline/config/settings.py
@config.yaml
@src/podcast_pipeline/stages/render.py
@src/podcast_pipeline/export_targets.py
@tests/test_render.py
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define typed per-target thumbnail compliance specs</name>
  <files>src/podcast_pipeline/config/settings.py
config.yaml</files>
  <action>Add typed config for thumbnail constraints per target (at minimum `youtube`, `spotify_video`, `apple_video`) including dimensions/aspect/format/size policy fields. Use official constraints where documented (for example YouTube and Apple episode art) and explicit project policy fields where docs are recommendation-only (for example Spotify suggested resolution).</action>
  <verify>uv run pytest tests/test_render.py -q -k "thumbnail and config"</verify>
  <done>Thumbnail constraints are explicit, typed, and configurable per export target.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce per-selected-target thumbnail output validation in render</name>
  <files>src/podcast_pipeline/stages/render.py</files>
  <action>Update thumbnail export flow to generate canonical primary/alternate outputs from one ranked selection set, then validate derived artifacts against every selected target's thumbnail constraints before success. Fail fast with structured target-specific validation details when constraints are unsatisfied.</action>
  <verify>uv run pytest tests/test_render.py -q -k "thumbnail and compliance"</verify>
  <done>Render cannot report success when selected target thumbnail requirements are unmet; failures are explicit and actionable.</done>
</task>

<task type="auto">
  <name>Task 3: Lock regressions and document deferred per-platform assignment UX</name>
  <files>tests/test_render.py
README.md</files>
  <action>Add tests for successful compliant outputs and fail-fast rejection paths by target. Update README to state MVP contract (single primary + optional alternates, per-target compliance enforced) and explicitly defer user-controlled per-platform thumbnail mapping to follow-on scope.</action>
  <verify>uv run pytest tests/test_render.py -q && rg -n "per-platform thumbnail assignment|deferred|single primary" README.md</verify>
  <done>Tests and docs codify non-negotiable MVP behavior and the post-MVP boundary for per-platform mapping UX.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_render.py -q`
- `uv run ruff check .`
</verification>

<success_criteria>
- Per-target thumbnail constraints are defined and enforced for selected export targets.
- Render fails fast with target-specific thumbnail validation diagnostics when constraints fail.
- MVP behavior is locked: one primary + optional alternates, with per-platform custom mapping explicitly deferred.
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-video-marketing-copy-thumbnail-visual-selection/06.1-05-SUMMARY.md`
</output>
