---
phase: 10-tauri-desktop-application-distribution
plan: 04
type: execute
wave: 3
depends_on: ["10-03"]
files_modified:
  - desktop/src/views/IngestionView.tsx
  - desktop/src/views/TranscriptView.tsx
  - desktop/src/hooks/useJobMutations.ts
autonomous: true

must_haves:
  truths:
    - "User can drag-and-drop video files onto the Ingestion view and jobs are created from the OS file paths"
    - "User can see a list of all jobs with current status, stage progress, and pipeline stage indicators"
    - "User can trigger run, resume, and delete actions on jobs from the Ingestion view"
    - "User sees system readiness status (FFmpeg, whisper model) in the Ingestion view before first run"
    - "User can view the transcript of a completed job with scrolling text and speaker labels"
    - "User can toggle filler word visibility in the transcript timeline view"
  artifacts:
    - path: "desktop/src/views/IngestionView.tsx"
      provides: "Drag-drop job creation + job list + pipeline progress + system readiness"
      min_lines: 100
    - path: "desktop/src/views/TranscriptView.tsx"
      provides: "Scrolling transcript with filler word toggle"
      min_lines: 80
    - path: "desktop/src/hooks/useJobMutations.ts"
      provides: "TanStack Query mutations for job CRUD and lifecycle actions"
      exports: ["useCreateJob", "useRunJob", "useResumeJob", "useDeleteJob"]
  key_links:
    - from: "desktop/src/views/IngestionView.tsx"
      to: "@tauri-apps/api/webview"
      via: "getCurrentWebview().onDragDropEvent() for native file drop"
      pattern: "onDragDropEvent"
    - from: "desktop/src/views/IngestionView.tsx"
      to: "desktop/src/hooks/useJobs.ts"
      via: "useJobs() for job list"
      pattern: "useJobs"
    - from: "desktop/src/views/IngestionView.tsx"
      to: "desktop/src/hooks/useSystemStatus.ts"
      via: "useSystemStatus() for readiness check"
      pattern: "useSystemStatus"
    - from: "desktop/src/views/TranscriptView.tsx"
      to: "desktop/src/hooks/useJobDetail.ts"
      via: "useJobDetail() for stage outputs containing transcript"
      pattern: "useJobDetail"
    - from: "desktop/src/hooks/useJobMutations.ts"
      to: "desktop/src/lib/backend.ts"
      via: "createJob, runJob, resumeJob, deleteJob imports"
      pattern: "useMutation"
---

<objective>
Build the Ingestion Dashboard (View 1) and Transcript Timeline (View 3) views. These are the primary job management and content review interfaces. Also create shared mutation hooks for job lifecycle actions.

Purpose: The Ingestion Dashboard replaces the current plain text input + job list in App.tsx with a native drag-drop experience, pipeline stage progress bars, batch queue visibility, and system readiness checks. The Transcript Timeline provides the content review experience that is a desktop superset of Streamlit's transcript display.

Output: 2 view components + 1 shared mutations hook.
</objective>

<execution_context>
@/home/kngpnn/.claude/get-shit-done/workflows/execute-plan.md
@/home/kngpnn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-tauri-desktop-application-distribution/10-03-SUMMARY.md
@desktop/src/lib/backend.ts
@desktop/src/stores/uiStore.ts
@desktop/src/hooks/useJobs.ts
@desktop/src/hooks/useJobDetail.ts
@desktop/src/hooks/useSystemStatus.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create job mutation hooks + Ingestion Dashboard view</name>
  <files>desktop/src/hooks/useJobMutations.ts, desktop/src/views/IngestionView.tsx</files>
  <action>
    Create directory `desktop/src/views/`.

    1. **`desktop/src/hooks/useJobMutations.ts`** — TanStack Query mutation hooks:
       - Import `useMutation`, `useQueryClient` from `@tanstack/react-query`
       - Import `createJob`, `runJob`, `resumeJob`, `deleteJob` from `../lib/backend`
       - Import types: `CreateJobResponse`, `LifecycleActionResponse`, `RunJobOptions`, `ResumeJobOptions` from `../lib/backend`

       Export 4 hooks:
       - `useCreateJob()`: `useMutation({ mutationFn: ({ videoPath, name }: { videoPath: string; name?: string }) => createJob(videoPath, name), onSuccess: () => queryClient.invalidateQueries({ queryKey: ['jobs'] }) })`
       - `useRunJob()`: `useMutation({ mutationFn: ({ jobId, options }: { jobId: string; options?: RunJobOptions }) => runJob(jobId, { background: true, ...options }), onSuccess: () => queryClient.invalidateQueries({ queryKey: ['jobs'] }) })`
       - `useResumeJob()`: `useMutation({ mutationFn: ({ jobId, options }: { jobId: string; options?: ResumeJobOptions }) => resumeJob(jobId, { background: true, ...options }), onSuccess: () => queryClient.invalidateQueries({ queryKey: ['jobs'] }) })`
       - `useDeleteJob()`: `useMutation({ mutationFn: (jobId: string) => deleteJob(jobId), onSuccess: () => queryClient.invalidateQueries({ queryKey: ['jobs'] }) })`

    2. **`desktop/src/views/IngestionView.tsx`** — Full Ingestion Dashboard:

       **Imports:**
       - `getCurrentWebview` from `@tauri-apps/api/webview` (for native drag-drop — use dynamic import wrapped in try/catch so it doesn't crash in non-Tauri dev mode)
       - `useJobs` from `../hooks/useJobs`
       - `useJobDetail` from `../hooks/useJobDetail`
       - `useSystemStatus` from `../hooks/useSystemStatus`
       - `useCreateJob`, `useRunJob`, `useResumeJob`, `useDeleteJob` from `../hooks/useJobMutations`
       - `useUIStore` from `../stores/uiStore`
       - `useSidecarStore` from `../stores/sidecarStore`
       - `STAGE_ORDER` from `../lib/backend`

       **Structure (top to bottom):**

       a) **System Readiness Banner** (top of view):
          - Use `useSystemStatus()` to show readiness state
          - When `data.ready === false`: show amber banner with `data.issues` list
          - When `data.ready === true`: show a subtle green "All systems ready" indicator
          - Show binary status icons (FFmpeg, podcast-backend) and model cache status

       b) **Drop Zone / Job Creator** (hero area):
          - Large drop zone area (min-h-48) with dashed border: `border-2 border-dashed border-[var(--color-border)] rounded-xl`
          - Text: "Drop video files here to create jobs" with a secondary "or enter path manually" link
          - Drag-over state: border turns accent color, background gets a subtle tint
          - Implement native drag-drop using `getCurrentWebview().onDragDropEvent()`:
            ```
            useEffect(() => {
              let unlisten: (() => void) | null = null;
              (async () => {
                try {
                  const { getCurrentWebview } = await import("@tauri-apps/api/webview");
                  unlisten = await getCurrentWebview().onDragDropEvent((event) => {
                    if (event.payload.type === 'hover') {
                      setDragOver(true);
                    } else if (event.payload.type === 'drop') {
                      setDragOver(false);
                      for (const path of event.payload.paths) {
                        createJobMutation.mutate({ videoPath: path });
                      }
                    } else if (event.payload.type === 'cancel') {
                      setDragOver(false);
                    }
                  });
                } catch {
                  // Not in Tauri context — show manual input fallback
                  setShowManualInput(true);
                }
              })();
              return () => unlisten?.();
            }, []);
            ```
          - Manual input fallback: text input for video path + optional job name + "Create" button (always visible below drop zone)
          - Do NOT use react-dropzone — it gives browser File objects without OS paths

       c) **Job List** (main content):
          - Use `useJobs()` to get job list with loading/error states
          - Each job renders as a card (`bg-[var(--color-bg-card)] rounded-lg p-4 border border-[var(--color-border)]`):
            - Job ID (monospace, accent color) + status badge (colored per status: running=amber, complete=green, failed=red, pending=gray)
            - Created timestamp (relative — "2 min ago" style using simple Date math, no extra library)
            - **Pipeline stage progress bar**: horizontal bar with 5 segments (ingest, transcribe, analyze, review, render). Each segment colored: complete=green, running=amber pulse, pending=gray, failed=red. Use `STAGE_ORDER` from backend.ts.
            - Action buttons row: "Run Full" (primary), "Resume" (secondary), "Delete" (danger, with confirm dialog)
          - Selected job (from `useUIStore.selectedJobId`) gets a ring highlight: `ring-2 ring-[var(--color-accent)]`
          - Click on a job card sets `useUIStore.setSelectedJob(jobId)`
          - Empty state: "No jobs yet. Drop a video file above to get started."
          - Loading state: skeleton/loading indicator

       d) **Selected Job Detail Panel** (right side or below, conditional):
          - Only renders when `selectedJobId` is not null
          - Use `useJobDetail(selectedJobId)` to get full detail
          - Show: job ID, input file path, status, created/updated timestamps
          - Per-stage detail: status, progress percent, error message, start/complete times
          - "Close" button to deselect

       **Anti-patterns to avoid:**
       - Do NOT use react-dropzone (no OS paths)
       - Do NOT use manual setInterval for polling (TanStack Query handles it)
       - Do NOT duplicate the boot/recovery logic from App.tsx (that stays in App.tsx)

       Export named: `export function IngestionView()`
  </action>
  <verify>
    - `cd /home/kngpnn/dev/podcast-pipeline-deepagent/desktop && npx tsc --noEmit` compiles without type errors
    - `ls desktop/src/views/IngestionView.tsx desktop/src/hooks/useJobMutations.ts` — files exist
    - `grep -q "onDragDropEvent" desktop/src/views/IngestionView.tsx` — native drag-drop is wired
    - `grep -q "useJobs" desktop/src/views/IngestionView.tsx` — TanStack Query job polling is used
    - `grep -q "useSystemStatus" desktop/src/views/IngestionView.tsx` — system readiness is displayed
    - `grep -q "useMutation" desktop/src/hooks/useJobMutations.ts` — mutations are TanStack Query based
  </verify>
  <done>
    - IngestionView renders drag-drop zone with native OS path support via Tauri webview API
    - Job list displays with pipeline stage progress bars and action buttons
    - System readiness banner shows binary/model cache status
    - All data fetching uses TanStack Query (no manual polling)
    - Job mutations (create, run, resume, delete) invalidate cache on success
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Transcript Timeline view</name>
  <files>desktop/src/views/TranscriptView.tsx</files>
  <action>
    **`desktop/src/views/TranscriptView.tsx`** — Scrolling transcript with filler word control:

    **Imports:**
    - `useUIStore` from `../stores/uiStore`
    - `useJobDetail` from `../hooks/useJobDetail`
    - `BACKEND_PORT` from `../lib/backend`
    - React: `useState`, `useMemo`, `useCallback`, `useRef`, `useEffect`

    **Data loading:**
    - Get `selectedJobId` from `useUIStore()`
    - Use `useJobDetail(selectedJobId)` to get job detail
    - The transcript data is in the job's stage outputs. The analyze stage outputs include `transcribe/word_alignment.json` and `transcribe/transcript.json` paths.
    - To load transcript content, fetch from `http://127.0.0.1:${BACKEND_PORT}/jobs/${jobId}/outputs/transcribe/transcript.json` — but this endpoint may not exist. Instead, use the job detail's `stages.transcribe.outputs` array to identify output files, then read them. If no direct file-serve endpoint exists, display the transcript data available in the job detail response.
    - **Pragmatic approach:** Since the backend may not serve raw output files via HTTP, implement the transcript view to work with what's available:
      1. Show stage completion status for transcribe/analyze
      2. If transcript data is available in job outputs metadata, render it
      3. Otherwise show "Transcript available after transcribe stage completes" placeholder
    - Define types for transcript data:
      ```typescript
      interface TranscriptSegment {
        text: string;
        start: number;
        end: number;
        speaker?: string;
        words?: TranscriptWord[];
      }
      interface TranscriptWord {
        word: string;
        start: number;
        end: number;
        is_filler?: boolean;
        filler_category?: string;
      }
      ```

    **Structure:**

    a) **Job Selector** (top):
       - If no job selected: "Select a job from the Ingestion view to view its transcript"
       - If job selected but transcribe stage not complete: "Transcription in progress..." with progress bar from job detail

    b) **Filler Control Bar** (toolbar below job selector):
       - Toggle switch: "Show Filler Words" (default: on)
       - When toggled off, filler words in the transcript are visually hidden (CSS opacity or display:none)
       - Filler word count badge: "X filler words detected"
       - Filter by category dropdown (if categories available): "All", "Disfluency", "Hedge", "Custom"

    c) **Transcript Body** (scrollable main area):
       - Render segments as paragraphs
       - Speaker labels as colored badges above paragraphs (if multi-speaker)
       - Timestamps on the left margin (start time in mm:ss format)
       - Filler words highlighted: `bg-[var(--color-warning)]/20 text-[var(--color-warning)] rounded px-1` when visible
       - When filler toggle is OFF, filler words get `opacity-30 line-through` styling
       - Smooth scrolling with `scroll-area` component or native `overflow-y-auto`

    d) **Stats Footer** (bottom):
       - Total duration, word count, speaker count, filler word percentage

    **State:**
    - `showFillers: boolean` (local state, default true)
    - `fillerFilter: string` (local state, default 'all')
    - `transcriptData: TranscriptSegment[] | null` (loaded from job outputs)

    **Fallback for missing data:**
    - If no transcript endpoint is available, render a structured placeholder showing stage outputs list from the job detail
    - The view must NOT crash if transcript data is unavailable — graceful degradation

    Export named: `export function TranscriptView()`
  </action>
  <verify>
    - `cd /home/kngpnn/dev/podcast-pipeline-deepagent/desktop && npx tsc --noEmit` compiles without type errors
    - `ls desktop/src/views/TranscriptView.tsx` — file exists
    - `grep -q "showFillers" desktop/src/views/TranscriptView.tsx` — filler toggle state exists
    - `grep -q "TranscriptSegment" desktop/src/views/TranscriptView.tsx` — transcript types defined
  </verify>
  <done>
    - TranscriptView renders scrolling transcript with speaker labels and timestamps
    - Filler word toggle shows/hides filler words with visual highlighting
    - Graceful fallback when transcript data is not yet available
    - View works with the selected job from uiStore
    - Type-checks cleanly
  </done>
</task>

</tasks>

<verification>
1. All 3 files created and type-check with `tsc --noEmit`
2. IngestionView uses native Tauri drag-drop (not react-dropzone)
3. Job list uses TanStack Query polling (not manual setInterval)
4. System readiness banner consumes `/system/status` endpoint
5. TranscriptView has filler word toggle
6. Both views read `selectedJobId` from `useUIStore`
7. `pnpm build` succeeds from desktop directory
</verification>

<success_criteria>
- Drag-and-drop creates jobs from OS file paths via Tauri webview API
- Job list shows pipeline stage progress with colored segments
- System readiness check displays binary/model status
- Transcript view renders with filler word toggle
- All data fetching via TanStack Query (no manual polling)
- Both views degrade gracefully when no job is selected or data is unavailable
</success_criteria>

<output>
After completion, create `.planning/phases/10-tauri-desktop-application-distribution/10-04-SUMMARY.md`
</output>
