---
phase: 07-smooth-editing-filler-word-control
plan: 04
type: execute
wave: 3
depends_on: ["02", "03"]
files_modified:
  - tests/test_pipeline.py
  - tests/test_render.py
  - tests/test_ui_app.py
  - README.md
autonomous: true

must_haves:
  truths:
    - "End-to-end flow from review decisions to render output preserves editorial intent and produces smoother splice behavior"
    - "Legacy review artifacts remain executable after Phase 7 changes"
    - "Operators have clear configuration and troubleshooting guidance for smoothing and filler-control behavior"
  artifacts:
    - path: "tests/test_pipeline.py"
      provides: "Integration coverage for review/edit-plan/render contracts under new and legacy decision paths"
      min_lines: 80
    - path: "tests/test_render.py"
      provides: "Cross-plan regression coverage for transition policy + enhancement compatibility"
      min_lines: 80
    - path: "tests/test_ui_app.py"
      provides: "Workflow-level persistence and handoff checks for filler decisions"
      min_lines: 60
    - path: "README.md"
      provides: "Operator-facing docs for Phase 7 controls, defaults, and known constraints"
      min_lines: 40
  key_links:
    - from: "tests/test_ui_app.py"
      to: "tests/test_pipeline.py"
      via: "UI persistence contract is verified through downstream pipeline consumption checks"
      pattern: "review_state|edit_plan|render"
    - from: "README.md"
      to: "config.yaml"
      via: "Documented smoothing/filler knobs match actual config contract and defaults"
      pattern: "smoothing|filler|defaults"
---

<objective>
Harden Phase 7 integration and document operator behavior.

Purpose: Ensure no hidden contract gaps remain after render and UI/review changes land.
Output: End-to-end regression coverage + compatibility checks + updated runbook-quality docs.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-smooth-editing-filler-word-control/07-RESEARCH.md
@.planning/phases/07-smooth-editing-filler-word-control/07-02-PLAN.md
@.planning/phases/07-smooth-editing-filler-word-control/07-03-PLAN.md
@src/podcast_pipeline/stages/render.py
@src/podcast_pipeline/stages/review.py
@src/podcast_pipeline/ui/app.py
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cross-path integration regressions for review→render behavior</name>
  <files>tests/test_pipeline.py
tests/test_render.py</files>
  <action>Add integration tests that exercise new filler decisions and snapped cuts through final render contract paths, including short-segment safeguards and Phase 6 enhancement compatibility assertions.</action>
  <verify>uv run pytest tests/test_pipeline.py tests/test_render.py -q -k "review or edit_plan or render"</verify>
  <done>Pipeline-level tests prove smoother edit behavior reaches render outputs without contract regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Lock backward compatibility for legacy review state and edit plans</name>
  <files>tests/test_pipeline.py
tests/test_ui_app.py
tests/test_render.py</files>
  <action>Add explicit regression cases where legacy `approved_filler_cuts`-only payloads (without new fields) still produce valid edit plans and render execution, while mixed new+legacy payloads remain deterministic.</action>
  <verify>uv run pytest tests/test_pipeline.py tests/test_ui_app.py tests/test_render.py -q -k "legacy or approved_filler_cuts or compatibility"</verify>
  <done>Legacy artifacts remain fully supported and verified after Phase 7 upgrades.</done>
</task>

<task type="auto">
  <name>Task 3: Update operator docs for smoothing/filler controls and troubleshooting</name>
  <files>README.md</files>
  <action>Document new smoothing and filler decision behavior, default policy, key configuration knobs, and troubleshooting guidance (xfade mismatch, short-segment clamp behavior, Streamlit state caveats). Keep docs aligned with actual config and test-proven behavior.</action>
  <verify>uv run pytest tests/test_pipeline.py tests/test_render.py tests/test_ui_app.py -q</verify>
  <done>README gives accurate, actionable operating guidance for the full Phase 7 workflow.</done>
</task>

</tasks>

<verification>
Final hardening verification:
- `uv run pytest tests/test_pipeline.py tests/test_render.py tests/test_ui_app.py -q`
</verification>

<success_criteria>
- End-to-end review/edit-plan/render flow is regression-locked for both new and legacy decision paths.
- No hidden compatibility gaps remain between Phase 7 smoothing and prior enhancement/render behavior.
- Operator documentation is accurate and implementation-aligned.
</success_criteria>

<output>
After completion, create `.planning/phases/07-smooth-editing-filler-word-control/07-04-SUMMARY.md`
</output>
