---
phase: 07-smooth-editing-filler-word-control
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/podcast_pipeline/stages/review.py
  - src/podcast_pipeline/ui/app.py
  - tests/test_review.py
  - tests/test_ui_app.py
  - tests/test_ui.py
autonomous: true

must_haves:
  truths:
    - "Editors can make explicit keep/remove decisions per detected filler and apply bulk actions by category"
    - "Review UI persists filler decisions reliably across reruns without double-edit/session-state drift"
    - "Saved filler decisions flow into `review/edit_plan.json` deterministically and remain backward-compatible with legacy review state"
  artifacts:
    - path: "src/podcast_pipeline/ui/app.py"
      provides: "Category-grouped filler review controls with per-item and bulk decision handling"
      min_lines: 180
    - path: "src/podcast_pipeline/stages/review.py"
      provides: "Decision persistence helpers that serialize `filler_decisions` and preserve legacy behavior"
      min_lines: 90
    - path: "tests/test_ui_app.py"
      provides: "Workflow tests for filler decision persistence and rerun-safe state updates"
      min_lines: 100
    - path: "tests/test_review.py"
      provides: "Contract tests for legacy/new review payload compatibility"
      min_lines: 60
  key_links:
    - from: "src/podcast_pipeline/ui/app.py"
      to: "src/podcast_pipeline/stages/review.py"
      via: "UI save path writes `ReviewDecisions` then regenerates `edit_plan.json`"
      pattern: "save_review_decisions|write_edit_plan|filler_decisions"
    - from: "src/podcast_pipeline/stages/review.py"
      to: "jobs/<job_id>/review/edit_plan.json"
      via: "Only fillers marked remove are emitted as filler cut ranges"
      pattern: "action == remove|filler_cuts"
---

<objective>
Ship a robust editorial control surface for filler words in Streamlit review flow.

Purpose: Replace coarse index approvals with deterministic per-filler and bulk decision workflows.
Output: Review UI/category controls + decision persistence updates + rerun-stability regressions.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-smooth-editing-filler-word-control/07-RESEARCH.md
@.planning/phases/07-smooth-editing-filler-word-control/07-01-PLAN.md
@docs/plans/2026-02-19-smooth-editing-and-filler-control-spec.md
@src/podcast_pipeline/stages/review.py
@src/podcast_pipeline/ui/app.py
@tests/test_ui_app.py
@tests/test_review.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add filler decision normalization and bulk-action semantics</name>
  <files>src/podcast_pipeline/stages/review.py
tests/test_review.py</files>
  <action>Implement normalized filler decision handling for category-aware review (`remove`, `keep`, and optional bulk presets), with a strict fallback path to legacy `approved_filler_cuts` when new decisions are absent. Keep output ordering deterministic.</action>
  <verify>uv run pytest tests/test_review.py -q -k "filler or decision or legacy"</verify>
  <done>Review contracts support new and old payloads and produce stable `edit_plan` filler ranges.</done>
</task>

<task type="auto">
  <name>Task 2: Build Streamlit category-grouped filler review UI with rerun-safe persistence</name>
  <files>src/podcast_pipeline/ui/app.py
tests/test_ui_app.py
tests/test_ui.py</files>
  <action>Update filler review section to group by category, expose per-item keep/remove controls, and add category-level bulk actions. Use stable widget keys + callback-safe session-state updates so decisions persist predictably across reruns without every-second-edit drift.</action>
  <verify>uv run pytest tests/test_ui_app.py tests/test_ui.py -q -k "filler or review_state or session_state"</verify>
  <done>Operators can quickly tune filler removals with deterministic state behavior and no disappearing edits.</done>
</task>

<task type="auto">
  <name>Task 3: Verify review→edit-plan wiring for new decision paths</name>
  <files>src/podcast_pipeline/stages/review.py
src/podcast_pipeline/ui/app.py
tests/test_ui_app.py
tests/test_review.py</files>
  <action>Add end-to-end helper-level regressions proving UI decisions are persisted and correctly translated into `edit_plan.json`, including mixed keep/remove cases and legacy compatibility fallback.</action>
  <verify>uv run pytest tests/test_ui_app.py tests/test_review.py -q</verify>
  <done>Decision wiring is regression-locked from UI interaction through persisted review/edit plan artifacts.</done>
</task>

</tasks>

<verification>
Review/UI verification:
- `uv run pytest tests/test_review.py tests/test_ui.py tests/test_ui_app.py -q`
</verification>

<success_criteria>
- Streamlit review flow supports explicit per-filler and category bulk decisions.
- Decision persistence is rerun-safe and backward-compatible.
- Edit-plan generation reflects final keep/remove decisions deterministically.
</success_criteria>

<output>
After completion, create `.planning/phases/07-smooth-editing-filler-word-control/07-03-SUMMARY.md`
</output>
