---
phase: 07-smooth-editing-filler-word-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/podcast_pipeline/utils/editing.py
  - src/podcast_pipeline/models/edit_plan.py
  - src/podcast_pipeline/stages/review.py
  - tests/test_editing.py
  - tests/test_review.py
  - tests/test_edit_plan.py
autonomous: true

must_haves:
  truths:
    - "Editors can explicitly keep or remove individual filler detections instead of relying on index-only approval lists"
    - "Existing jobs that only contain legacy `approved_filler_cuts` review data continue to load and generate valid edit plans"
    - "Cut boundary snapping can be applied with bounded shifts so edits avoid obvious mid-word truncation artifacts"
  artifacts:
    - path: "src/podcast_pipeline/utils/editing.py"
      provides: "Word-boundary extraction and snapping helpers with max-shift and validity guards"
      min_lines: 120
    - path: "src/podcast_pipeline/models/edit_plan.py"
      provides: "Backward-compatible edit-plan models with optional smoothing/snap/editorial metadata"
      min_lines: 80
    - path: "src/podcast_pipeline/stages/review.py"
      provides: "Review decision normalization and edit-plan writer support for per-filler decisions"
      min_lines: 80
    - path: "tests/test_editing.py"
      provides: "Deterministic tests for boundary extraction/snap behavior and edge cases"
      min_lines: 90
    - path: "tests/test_review.py"
      provides: "Compatibility tests for legacy and new filler-decision payloads"
      min_lines: 50
  key_links:
    - from: "src/podcast_pipeline/stages/review.py"
      to: "src/podcast_pipeline/models/edit_plan.py"
      via: "`write_edit_plan` maps review decisions into typed filler/content cut models"
      pattern: "filler_decisions|approved_filler_cuts|FillerCutRange"
    - from: "src/podcast_pipeline/utils/editing.py"
      to: "src/podcast_pipeline/stages/review.py"
      via: "Snapping helpers are reusable by review/render edit-plan preparation flow"
      pattern: "snap_to_word_boundary|snap_cut_range"
---

<objective>
Define the Phase 7 foundation contracts for boundary-safe cuts and granular filler editorial decisions.

Purpose: Create stable, backward-compatible primitives before any render or UI behavior changes.
Output: New editing helpers + additive model fields + review decision contract with regression coverage.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-smooth-editing-filler-word-control/07-RESEARCH.md
@docs/plans/2026-02-19-smooth-editing-and-filler-control-spec.md
@src/podcast_pipeline/models/edit_plan.py
@src/podcast_pipeline/stages/review.py
@tests/test_review.py
@tests/test_edit_plan.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reusable word-boundary snapping utilities</name>
  <files>src/podcast_pipeline/utils/editing.py
tests/test_editing.py</files>
  <action>Create typed helpers for extracting inter-word boundaries and snapping cut timestamps/ranges (`nearest`, `before`, `after`) with explicit `max_shift` limits and invalid-range fallback. Keep logic deterministic and side-effect free so render/review can share it.</action>
  <verify>uv run pytest tests/test_editing.py -q</verify>
  <done>Boundary snap utilities pass targeted behavior tests for nearest/dir-constrained snapping, max-shift clamping, and invalid-range fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Extend edit-plan schema with additive editorial and smoothing metadata</name>
  <files>src/podcast_pipeline/models/edit_plan.py
tests/test_edit_plan.py</files>
  <action>Add optional metadata fields to `FillerCutRange`/`ContentCutRange` (category/context/editorial_action/snapped/original_start/original_end/smoothing overrides) with defaults that preserve old payload validity. Keep overlap validation strict and unchanged.</action>
  <verify>uv run pytest tests/test_edit_plan.py -q</verify>
  <done>Existing edit-plan payloads still validate, and new metadata fields can be round-tripped without weakening overlap/time-range guards.</done>
</task>

<task type="auto">
  <name>Task 3: Upgrade review decisions to per-filler actions with legacy fallback</name>
  <files>src/podcast_pipeline/stages/review.py
tests/test_review.py</files>
  <action>Add `FillerDecision` + `filler_decisions` support and update `write_edit_plan` to prefer explicit per-filler actions, then fall back to legacy `approved_filler_cuts` behavior when absent. Preserve deterministic ordering and existing default approve behavior for non-enriched inputs.</action>
  <verify>uv run pytest tests/test_review.py -q</verify>
  <done>Review stage accepts old and new decision payloads; edit-plan output remains deterministic and backward-compatible.</done>
</task>

</tasks>

<verification>
Run contract-level tests for editing utility behavior and review/edit-plan model compatibility:
- `uv run pytest tests/test_editing.py tests/test_review.py tests/test_edit_plan.py -q`
</verification>

<success_criteria>
- Word-boundary utilities exist and are regression-tested for snap correctness and safety limits.
- Review decisions support per-filler keep/remove actions with no breakage for legacy review state.
- Edit-plan schema carries additive Phase 7 metadata without invalidating existing artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/07-smooth-editing-filler-word-control/07-01-SUMMARY.md`
</output>
