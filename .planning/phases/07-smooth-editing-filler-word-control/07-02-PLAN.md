---
phase: 07-smooth-editing-filler-word-control
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/podcast_pipeline/config/settings.py
  - config.yaml
  - src/podcast_pipeline/stages/render.py
  - tests/test_render.py
autonomous: true

must_haves:
  truths:
    - "Render applies micro-fade smoothing to splice edges and uses stronger crossfade/dissolve transitions only for content-cut joins"
    - "Cut boundaries are snapped to transcript word gaps with bounded shift before filtergraph generation"
    - "Short-segment joins do not break render due to overlong crossfade/xfade durations"
    - "Render degrades safely when optional FFmpeg transition filters are unavailable and surfaces actionable diagnostics"
    - "Phase 6 enhancement chain remains additive and intact after Phase 7 smoothing changes"
  artifacts:
    - path: "src/podcast_pipeline/config/settings.py"
      provides: "Typed smoothing policy config (micro-fade, content crossfade/dissolve, clamp, snap limits)"
      min_lines: 70
    - path: "config.yaml"
      provides: "Operator-visible defaults for smoothing and snapping behavior"
      min_lines: 40
    - path: "src/podcast_pipeline/stages/render.py"
      provides: "Transition-aware edit filtergraph builder with snap + duration-clamp guardrails + FFmpeg capability checks"
      min_lines: 240
    - path: "tests/test_render.py"
      provides: "Regression tests for micro-fade, selective crossfade/xfade, clamp guards, and Phase 6 chain preservation"
      min_lines: 140
  key_links:
    - from: "config.yaml"
      to: "src/podcast_pipeline/config/settings.py"
      via: "Typed smoothing defaults loaded by runtime config model"
      pattern: "smoothing|crossfade|dissolve|snap"
    - from: "src/podcast_pipeline/stages/render.py"
      to: "src/podcast_pipeline/utils/editing.py"
      via: "Render uses shared word-boundary snapping helpers before edit filtergraph build"
      pattern: "snap_cut_range|find_word_boundaries"
    - from: "src/podcast_pipeline/stages/render.py"
      to: "jobs/<job_id>/review/edit_plan.json"
      via: "Render consumes snapped and typed cut metadata from edit-plan artifacts before applying enhancement filters"
      pattern: "_load_edit_plan|_build_edit_plan_filter|_build_audio_enhancement_filters"
---

<objective>
Implement transition-aware render smoothing and boundary-safe cut application.

Purpose: Deliver audible/visual smoothness improvements without destabilizing the existing render pipeline.
Output: Typed smoothing config + snapped cut handling + selective transition filtergraph + guardrail tests.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-smooth-editing-filler-word-control/07-RESEARCH.md
@.planning/phases/07-smooth-editing-filler-word-control/07-01-PLAN.md
@docs/plans/2026-02-19-smooth-editing-and-filler-control-spec.md
@src/podcast_pipeline/stages/render.py
@src/podcast_pipeline/config/settings.py
@config.yaml
@tests/test_render.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed smoothing policy config and safe defaults</name>
  <files>src/podcast_pipeline/config/settings.py
config.yaml
tests/test_render.py</files>
  <action>Add a typed Phase 7 smoothing config section for micro-fade duration, content audio crossfade duration, content video dissolve duration, max snap shift, and per-join clamp ratio. Defaults should be conservative and backward-safe (feature enabled with mild values, no aggressive transitions by default).</action>
  <verify>uv run pytest tests/test_render.py -q -k "smoothing and config"</verify>
  <done>Runtime config validates smoothing settings, and defaults are explicitly documented in config and tests.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild edit filtergraph with snap + selective transitions + clamp guards</name>
  <files>src/podcast_pipeline/stages/render.py
tests/test_render.py</files>
  <action>Update edit graph construction so cut ranges are snapped to word boundaries (bounded shift), all joins get micro audio fades, and content-cut joins can receive clamped `acrossfade`/`xfade`. Normalize xfade inputs (fps/scale/pix_fmt/timebase), clamp transition durations against adjacent keep segments, and add a required-filter capability check (`ffmpeg -filters`) so unavailable optional filters fall back safely with clear diagnostics.</action>
  <verify>uv run pytest tests/test_render.py -q -k "edit_plan_filter and (micro_fade or acrossfade or xfade or clamp or snap)"</verify>
  <done>Render graph emits deterministic transition behavior with no overlong-transition failures on short segments and graceful fallback behavior when optional transition filters are missing.</done>
</task>

<task type="auto">
  <name>Task 3: Lock regressions for Phase 6 compatibility and transition boundaries</name>
  <files>tests/test_render.py</files>
  <action>Add regression coverage proving Phase 6 enhancement ordering remains intact and that filler-only joins do not apply heavy transitions by default. Include explicit assertions for required timestamp reset and xfade normalization constraints.</action>
  <verify>uv run pytest tests/test_render.py -q -k "phase6 or transition or timestamp_reset or xfade"</verify>
  <done>Tests fail on any regression that breaks enhancement chain ordering, misapplies transitions, or violates xfade preconditions.</done>
</task>

</tasks>

<verification>
Render-level verification:
- `uv run pytest tests/test_render.py -q`
</verification>

<success_criteria>
- Phase 7 smoothing config is typed, validated, and operator-visible.
- Render applies word-snapped cuts and selective transition policy with short-segment safety guards.
- Optional FFmpeg transition filter absence does not hard-fail render; behavior is deterministic and operator-visible.
- Phase 6 enhancement behavior remains additive and test-locked after transition integration.
</success_criteria>

<output>
After completion, create `.planning/phases/07-smooth-editing-filler-word-control/07-02-SUMMARY.md`
</output>
