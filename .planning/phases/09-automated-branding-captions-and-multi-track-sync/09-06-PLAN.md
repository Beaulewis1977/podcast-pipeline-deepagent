---
phase: 09-automated-branding-captions-and-multi-track-sync
plan: 06
type: execute
wave: 4
depends_on: ["05"]
files_modified:
  - src/podcast_pipeline/utils/captions.py
  - src/podcast_pipeline/stages/render.py
  - src/podcast_pipeline/config/settings.py
  - tests/test_captions.py
  - tests/test_render.py
autonomous: true

must_haves:
  truths:
    - "Word-aligned ASS captions can be generated automatically for 16:9, 9:16, and 1:1 outputs"
    - "Per-word highlight timing is preserved from transcript alignment data"
    - "Render can burn generated ASS captions through libass with deterministic failure handling"
  artifacts:
    - path: "src/podcast_pipeline/utils/captions.py"
      provides: "ASS generation engine with style templates and safe-zone presets"
      min_lines: 220
    - path: "src/podcast_pipeline/stages/render.py"
      provides: "Caption burn-in integration using generated ASS artifacts"
      min_lines: 90
    - path: "tests/test_captions.py"
      provides: "ASS formatting, timing, and aspect-ratio template coverage"
      min_lines: 140
  key_links:
    - from: "src/podcast_pipeline/utils/captions.py"
      to: "src/podcast_pipeline/stages/render.py"
      via: "generated .ass path consumed by ffmpeg ass/libass filter path"
      pattern: "ass=|libass|caption"
    - from: "src/podcast_pipeline/config/settings.py"
      to: "src/podcast_pipeline/utils/captions.py"
      via: "caption style and safe-zone configuration by target aspect ratio"
      pattern: "caption|safe_zone|aspect"
---

<objective>
Ship an ASS caption engine with word-level highlighting and integrate deterministic burn-in into render.

Purpose: Move caption generation from static subtitle exports to production-ready branded overlays.
Output: Caption generator utility, render integration, and robust caption regressions.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-automated-branding-captions-and-multi-track-sync/09-RESEARCH.md
@src/podcast_pipeline/models/transcript.py
@src/podcast_pipeline/stages/transcribe.py
@src/podcast_pipeline/stages/render.py
@src/podcast_pipeline/config/settings.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ASS caption generator with ratio-aware templates</name>
  <files>src/podcast_pipeline/utils/captions.py
src/podcast_pipeline/config/settings.py
tests/test_captions.py</files>
  <action>Create caption generation helpers that transform `word_alignment.json` into ASS events with per-word highlight spans, brand-style support, and safe-zone templates for 16:9, 9:16, and 1:1 outputs. Validate timestamp ordering and clamp invalid ranges.</action>
  <verify>uv run pytest tests/test_captions.py -q -k "ass or safe_zone or highlight"</verify>
  <done>Generated ASS files are syntactically valid and preserve word-level timing semantics across all target ratios.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate caption burn-in path in render stage</name>
  <files>src/podcast_pipeline/stages/render.py
tests/test_render.py</files>
  <action>Add render wiring to generate ASS artifacts per target when captions are enabled and burn them with FFmpeg `ass` filter/libass. Emit explicit errors when required alignment or ASS artifacts are unavailable instead of silently skipping.</action>
  <verify>uv run pytest tests/test_render.py -q -k "caption and burn"</verify>
  <done>Render outputs include burned captions when enabled and fail loudly with actionable diagnostics when prerequisites are missing.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for style templates and fallback behavior</name>
  <files>tests/test_captions.py
tests/test_render.py</files>
  <action>Cover edge cases: empty word alignment, overlapping word timings, unsupported ratio requests, and style fallback to defaults. Ensure legacy SRT/VTT outputs remain unaffected by ASS path introduction.</action>
  <verify>uv run pytest tests/test_captions.py tests/test_render.py -q -k "caption"</verify>
  <done>Caption pathway is stable, deterministic, and backward-compatible with existing subtitle workflows.</done>
</task>

</tasks>

<verification>
ASS generation is deterministic across aspect ratios and render burn-in behavior is fully regression-covered.
</verification>

<success_criteria>
- Word-level ASS captions are produced from alignment artifacts for all target ratios.
- Render burn-in uses generated ASS safely with explicit failure semantics.
- Existing non-ASS subtitle paths continue working unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/09-automated-branding-captions-and-multi-track-sync/09-06-SUMMARY.md`
</output>
