---
phase: 09-automated-branding-captions-and-multi-track-sync
plan: 10
type: execute
wave: 5
depends_on: ["02", "03", "04", "05", "06", "07", "08", "09"]
files_modified:
  - src/podcast_pipeline/ui/app.py
  - src/podcast_pipeline/stages/review.py
  - src/podcast_pipeline/stages/analyze.py
  - src/podcast_pipeline/stages/render.py
  - src/podcast_pipeline/service/supervisor.py
  - tests/test_ui_app.py
  - tests/test_pipeline.py
  - tests/test_supervisor.py
  - docs/runbooks/phase-09-brand-studio.md
autonomous: true

must_haves:
  truths:
    - "Streamlit exposes a Brand Studio workflow for profile CRUD and brand asset/voice management"
    - "Production controls allow caption styling, thumbnail selection, and sync/audio-mix controls in one operator flow"
    - "End-to-end run can produce a branded, captioned, synced, multi-platform output using Phase 9 capabilities"
    - "GPU-heavy operations (local FLUX generation and NVENC encode) are serialized across jobs to prevent VRAM contention crashes"
  artifacts:
    - path: "src/podcast_pipeline/ui/app.py"
      provides: "Brand Studio tab + production sidebar control surfaces wired to persisted review/job state"
      min_lines: 220
    - path: "src/podcast_pipeline/stages/review.py"
      provides: "Review-state fields for branding/caption/sync/mix selections"
      min_lines: 80
    - path: "docs/runbooks/phase-09-brand-studio.md"
      provides: "Operator playbook for Phase 9 setup, execution, and troubleshooting"
      min_lines: 120
    - path: "src/podcast_pipeline/service/supervisor.py"
      provides: "Global GPU lease/semaphore policy for safe cross-job scheduling of heavy GPU workloads"
      min_lines: 60
  key_links:
    - from: "src/podcast_pipeline/ui/app.py"
      to: "src/podcast_pipeline/stages/review.py"
      via: "Brand Studio/Production selections persist through review_state contract"
      pattern: "review_state|branding|caption|sync"
    - from: "src/podcast_pipeline/stages/review.py"
      to: "src/podcast_pipeline/stages/render.py"
      via: "Persisted review decisions are consumed by render orchestration for final outputs"
      pattern: "ReviewDecisions|edit_plan|render"
    - from: "src/podcast_pipeline/stages/analyze.py"
      to: "src/podcast_pipeline/service/supervisor.py"
      via: "Thumbnail generation path is marked as GPU-heavy and participates in cross-job lease gating"
      pattern: "thumbnail|gpu|lease|semaphore"
    - from: "src/podcast_pipeline/stages/render.py"
      to: "src/podcast_pipeline/service/supervisor.py"
      via: "NVENC-heavy render path participates in the same cross-job lease policy"
      pattern: "nvenc|render|gpu|lease|semaphore"
---

<objective>
Deliver the operator-facing Streamlit integration layer that activates Phase 9 capabilities end-to-end.

Purpose: Convert new backend/media capabilities into an executable production workflow.
Output: Brand Studio UI, production controls, integration tests, and runbook documentation.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-automated-branding-captions-and-multi-track-sync/09-RESEARCH.md
@src/podcast_pipeline/ui/app.py
@src/podcast_pipeline/stages/review.py
@src/podcast_pipeline/stages/analyze.py
@src/podcast_pipeline/stages/render.py
@src/podcast_pipeline/service/supervisor.py
@tests/test_ui_app.py
@tests/test_pipeline.py
@tests/test_supervisor.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Streamlit Brand Studio profile management workflow</name>
  <files>src/podcast_pipeline/ui/app.py
src/podcast_pipeline/stages/review.py
tests/test_ui_app.py</files>
  <action>Add a dedicated Brand Studio tab for profile create/save/load/delete, logo/font asset linking, and brand voice editing. Persist selected profile and relevant overrides through review state so reruns and resumes remain truthful.</action>
  <verify>uv run pytest tests/test_ui_app.py -q -k "brand_studio or profile"</verify>
  <done>Operators can create and manage branding profiles without manual artifact editing.</done>
</task>

<task type="auto">
  <name>Task 2: Add Production sidebar controls wired to review/render contracts</name>
  <files>src/podcast_pipeline/ui/app.py
src/podcast_pipeline/stages/review.py
src/podcast_pipeline/stages/analyze.py
src/podcast_pipeline/stages/render.py
src/podcast_pipeline/service/supervisor.py
tests/test_ui_app.py</files>
  <action>Add production controls for caption style selection, thumbnail gallery/ranking, auto-ducking toggle, and manual sync offset slider. Ensure each control persists via review-state contracts and is consumed by render without hidden defaults. Add cross-job GPU safety orchestration: enforce a shared GPU lease/semaphore policy so local FLUX generation and NVENC-heavy render jobs do not run concurrently across different jobs. Keep same-job stage execution sequential and ensure FLUX models are explicitly unloaded before continuing to subsequent stages.</action>
  <verify>uv run pytest tests/test_ui_app.py tests/test_pipeline.py tests/test_supervisor.py -q -k "production or gpu or semaphore or lease"</verify>
  <done>UI controls are truthfully wired, and GPU-heavy workloads are serialized across jobs to avoid FLUX/NVENC VRAM contention failures.</done>
</task>

<task type="auto">
  <name>Task 3: Add end-to-end integration regressions and operator runbook</name>
  <files>tests/test_pipeline.py
tests/test_supervisor.py
docs/runbooks/phase-09-brand-studio.md</files>
  <action>Add integration tests for a representative Phase 9 flow (profile + captions + sync + thumbnail + export), including regressions for cross-job GPU lease behavior (second GPU-heavy run blocks/queues deterministically while first is active). Document setup/troubleshooting guidance, including dev-only MCP boundary, optional cloud/local thumbnail prerequisites, and GPU serialization expectations.</action>
  <verify>uv run pytest tests/test_pipeline.py tests/test_supervisor.py -q -k "phase9 or branding or captions or sync or gpu"</verify>
  <done>Phase 9 workflow is executable end-to-end with documented operator steps, tested core path, and explicit GPU contention safeguards.</done>
</task>

</tasks>

<verification>
Brand Studio and production controls persist through review state and drive render behavior in tested end-to-end flows.
</verification>

<success_criteria>
- Streamlit exposes full Phase 9 operator controls with persistent state.
- Render behavior matches persisted selections for captions/sync/thumbnail/audio mix.
- Runbook plus integration tests make the phase executable without hidden steps.
- Cross-job local FLUX and NVENC workloads are serialized by design to prevent VRAM race failures.
</success_criteria>

<output>
After completion, create `.planning/phases/09-automated-branding-captions-and-multi-track-sync/09-10-SUMMARY.md`
</output>
