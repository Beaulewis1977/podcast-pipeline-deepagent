---
phase: 09-automated-branding-captions-and-multi-track-sync
plan: 04
type: execute
wave: 3
depends_on: ["01"]
files_modified:
  - src/podcast_pipeline/config/settings.py
  - config.yaml
  - src/podcast_pipeline/stages/render.py
  - tests/test_render.py
autonomous: true

must_haves:
  truths:
    - "HEVC 10-bit export can run on NVENC-capable machines and falls back to software x265 when required"
    - "AV1 export remains explicitly experimental and opt-in rather than silently enabled"
    - "Render output profiles remain validated and deterministic across platform specs"
    - "RIFE 60fps uplift is controlled by an explicit `force_60fps_shortform` toggle and only applies to short-form vertical exports"
  artifacts:
    - path: "src/podcast_pipeline/config/settings.py"
      provides: "PlatformSpec validation for hevc10/nvenc/x265 fallback and av1 experimental toggle plus SmoothingConfig `force_60fps_shortform`"
      min_lines: 110
    - path: "src/podcast_pipeline/stages/render.py"
      provides: "Runtime encoder capability detection and explicit fallback selection"
      min_lines: 120
    - path: "tests/test_render.py"
      provides: "Profile-level, pix_fmt, capability, and fallback regression tests"
      min_lines: 140
  key_links:
    - from: "config.yaml"
      to: "src/podcast_pipeline/config/settings.py"
      via: "typed platform profile definitions for hevc10 + av1 toggle and explicit short-form 60fps RIFE flag"
      pattern: "hevc|nvenc|libx265|av1|force_60fps_shortform|smoothing"
    - from: "src/podcast_pipeline/config/settings.py"
      to: "src/podcast_pipeline/stages/render.py"
      via: "validated codec/profile/pix_fmt data and short-form 60fps flag drive runtime selection and RIFE gating"
      pattern: "PlatformSpec|video_codec|pixel_format|experimental|force_60fps_shortform|RifeBridge"
---

<objective>
Introduce HEVC 10-bit profile support with deterministic capability fallback and explicit AV1 experimental gating.

Purpose: Improve output quality and hardware acceleration while preserving predictable cross-machine behavior.
Output: PlatformSpec/render updates plus codec regression coverage.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-automated-branding-captions-and-multi-track-sync/09-RESEARCH.md
@src/podcast_pipeline/config/settings.py
@config.yaml
@src/podcast_pipeline/stages/render.py
@tests/test_render.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend platform config schema for HEVC 10-bit and AV1 experimental mode</name>
  <files>src/podcast_pipeline/config/settings.py
config.yaml</files>
  <action>Add typed defaults and validators for HEVC 10-bit profile settings (including NVENC preference and software fallback) plus an explicit AV1 experimental toggle. Also add `SmoothingConfig.force_60fps_shortform: bool = False` and wire it into `config.yaml` defaults. Keep invalid profile/pixel-format combinations fail-fast at model validation time.</action>
  <verify>uv run pytest tests/test_render.py -q -k "platform_spec and (hevc or av1)"</verify>
  <done>Configuration clearly expresses codec intent, short-form 60fps intent, and blocks invalid combinations before render runs.</done>
</task>

<task type="auto">
  <name>Task 2: Implement runtime encoder capability detection and fallback chain</name>
  <files>src/podcast_pipeline/stages/render.py
tests/test_render.py</files>
  <action>Update render encoder selection to detect local FFmpeg encoder availability and choose: NVENC HEVC 10-bit when present, otherwise `libx265` with equivalent profile intent. Keep AV1 path gated by explicit config and emit actionable logs when unavailable. Implement short-form RIFE gating: when `force_60fps_shortform=true`, apply 30→60fps uplift only for short-form vertical exports (TikTok/Reels/Shorts), and skip long-form exports. Ensure the RIFE up-conversion runs before branding/caption burn-in in render ordering.</action>
  <verify>uv run pytest tests/test_render.py -q -k "encoder_capability or fallback"</verify>
  <done>Render reliably chooses a valid encoder path, and short-form RIFE uplift is explicitly gated and ordered.</done>
</task>

<task type="auto">
  <name>Task 3: Lock codec behavior with regression tests</name>
  <files>tests/test_render.py</files>
  <action>Add tests for codec/profile/level/pixel-format validation, NVENC detection branches, x265 fallback behavior, and AV1 experimental toggle semantics. Add regressions for `force_60fps_shortform`: enabled only affects short-form vertical targets, long-form targets are untouched, and the render pipeline ordering applies RIFE before caption/branding overlays. Ensure prior platform exports remain unaffected when new options are disabled.</action>
  <verify>uv run pytest tests/test_render.py -q -k "hevc or nvenc or x265 or av1 or force_60fps_shortform or rife"</verify>
  <done>Codec and short-form 60fps pathways are deterministic and regression-protected across supported environments.</done>
</task>

</tasks>

<verification>
HEVC 10-bit profiles validate and execute deterministically, with tested fallback behavior, explicit AV1 opt-in control, and explicit short-form 60fps RIFE gating.
</verification>

<success_criteria>
- Platform config supports HEVC 10-bit with strong validation.
- Render chooses NVENC or x265 explicitly based on observed capability.
- AV1 remains opt-in and test-covered, with no hidden activation.
- `force_60fps_shortform` is implemented as an explicit SmoothingConfig/config flag and only affects short-form vertical exports.
</success_criteria>

<output>
After completion, create `.planning/phases/09-automated-branding-captions-and-multi-track-sync/09-04-SUMMARY.md`
</output>
