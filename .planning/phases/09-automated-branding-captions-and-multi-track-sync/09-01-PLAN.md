---
phase: 09-automated-branding-captions-and-multi-track-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/podcast_pipeline/utils/ffmpeg_toolkit.py
  - src/podcast_pipeline/utils/__init__.py
  - tests/test_ffmpeg_toolkit.py
  - tests/test_utils.py
autonomous: true

must_haves:
  truths:
    - "A typed FFmpeg toolkit exists as the single deterministic media command surface for Phase 9 features"
    - "Toolkit failures are surfaced as structured, actionable errors rather than raw subprocess crashes"
    - "Existing callers of src/podcast_pipeline/utils/ffmpeg.py remain stable and regression-locked"
  artifacts:
    - path: "src/podcast_pipeline/utils/ffmpeg_toolkit.py"
      provides: "14 typed toolkit operations across probe/encode/filter/edit/package groups"
      min_lines: 260
    - path: "tests/test_ffmpeg_toolkit.py"
      provides: "Command-shape, validation, and failure-path coverage for toolkit APIs"
      min_lines: 180
    - path: "tests/test_utils.py"
      provides: "Regression lock for legacy ffmpeg helpers that remain in active use"
      min_lines: 40
  key_links:
    - from: "src/podcast_pipeline/utils/ffmpeg_toolkit.py"
      to: "src/podcast_pipeline/utils/ffmpeg.py"
      via: "shared subprocess and probe semantics with deterministic argument construction"
      pattern: "run_ffmpeg|run_ffprobe|FFmpegError"
    - from: "tests/test_ffmpeg_toolkit.py"
      to: "src/podcast_pipeline/utils/ffmpeg_toolkit.py"
      via: "typed input/output validation and generated command assertions"
      pattern: "model_validate|build_command|raises"
---

<objective>
Build the deterministic FFmpeg foundation for Phase 9: one typed toolkit module that all later plans can reuse.

Purpose: Remove ad-hoc command construction and make media operations testable and predictable.
Output: A 14-operation toolkit with typed I/O, structured errors, and regression safety.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-automated-branding-captions-and-multi-track-sync/09-RESEARCH.md
@docs/plans/2026-02-21-branding-automation-and-sync-spec_v4.md
@src/podcast_pipeline/utils/ffmpeg.py
@tests/test_utils.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create typed FFmpeg toolkit module with validated request/response models</name>
  <files>src/podcast_pipeline/utils/ffmpeg_toolkit.py
src/podcast_pipeline/utils/__init__.py</files>
  <action>Create `ffmpeg_toolkit.py` with typed Pydantic request/response models and a deterministic subprocess execution layer (no shell string composition, no eval-like parsing). Define grouped operations for probe/encode/filter/edit/package and expose the toolkit via `utils.__init__` for import stability.</action>
  <verify>uv run pytest tests/test_ffmpeg_toolkit.py -q -k "models or validation"</verify>
  <done>Toolkit accepts typed inputs, rejects malformed parameters early, and is importable from the utils package.</done>
</task>

<task type="auto">
  <name>Task 2: Implement all 14 toolkit operations with deterministic command builders</name>
  <files>src/podcast_pipeline/utils/ffmpeg_toolkit.py
tests/test_ffmpeg_toolkit.py</files>
  <action>Implement the 14 operations defined in the phase spec using stable argument lists and explicit ffmpeg/ffprobe invocation helpers. Include operations needed by downstream plans such as probing metadata, trim/concat, subtitle burn-in, audio filters, and overlay compositing. Ensure errors return structured context (operation, args, stderr excerpt).</action>
  <verify>uv run pytest tests/test_ffmpeg_toolkit.py -q -k "operation or command"</verify>
  <done>All toolkit operations execute through one deterministic path and emit structured failures suitable for stage-level diagnostics.</done>
</task>

<task type="auto">
  <name>Task 3: Lock backward compatibility for existing ffmpeg utility callers</name>
  <files>tests/test_utils.py
tests/test_ffmpeg_toolkit.py</files>
  <action>Add regression tests confirming legacy `utils/ffmpeg.py` APIs used by ingest/transcribe/render remain behavior-compatible while the new toolkit lands. Assert no required call-site rewrites are introduced in this foundation plan.</action>
  <verify>uv run pytest tests/test_utils.py tests/test_ffmpeg_toolkit.py -q</verify>
  <done>Legacy FFmpeg utilities remain stable, and new toolkit coverage prevents regressions before dependent plans build on top.</done>
</task>

</tasks>

<verification>
The toolkit module exists, all 14 operations are test-covered, and existing ffmpeg utility behavior remains intact.
</verification>

<success_criteria>
- `ffmpeg_toolkit.py` is the typed deterministic media command surface for Phase 9.
- Structured error contracts replace opaque subprocess failures for toolkit operations.
- Legacy ffmpeg helper behavior is regression-locked to avoid collateral breakage.
</success_criteria>

<output>
After completion, create `.planning/phases/09-automated-branding-captions-and-multi-track-sync/09-01-SUMMARY.md`
</output>
