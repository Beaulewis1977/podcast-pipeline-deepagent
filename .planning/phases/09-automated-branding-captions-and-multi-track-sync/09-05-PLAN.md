---
phase: 09-automated-branding-captions-and-multi-track-sync
plan: 05
type: execute
wave: 3
depends_on: ["01"]
files_modified:
  - src/podcast_pipeline/models/branding.py
  - src/podcast_pipeline/models/__init__.py
  - src/podcast_pipeline/utils/branding.py
  - src/podcast_pipeline/config/settings.py
  - src/podcast_pipeline/providers/base.py
  - src/podcast_pipeline/stages/analyze.py
  - tests/test_models.py
  - tests/test_providers.py
autonomous: true

must_haves:
  truths:
    - "Operators can define a BrandingProfile with reusable brand voice, assets, and per-platform overrides"
    - "Brand voice text is safely injected into provider prompts without breaking structured-output contracts"
    - "A deterministic profile merge order exists: base profile first, then platform override"
  artifacts:
    - path: "src/podcast_pipeline/models/branding.py"
      provides: "Typed BrandingProfile model with validation and override merge support"
      min_lines: 180
    - path: "src/podcast_pipeline/utils/branding.py"
      provides: "Profile load/save/resolve helpers for branding/<name>.yaml artifacts"
      min_lines: 120
    - path: "src/podcast_pipeline/providers/base.py"
      provides: "Prompt builder extension that accepts sanitized optional brand_voice"
      min_lines: 40
  key_links:
    - from: "src/podcast_pipeline/utils/branding.py"
      to: "src/podcast_pipeline/providers/base.py"
      via: "resolved profile feeds brand_voice into analysis prompt construction"
      pattern: "brand_voice|resolve_profile|_build_prompt"
    - from: "src/podcast_pipeline/models/branding.py"
      to: "src/podcast_pipeline/stages/analyze.py"
      via: "platform override resolution chosen from job export context"
      pattern: "platform_overrides|merge|analyze"
---

<objective>
Introduce a typed BrandingProfile contract and wire brand voice into provider prompt generation with safe override behavior.

Purpose: Establish one source of truth for brand styling inputs used across captions, thumbnails, and copy generation.
Output: Branding model + utilities + prompt injection wiring + regression tests.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-automated-branding-captions-and-multi-track-sync/09-RESEARCH.md
@src/podcast_pipeline/models/analysis.py
@src/podcast_pipeline/providers/base.py
@src/podcast_pipeline/stages/analyze.py
@src/podcast_pipeline/config/settings.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BrandingProfile model and YAML serialization contract</name>
  <files>src/podcast_pipeline/models/branding.py
src/podcast_pipeline/models/__init__.py
src/podcast_pipeline/utils/branding.py</files>
  <action>Implement `BrandingProfile` with brand voice, visual asset references, caption defaults, sound-kit references, and `platform_overrides`. Add load/save helpers for `branding/<name>.yaml` and deterministic merge methods that preserve base defaults unless overridden.</action>
  <verify>uv run pytest tests/test_models.py -q -k "branding_profile or platform_overrides"</verify>
  <done>Branding profiles serialize/deserialize safely and merge predictably by target platform.</done>
</task>

<task type="auto">
  <name>Task 2: Wire branding profile resolution into runtime configuration flow</name>
  <files>src/podcast_pipeline/config/settings.py
src/podcast_pipeline/stages/analyze.py
src/podcast_pipeline/utils/branding.py</files>
  <action>Add configuration hooks that resolve an active branding profile for each job/export context and provide platform-specific resolved branding payloads to downstream stages. Keep behavior backward-compatible when no branding profile is configured.</action>
  <verify>uv run pytest tests/test_models.py tests/test_pipeline.py -q -k "branding and resolve"</verify>
  <done>Jobs run without branding by default but can opt into typed profile-driven behavior deterministically.</done>
</task>

<task type="auto">
  <name>Task 3: Inject sanitized brand voice into provider prompt path</name>
  <files>src/podcast_pipeline/providers/base.py
src/podcast_pipeline/stages/analyze.py
tests/test_providers.py</files>
  <action>Extend `_build_prompt` to accept optional `brand_voice` and inject it in a bounded, sanitized block that cannot override schema instructions. Add tests proving brand voice appears when configured and malformed control text is normalized safely.</action>
  <verify>uv run pytest tests/test_providers.py -q -k "brand_voice or prompt"</verify>
  <done>Brand voice influences copy direction without weakening strict JSON output requirements.</done>
</task>

</tasks>

<verification>
Branding profiles load/merge correctly, are optional by default, and safely influence provider prompts when enabled.
</verification>

<success_criteria>
- `BrandingProfile` is a typed, serializable source of truth with per-platform overrides.
- Prompt injection supports bounded `brand_voice` without schema drift risks.
- Regression tests lock no-branding and branding-enabled execution paths.
</success_criteria>

<output>
After completion, create `.planning/phases/09-automated-branding-captions-and-multi-track-sync/09-05-SUMMARY.md`
</output>
