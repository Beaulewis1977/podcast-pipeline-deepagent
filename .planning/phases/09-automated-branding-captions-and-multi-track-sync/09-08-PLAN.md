---
phase: 09-automated-branding-captions-and-multi-track-sync
plan: 08
type: execute
wave: 4
depends_on: ["05"]
files_modified:
  - src/podcast_pipeline/config/settings.py
  - src/podcast_pipeline/models/branding.py
  - src/podcast_pipeline/utils/audio_mix.py
  - src/podcast_pipeline/stages/render.py
  - tests/test_render.py
  - tests/test_models.py
autonomous: true

must_haves:
  truths:
    - "Brand-linked sound kits (intro/transition/outro) can be configured and applied during render"
    - "Voice remains intelligible under stingers via deterministic sidechain ducking defaults"
    - "Missing optional sound assets degrade gracefully without breaking base exports"
    - "Stinger sources are format-normalized to a canonical working audio format before ducking, avoiding container/codec timing drift"
  artifacts:
    - path: "src/podcast_pipeline/utils/audio_mix.py"
      provides: "Deterministic stinger placement and sidechain filtergraph helpers"
      min_lines: 150
    - path: "src/podcast_pipeline/stages/render.py"
      provides: "Render integration for intro/transition/outro tracks with ducking"
      min_lines: 90
    - path: "src/podcast_pipeline/config/settings.py"
      provides: "Typed sound-kit and ducking config schema"
      min_lines: 80
  key_links:
    - from: "src/podcast_pipeline/models/branding.py"
      to: "src/podcast_pipeline/utils/audio_mix.py"
      via: "resolved profile sound-kit paths feed stinger placement"
      pattern: "sounds|intro|transition|outro"
    - from: "src/podcast_pipeline/utils/audio_mix.py"
      to: "src/podcast_pipeline/stages/render.py"
      via: "ffprobe-guided stinger normalization feeds sidechaincompress in render audio chain"
      pattern: "ffprobe|aresample|aformat|sidechaincompress|attack|release|ratio|threshold"
---

<objective>
Add production sound-kit support with auto-ducked stingers integrated into render.

Purpose: Improve perceived production quality while preserving speech clarity and deterministic render behavior.
Output: Typed sound-kit config, render wiring, and ducking regressions.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-automated-branding-captions-and-multi-track-sync/09-RESEARCH.md
@src/podcast_pipeline/models/branding.py
@src/podcast_pipeline/stages/render.py
@src/podcast_pipeline/config/settings.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed sound-kit and ducking config contracts</name>
  <files>src/podcast_pipeline/config/settings.py
src/podcast_pipeline/models/branding.py
tests/test_models.py</files>
  <action>Extend branding/config models to support intro/transition/outro stinger paths and ducking controls with explicit defaults (`attack=5ms`, `release=200ms`, `ratio=4:1`, `threshold=-30dB`). Add typed audio normalization policy for stingers (target sample rate/channels/sample format) and validate with probe metadata; avoid extension-only allow/deny rules. Retain safe no-op behavior when optional assets are missing.</action>
  <verify>uv run pytest tests/test_models.py -q -k "branding and sound"</verify>
  <done>Sound-kit and ducking settings are typed, validated, and backward-compatible when unset.</done>
</task>

<task type="auto">
  <name>Task 2: Implement audio mix helpers and render-stage integration</name>
  <files>src/podcast_pipeline/utils/audio_mix.py
src/podcast_pipeline/stages/render.py
tests/test_render.py</files>
  <action>Create reusable audio-mix helpers that insert intro/transition/outro stingers at deterministic boundaries and apply `sidechaincompress` against voice tracks. Before ducking, probe and normalize stingers to a canonical working format (for example 48 kHz stereo PCM) so VBR/container quirks do not destabilize timing/sidechain behavior. Integrate into render audio path without altering Phase 7 smoothing/edit semantics.</action>
  <verify>uv run pytest tests/test_render.py -q -k "sound_kit or sidechain or duck"</verify>
  <done>Rendered outputs include configured stingers with bounded ducking that preserves spoken-word clarity.</done>
</task>

<task type="auto">
  <name>Task 3: Add degraded-mode and missing-asset regressions</name>
  <files>tests/test_render.py</files>
  <action>Add tests for missing sound files, disabled ducking, and fallback behavior so base exports still succeed with warnings instead of hard failures when optional sound-kit assets are absent.</action>
  <verify>uv run pytest tests/test_render.py -q -k "sound_kit and (fallback or warning)"</verify>
  <done>Sound-kit feature is additive and failure-tolerant without masking hard errors in required paths.</done>
</task>

</tasks>

<verification>
Sound-kit features are typed, render-integrated, and covered for both happy-path and degraded-mode behavior.
</verification>

<success_criteria>
- Intro/transition/outro audio assets can be configured per branding profile.
- Auto-ducking runs with deterministic defaults and test-backed behavior.
- Optional asset failures degrade safely without breaking core render outputs.
</success_criteria>

<output>
After completion, create `.planning/phases/09-automated-branding-captions-and-multi-track-sync/09-08-SUMMARY.md`
</output>
