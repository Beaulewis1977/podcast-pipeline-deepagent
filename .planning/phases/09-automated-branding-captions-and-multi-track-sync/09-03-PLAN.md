---
phase: 09-automated-branding-captions-and-multi-track-sync
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/podcast_pipeline/providers/claude_provider.py
  - src/podcast_pipeline/providers/__init__.py
  - src/podcast_pipeline/stages/analyze.py
  - src/podcast_pipeline/config/settings.py
  - pyproject.toml
  - .env.example
  - tests/test_providers.py
  - tests/test_config.py
autonomous: true
user_setup:
  - service: anthropic
    why: "Enable Claude analysis provider runtime calls"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console -> API Keys (stored in .env)"

must_haves:
  truths:
    - "Claude is a selectable analysis provider that conforms to the existing AnalysisProvider contract"
    - "If Claude credentials are missing or requests fail, analyze stage degrades/falls back without crashing"
    - "Provider and model validation in config supports Claude safely alongside existing providers"
    - "Claude analysis output is schema-constrained structured output; raw prose JSON parsing is not used as the primary contract"
  artifacts:
    - path: "src/podcast_pipeline/providers/claude_provider.py"
      provides: "Anthropic-backed provider with strict parse and retry/error contracts"
      min_lines: 220
    - path: "src/podcast_pipeline/stages/analyze.py"
      provides: "Provider selection/fallback wiring that can execute Claude and preserve degraded metadata"
      min_lines: 40
    - path: "tests/test_providers.py"
      provides: "Provider protocol conformance and failure-mode coverage for Claude provider"
      min_lines: 140
  key_links:
    - from: "src/podcast_pipeline/config/settings.py"
      to: "src/podcast_pipeline/stages/analyze.py"
      via: "SUPPORTED_MODEL_PROVIDERS and model validation permit claude selection"
      pattern: "SUPPORTED_MODEL_PROVIDERS|SUPPORTED_MODELS_BY_PROVIDER"
    - from: "src/podcast_pipeline/providers/claude_provider.py"
      to: "src/podcast_pipeline/providers/base.py"
      via: "_build_prompt usage with SDK structured output (json schema / tool schema) and typed fallback parse errors"
      pattern: "messages\.parse|output_format|output_config|tools|tool_use|ProviderParseError"
---

<objective>
Add Claude as a first-class analysis provider using Anthropic SDK while preserving existing fallback/degraded behavior contracts.

Purpose: Expand provider resilience and quality options without destabilizing analyze-stage orchestration.
Output: New Claude provider implementation, config wiring, and regression tests.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-automated-branding-captions-and-multi-track-sync/09-RESEARCH.md
@src/podcast_pipeline/providers/base.py
@src/podcast_pipeline/providers/gemini.py
@src/podcast_pipeline/providers/kimi.py
@src/podcast_pipeline/stages/analyze.py
@src/podcast_pipeline/config/settings.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Anthropic-backed Claude provider with strict response contract</name>
  <files>src/podcast_pipeline/providers/claude_provider.py
pyproject.toml
.env.example</files>
  <action>Create a new provider implementation using `anthropic` SDK with schema-constrained structured output (`messages.parse` + `output_format`/`output_config` JSON schema, or equivalent tool-schema contract) that adheres to `AnalysisProvider` semantics and sets `supports_video=False`. Do not rely on parsing free-form assistant prose with `json.loads(raw_text)`. Keep explicit `ProviderParseError` for malformed/contract-violating responses as a guarded fallback.</action>
  <verify>uv run pytest tests/test_providers.py -q -k "claude and (parse or contract)"</verify>
  <done>Claude provider produces valid `AnalysisResult` via structured output contracts and fails with typed provider errors when schema guarantees are violated.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Claude into config validation and analyze-stage provider selection</name>
  <files>src/podcast_pipeline/config/settings.py
src/podcast_pipeline/providers/__init__.py
src/podcast_pipeline/stages/analyze.py
tests/test_config.py</files>
  <action>Extend supported provider/model sets for `claude`, register provider exports, and update analyze-stage initialization so Claude can be primary or fallback without breaking existing Gemini/Kimi flows. Preserve degraded-mode metadata behavior for transcript-only providers.</action>
  <verify>uv run pytest tests/test_config.py tests/test_providers.py -q -k "provider and claude"</verify>
  <done>Config accepts supported Claude model selections and analyze stage can route to Claude with safe fallback semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Add failure-path and credential-absence regressions</name>
  <files>tests/test_providers.py
tests/test_pipeline.py</files>
  <action>Add tests for missing `ANTHROPIC_API_KEY`, retryable vs non-retryable provider errors, and analyze-stage continuation when Claude fails and fallback provider succeeds. Ensure diagnostics remain actionable and deterministic.</action>
  <verify>uv run pytest tests/test_providers.py tests/test_pipeline.py -q -k "claude or fallback_provider"</verify>
  <done>Credential and runtime failures are handled gracefully without analyze-stage hard crashes.</done>
</task>

</tasks>

<verification>
Claude provider is selectable, validated, and covered for success/failure behavior with fallback-safe analyze execution.
</verification>

<success_criteria>
- `claude` appears in supported provider config with constrained model options.
- Analyze stage can use Claude and recover via fallback providers on failure.
- Provider tests lock structured-output contract behavior, parse/retry failure paths, and availability semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/09-automated-branding-captions-and-multi-track-sync/09-03-SUMMARY.md`
</output>
