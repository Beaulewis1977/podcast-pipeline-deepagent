---
phase: 09-automated-branding-captions-and-multi-track-sync
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/podcast_pipeline/mcp/__init__.py
  - src/podcast_pipeline/mcp/ffmpeg_server.py
  - pyproject.toml
  - tests/test_mcp_ffmpeg_server.py
  - .mcp.json
autonomous: true

must_haves:
  truths:
    - "Developers can run a local FFmpeg MCP server that exposes the same typed toolkit operations"
    - "MCP tooling remains dev-only and is not required by production pipeline execution"
    - "Every exposed MCP tool maps directly to a deterministic toolkit function"
  artifacts:
    - path: "src/podcast_pipeline/mcp/ffmpeg_server.py"
      provides: "FastMCP server wiring for all toolkit tools"
      min_lines: 180
    - path: "tests/test_mcp_ffmpeg_server.py"
      provides: "Coverage for tool registration, invocation mapping, and dev-only import boundaries"
      min_lines: 120
    - path: ".mcp.json"
      provides: "Local stdio MCP launch config for Claude Code DX"
      min_lines: 20
  key_links:
    - from: "src/podcast_pipeline/mcp/ffmpeg_server.py"
      to: "src/podcast_pipeline/utils/ffmpeg_toolkit.py"
      via: "one wrapper function per toolkit operation with typed payload translation"
      pattern: "@mcp.tool|toolkit\."
    - from: "pyproject.toml"
      to: "src/podcast_pipeline/mcp/ffmpeg_server.py"
      via: "dev dependency grouping for fastmcp runtime isolation"
      pattern: "optional-dependencies|dev"
---

<objective>
Expose the Phase 9 media toolkit through a developer-only FastMCP server for local automation and debugging.

Purpose: Improve Claude Code developer experience without coupling production runtime to MCP transport.
Output: FastMCP server module, launch config, and registration/integration tests.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<agent_directive>
IMPORTANT: This plan builds an MCP server. You MUST delegate MCP server creation to the
`mcp-creator` subagent (~/.claude/agents/mcp-creator.md), which invokes the /mcp-builder-pro
skill and enforces 2025-11-25 MCP spec best practices (outputSchema on every tool, typed
inputSchema, config_snippet, token_estimate).

Invoke the subagent using the Task tool with subagent_type="mcp-creator" and pass an
orchestrator-mode spec block:

```yaml
Task: Create MCP server
Spec:
  name: ffmpeg-server
  purpose: Expose all 14 typed FFmpeg toolkit operations as MCP tools for local developer automation
  tools:
    - name: probe_media
      description: Probe media file metadata (streams, duration, format)
      input_schema: {input_path: string}
    # ... remaining toolkit operations resolved from ffmpeg_toolkit.py at execution time
  env_vars: []
  output_location: src/podcast_pipeline/mcp/
```

Do NOT write the FastMCP server implementation manually — let the mcp-creator subagent
produce it. Then wire the generated server into the dev dependency group and .mcp.json
as described in the tasks below.
</agent_directive>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-automated-branding-captions-and-multi-track-sync/09-RESEARCH.md
@src/podcast_pipeline/utils/ffmpeg_toolkit.py
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dev-only MCP module and dependency wiring</name>
  <files>src/podcast_pipeline/mcp/__init__.py
src/podcast_pipeline/mcp/ffmpeg_server.py
pyproject.toml</files>
  <action>Create a new `podcast_pipeline.mcp` package and add `fastmcp` in the dev dependency group only. Keep imports localized so production code paths do not require FastMCP to run tests or execute pipeline stages.</action>
  <verify>uv run pytest tests/test_mcp_ffmpeg_server.py -q -k "import or dependency"</verify>
  <done>FastMCP usage is isolated to dev tooling and does not affect production import/runtime paths.</done>
</task>

<task type="auto">
  <name>Task 2: Generate FastMCP server via mcp-creator subagent</name>
  <files>src/podcast_pipeline/mcp/ffmpeg_server.py
tests/test_mcp_ffmpeg_server.py
.mcp.json</files>
  <action>DO NOT implement the FastMCP server manually. Instead, invoke the `mcp-creator` subagent
  (~/.claude/agents/mcp-creator.md) using the Task tool with subagent_type="mcp-creator". Read
  `src/podcast_pipeline/utils/ffmpeg_toolkit.py` first to enumerate all toolkit operations, then
  pass a complete orchestrator-mode spec block:

  ```yaml
  Task: Create MCP server
  Spec:
    name: ffmpeg-server
    purpose: Expose all typed FFmpeg toolkit operations as MCP tools for local developer automation
    tools:
      # one entry per toolkit operation — resolve names and descriptions from ffmpeg_toolkit.py
    env_vars: []
    output_location: src/podcast_pipeline/mcp/
  ```

  The subagent will produce `ffmpeg_server.py` with outputSchema on every tool per the
  2025-11-25 MCP spec. After it completes, add `.mcp.json` stdio configuration so Claude Code
  can launch the server consistently in local development.</action>
  <verify>uv run pytest tests/test_mcp_ffmpeg_server.py -q -k "registration or tool"</verify>
  <done>All toolkit capabilities are callable through MCP, generated by mcp-creator with proper
  outputSchema, and mapped 1:1 to toolkit operations.</done>
</task>

<task type="auto">
  <name>Task 3: Add runtime-boundary tests for dev-only isolation</name>
  <files>tests/test_mcp_ffmpeg_server.py</files>
  <action>Add tests ensuring MCP server code is never imported by pipeline stages and that missing `fastmcp` in non-dev environments yields clear, scoped errors only when MCP entrypoints are invoked.</action>
  <verify>uv run pytest tests/test_mcp_ffmpeg_server.py -q -k "dev_only or isolation"</verify>
  <done>MCP developer tooling is available when requested and safely absent from production pathways.</done>
</task>

</tasks>

<verification>
MCP server launches in dev mode, all tools are registered, and production paths remain decoupled from MCP dependencies.
</verification>

<success_criteria>
- FastMCP server wraps every toolkit operation with typed adapters.
- `.mcp.json` enables reproducible local stdio launch.
- No production stage imports or runtime contracts depend on FastMCP.
</success_criteria>

<output>
After completion, create `.planning/phases/09-automated-branding-captions-and-multi-track-sync/09-02-SUMMARY.md`
</output>
