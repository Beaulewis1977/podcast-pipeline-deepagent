---
phase: 09-automated-branding-captions-and-multi-track-sync
plan: 09
type: execute
wave: 4
depends_on: ["01", "04", "05"]
files_modified:
  - src/podcast_pipeline/utils/thumbnails.py
  - src/podcast_pipeline/stages/analyze.py
  - src/podcast_pipeline/config/settings.py
  - pyproject.toml
  - tests/test_thumbnails.py
  - tests/test_pipeline.py
  - tests/test_render.py
autonomous: true
user_setup:
  - service: google_cloud_vertex_ai
    why: "Enable Imagen 4 generation path"
    env_vars:
      - name: GOOGLE_CLOUD_PROJECT
        source: "Google Cloud project settings"
      - name: GOOGLE_APPLICATION_CREDENTIALS
        source: "Service account JSON key path"
  - service: local_flux
    why: "Enable local FLUX fallback path"
    env_vars:
      - name: HF_HOME
        source: "Local model cache path (optional)"

must_haves:
  truths:
    - "Thumbnail generation prefers Imagen 4 GA and falls back to local FLUX when cloud path is unavailable"
    - "Prompt-hash caching prevents duplicate thumbnail generation work"
    - "Generated thumbnails can receive branding overlays through the toolkit overlay operation"
  artifacts:
    - path: "src/podcast_pipeline/utils/thumbnails.py"
      provides: "Dual-backend thumbnail service with cache, fallback, and VRAM preflight"
      min_lines: 260
    - path: "src/podcast_pipeline/stages/analyze.py"
      provides: "Analyze-stage wiring to generate/store thumbnail artifacts via thumbnail service"
      min_lines: 90
    - path: "pyproject.toml"
      provides: "Optional thumbnail dependency group aligned to currently available stable versions"
      min_lines: 20
  key_links:
    - from: "src/podcast_pipeline/utils/thumbnails.py"
      to: "src/podcast_pipeline/utils/ffmpeg_toolkit.py"
      via: "post-generation overlay composition uses toolkit overlay_image operation"
      pattern: "overlay|toolkit"
    - from: "src/podcast_pipeline/stages/analyze.py"
      to: "src/podcast_pipeline/ui/app.py"
      via: "thumbnail artifact metadata persisted in analysis payload for UI gallery"
      pattern: "thumbnail_frames|image_path"
---

<objective>
Build an AI thumbnail service with Imagen 4 primary path, FLUX fallback, caching, and branding overlay support.

Purpose: Produce actionable thumbnail assets reliably across cloud and local environments.
Output: Thumbnail service, analyze integration, dependency wiring, and robust backend regressions.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-automated-branding-captions-and-multi-track-sync/09-RESEARCH.md
@src/podcast_pipeline/stages/analyze.py
@src/podcast_pipeline/config/settings.py
@src/podcast_pipeline/utils/ffmpeg_toolkit.py
@tests/test_pipeline.py
@tests/test_render.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement thumbnail service with Imagen-first + FLUX fallback routing</name>
  <files>src/podcast_pipeline/utils/thumbnails.py
src/podcast_pipeline/config/settings.py
tests/test_thumbnails.py</files>
  <action>Create a thumbnail service that tries Imagen 4 GA model IDs first (`imagen-4.0-generate-001` family) and falls back to local FLUX when cloud credentials/quota are unavailable. Add prompt-hash cache keys and deterministic artifact naming to avoid duplicate generation churn.</action>
  <verify>uv run pytest tests/test_thumbnails.py -q -k "imagen or flux or cache"</verify>
  <done>Thumbnail generation is resilient, deterministic, and avoids redundant model calls for identical prompts/settings.</done>
</task>

<task type="auto">
  <name>Task 2: Add VRAM preflight and branding overlay integration</name>
  <files>src/podcast_pipeline/utils/thumbnails.py
src/podcast_pipeline/stages/analyze.py
tests/test_pipeline.py</files>
  <action>Implement local-FLUX VRAM preflight checks before model load and return explicit degraded/failure metadata when insufficient memory is detected. After generation, apply optional branding overlay via toolkit `overlay_image` and persist relative image paths into analysis artifacts.</action>
  <verify>uv run pytest tests/test_thumbnails.py tests/test_pipeline.py -q -k "vram or overlay or thumbnail"</verify>
  <done>Local fallback path avoids OOM surprises and generated thumbnails are consistently discoverable by downstream UI/render logic.</done>
</task>

<task type="auto">
  <name>Task 3: Align optional dependency group and add integration regressions</name>
  <files>pyproject.toml
tests/test_thumbnails.py
tests/test_render.py</files>
  <action>Add a dedicated `[thumbnails]` optional dependency group with currently available stable package floors (including correcting unavailable floor assumptions). Add regressions for cache hits, backend failover, and overlay post-processing continuity.</action>
  <verify>uv run pytest tests/test_thumbnails.py tests/test_render.py -q -k "thumbnail"</verify>
  <done>Dependency constraints are installable today and thumbnail workflows are protected against backend drift/regressions.</done>
</task>

</tasks>

<verification>
Thumbnail generation is cloud-first with local fallback, cache-aware, overlay-capable, and backed by installable dependency constraints.
</verification>

<success_criteria>
- Imagen 4 GA and FLUX fallback pathways both execute with explicit routing diagnostics.
- Prompt-hash cache avoids duplicate thumbnail generation runs.
- Thumbnail artifacts remain compatible with existing UI/render consumers.
</success_criteria>

<output>
After completion, create `.planning/phases/09-automated-branding-captions-and-multi-track-sync/09-09-SUMMARY.md`
</output>
