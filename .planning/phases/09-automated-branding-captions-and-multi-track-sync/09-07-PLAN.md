---
phase: 09-automated-branding-captions-and-multi-track-sync
plan: 07
type: execute
wave: 4
depends_on: ["05"]
files_modified:
  - src/podcast_pipeline/utils/sync.py
  - src/podcast_pipeline/stages/ingest.py
  - src/podcast_pipeline/stages/render.py
  - src/podcast_pipeline/ui/app.py
  - tests/test_sync.py
  - tests/test_ui_app.py
autonomous: true

must_haves:
  truths:
    - "Multi-track sources can be auto-aligned using bounded cross-correlation with confidence scoring"
    - "Computed sync offset is persisted and reused by downstream stages"
    - "Operators can manually adjust sync offset in Streamlit when confidence is low"
  artifacts:
    - path: "src/podcast_pipeline/utils/sync.py"
      provides: "Bounded sync estimator using resample + correlation + confidence metrics"
      min_lines: 180
    - path: "src/podcast_pipeline/stages/ingest.py"
      provides: "Sync artifact generation and manifest persistence during ingest"
      min_lines: 60
    - path: "src/podcast_pipeline/ui/app.py"
      provides: "Manual sync offset control with bounded +/- 5000 ms fallback"
      min_lines: 80
  key_links:
    - from: "src/podcast_pipeline/utils/sync.py"
      to: "src/podcast_pipeline/stages/ingest.py"
      via: "estimated offset + confidence persisted into job artifacts"
      pattern: "correlate|resample|sync_offset_ms|confidence"
    - from: "src/podcast_pipeline/ui/app.py"
      to: "src/podcast_pipeline/stages/render.py"
      via: "manual override value updates persisted sync artifact consumed at render"
      pattern: "sync_slider|manual_offset_ms|manifest"
---

<objective>
Implement bounded automatic multi-track sync with persisted offsets and a manual UI fallback.

Purpose: Ensure separate audio/video tracks stay aligned without expensive unbounded correlation or fragile manual-only workflows.
Output: Sync utility, stage wiring, manual slider fallback, and regression tests.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-automated-branding-captions-and-multi-track-sync/09-RESEARCH.md
@src/podcast_pipeline/stages/ingest.py
@src/podcast_pipeline/stages/render.py
@src/podcast_pipeline/ui/app.py
@src/podcast_pipeline/models/job.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build bounded cross-correlation sync utility with confidence scoring</name>
  <files>src/podcast_pipeline/utils/sync.py
tests/test_sync.py</files>
  <action>Create a sync estimator that downsamples to mono 8 kHz, analyzes a bounded head window (~60s), uses `scipy.signal.correlate` + `correlation_lags` and `librosa.resample`, then returns offset in ms with confidence and warning flags for ambiguous matches.</action>
  <verify>uv run pytest tests/test_sync.py -q -k "offset or confidence or bounded"</verify>
  <done>Sync estimator returns stable offsets for fixture data and flags low-confidence/no-clap conditions explicitly.</done>
</task>

<task type="auto">
  <name>Task 2: Persist sync artifacts and consume offsets in pipeline stages</name>
  <files>src/podcast_pipeline/stages/ingest.py
src/podcast_pipeline/stages/render.py
tests/test_pipeline.py</files>
  <action>Persist sync output (offset, confidence, source, timestamp) into deterministic job artifacts/manifest during ingest and ensure render consumes the persisted/manual-resolved offset when aligning multi-track audio operations.</action>
  <verify>uv run pytest tests/test_pipeline.py tests/test_sync.py -q -k "sync"</verify>
  <done>Auto-detected or overridden sync offsets are durable and used consistently by downstream processing.</done>
</task>

<task type="auto">
  <name>Task 3: Add Streamlit manual sync slider fallback</name>
  <files>src/podcast_pipeline/ui/app.py
tests/test_ui_app.py</files>
  <action>Add a bounded `-5000..5000 ms` manual sync control in Streamlit that appears when confidence is low (or user chooses override), persists choice to review/job state, and surfaces clear warnings when manual override diverges from auto offset.</action>
  <verify>uv run pytest tests/test_ui_app.py -q -k "sync and slider"</verify>
  <done>Operators can reliably correct low-confidence auto sync results without editing artifacts manually.</done>
</task>

</tasks>

<verification>
Automatic and manual sync paths are both deterministic, persisted, and exercised by dedicated tests.
</verification>

<success_criteria>
- Sync utility computes bounded offsets with confidence and warning metadata.
- Sync artifacts persist and are honored by render.
- Streamlit exposes safe manual override with persistence and diagnostics.
</success_criteria>

<output>
After completion, create `.planning/phases/09-automated-branding-captions-and-multi-track-sync/09-07-SUMMARY.md`
</output>
