---
phase: 09-automated-branding-captions-and-multi-track-sync
plan: 11
type: execute
wave: 6
depends_on: ["09-09"]
files_modified:
  - src/podcast_pipeline/utils/thumbnails.py
  - src/podcast_pipeline/config/settings.py
  - src/podcast_pipeline/stages/analyze.py
  - src/podcast_pipeline/ui/app.py
  - tests/test_thumbnails.py
  - tests/test_pipeline.py
  - pyproject.toml
autonomous: true

must_haves:
  truths:
    - "All Imagen 4, google-cloud-aiplatform, Vertex AI, FLUX.1 Schnell, diffusers, optimum-quanto, and VRAM preflight code is completely removed from source, tests, and dependencies"
    - "Thumbnail generation uses gemini-2.5-flash-image for image generation via google.genai SDK; _audit_with_gemini_pro() using gemini-3-pro-image-preview is defined for future use but not wired into the generation flow in this plan"
    - "Prompt-hash caching (compute_cache_key + _cache_path) is preserved — identical prompts do not re-trigger generation"
    - "Branding overlay (_apply_branding_overlay via FFmpeg toolkit) is preserved unchanged"
    - "The [thumbnails] optional dependency group in pyproject.toml contains zero Imagen/FLUX/Vertex packages"
    - "Plans 09-01 through 09-10 remain functionally intact with zero regressions"
    - "All tests pass (uv run pytest tests/ -q) with 100% pass rate"
  artifacts:
    - path: "src/podcast_pipeline/utils/thumbnails.py"
      provides: "Single-backend Gemini Vision thumbnail generation service with prompt-hash caching and branding overlay"
      min_lines: 250
    - path: "tests/test_thumbnails.py"
      provides: "Gemini Vision endpoint mocks replacing all Imagen/FLUX mocks; cache, availability, branding, and degradation tests"
      min_lines: 200
  key_links:
    - from: "src/podcast_pipeline/utils/thumbnails.py"
      to: "src/podcast_pipeline/stages/analyze.py"
      via: "Analyze stage passes prompts to Gemini-based ThumbnailService via ThumbnailRequest"
      pattern: "ThumbnailService|ThumbnailRequest|generate"
    - from: "src/podcast_pipeline/ui/app.py"
      to: "src/podcast_pipeline/utils/thumbnails.py"
      via: "Production sidebar AI Thumbnail toggle help text references Gemini (not Imagen/FLUX)"
      pattern: "ai_thumbnails|Gemini"
---

<objective>
Refactor the AI Thumbnail Studio to replace Imagen 4 GA / FLUX.1 Schnell dual-backend with a single Gemini Vision backend via google.genai SDK.

Purpose: Eliminate Vertex AI auth complexity, VRAM/GPU requirements, and three optional dependencies (google-cloud-aiplatform, diffusers, optimum-quanto) by switching to the Gemini Vision models already available through the project's existing google-genai SDK.

Output: A clean, Gemini-only thumbnails.py with updated config, dependencies, tests, and verified stage/UI wiring.
</objective>

<execution_context>
@/home/kngpnn/.claude/get-shit-done/workflows/execute-plan.md
@/home/kngpnn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-automated-branding-captions-and-multi-track-sync/09-11-RESEARCH.md
@.planning/phases/09-automated-branding-captions-and-multi-track-sync/PHASE-09-11-VISION-MODEL-UPDATES.MD
@src/podcast_pipeline/utils/thumbnails.py
@src/podcast_pipeline/config/settings.py
@src/podcast_pipeline/stages/analyze.py
@src/podcast_pipeline/ui/app.py
@tests/test_thumbnails.py
@tests/test_pipeline.py
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor thumbnails.py — replace Imagen 4 / FLUX.1 with Gemini Vision</name>
  <files>src/podcast_pipeline/utils/thumbnails.py
src/podcast_pipeline/config/settings.py</files>
  <action>
CRITICAL: Read these two documents FIRST — they are the single source of truth:
  @.planning/phases/09-automated-branding-captions-and-multi-track-sync/PHASE-09-11-VISION-MODEL-UPDATES.MD
  @.planning/phases/09-automated-branding-captions-and-multi-track-sync/09-11-RESEARCH.md

The RESEARCH.md contains verified SDK patterns (google-genai 1.60.0), model IDs, anti-patterns, and pitfalls. Follow it precisely.

### In src/podcast_pipeline/utils/thumbnails.py:

**REMOVE these items completely:**
- Constants: IMAGEN4_MODEL_STANDARD, IMAGEN4_MODEL_FAST, IMAGEN4_MODEL_ULTRA, FLUX_SCHNELL_MODEL_ID, FLUX_MIN_FREE_VRAM_GIB, FLUX_DEFAULT_STEPS
- Enum values: ThumbnailBackend.IMAGEN4, ThumbnailBackend.FLUX
- Functions: _free_vram_gib(), check_vram_preflight(), _imagen4_available(), _flux_available(), _generate_imagen4(), _unload_gpu_models(), _generate_flux()
- Imports: importlib, importlib.util (only used by _flux_available)
- ThumbnailRequest fields: project_id, location
- All docstrings/comments referencing Imagen, FLUX, Vertex AI, VRAM, diffusers, optimum-quanto

**ADD these items:**
- Constants:
    GEMINI_VISION_FLASH = "gemini-2.5-flash-image"      # Image generation (GA)
    GEMINI_VISION_PRO = "gemini-3-pro-image-preview"     # Vision audit / prompt engineering (Preview)

- Enum:
    class ThumbnailBackend(str, Enum):
        GEMINI = "gemini"
        NONE = "none"

- Function _gemini_available() -> tuple[bool, str]:
    Check for GEMINI_API_KEY env var and google.genai importability.
    Pattern from RESEARCH.md "Pattern 4: Availability Check".

- Function _generate_gemini(prompt, output_dir, *, model, aspect_ratio, seed) -> list[tuple[Path, bool]]:
    Use client.models.generate_content() with response_modalities=["TEXT", "IMAGE"] and
    types.ImageConfig(aspect_ratio="16:9", output_mime_type="image/jpeg").
    Access image bytes via part.inline_data.data — do NOT use part.as_image() (requires PIL).
    Write bytes to disk via Path.write_bytes().
    Pattern from RESEARCH.md "Pattern 1: Image Generation".

- Function _audit_with_gemini_pro(image_path, audit_prompt, *, model) -> str:
    Pass image bytes via types.Part.from_bytes() for vision analysis.
    Pattern from RESEARCH.md "Pattern 2: Vision Analysis".
    This function supports future compositional auditing but is not wired into
    the generation flow yet — just define it for downstream use.

**IMPORTANT — field naming:**
The RESEARCH.md examples show `generation_model` as a field name. Do NOT rename the existing
`model` field to `generation_model`. Keep the field named `model` because analyze.py line 413
passes `model=gen_config.model` and ThumbnailGenerationConfig.model uses this name. Renaming
would silently break the analyze stage wiring.

**UPDATE these items:**
- ThumbnailRequest: Remove project_id and location fields. Keep `model` field name, change its
  default to GEMINI_VISION_FLASH. Add audit_model field defaulting to GEMINI_VISION_PRO.
- ThumbnailService._select_backend(): Single check — call _gemini_available(). Return GEMINI or NONE.
- ThumbnailService._process_prompt(): Change model_key to use request.model (always Gemini model ID now).
- ThumbnailService._run_backend(): Single branch — call _generate_gemini() for GEMINI backend.
- Module docstring: Update to describe Gemini Vision single-backend architecture.

**PRESERVE unchanged:**
- compute_cache_key(), _cache_path() — cache logic is model-agnostic
- _apply_branding_overlay() — uses FFmpeg toolkit, not model-specific
- ThumbnailArtifact, ThumbnailResult, ThumbnailStatus dataclasses
- ThumbnailService.generate() overall flow (cache check -> generate -> brand -> return)
- The 5-prompt truncation safety limit

### In src/podcast_pipeline/config/settings.py:

Update ThumbnailGenerationConfig:
- Change model default from "imagen-4.0-generate-001" to "gemini-2.5-flash-image"
- Update description strings to reference Gemini Vision (remove Imagen/FLUX mentions)
- Update the class docstring to say "Gemini Vision backend" instead of "Imagen 4 nor FLUX"

SAFETY: Do NOT modify any other config classes or any files from plans 09-01 through 09-10.
  </action>
  <verify>uv run ruff check src/podcast_pipeline/utils/thumbnails.py src/podcast_pipeline/config/settings.py && uv run ruff format --check src/podcast_pipeline/utils/thumbnails.py src/podcast_pipeline/config/settings.py && uv run mypy src/podcast_pipeline/utils/thumbnails.py src/podcast_pipeline/config/settings.py</verify>
  <done>thumbnails.py uses exclusively Gemini Vision models via google.genai SDK. Zero traces of Imagen/FLUX/Vertex/VRAM code remain. Settings default to gemini-2.5-flash-image.</done>
</task>

<task type="auto">
  <name>Task 2: Clean up [thumbnails] dependencies in pyproject.toml</name>
  <files>pyproject.toml</files>
  <action>
In pyproject.toml, find the [project.optional-dependencies] thumbnails group (currently at ~line 68):

REMOVE these three entries:
    "google-cloud-aiplatform>=1.70.0,<2",
    "diffusers>=0.32.0,<1",
    "optimum-quanto>=0.2.5,<1",

The thumbnails group should become empty (thumbnails = []) or be removed entirely.

VERIFY: google-genai>=1.0.0 is already in [project.dependencies] (core deps, ~line 28).
It does NOT need to be added to [thumbnails] — it is always installed.

Also clean up mypy overrides in pyproject.toml:
- Remove these entries from the [[tool.mypy.overrides]] module list: "google.cloud.aiplatform",
  "google.cloud.aiplatform.*", "diffusers", "diffusers.*", "optimum.quanto", "optimum.quanto.*"
  (these were only needed for the removed [thumbnails] optional deps).
- Update the comment at ~line 274 that says "thumbnails.py uses lazy imports of optional
  [thumbnails] extras" to reflect that the optional extras are no longer needed for the Gemini
  backend (google-genai is a core dep, always installed).
- The [[tool.mypy.overrides]] block for thumbnails.py (module = "podcast_pipeline.utils.thumbnails",
  ignore_missing_imports = true, warn_unused_ignores = false) can be kept — thumbnails.py still
  does lazy imports of google.genai inside functions, and mypy strictness varies by environment.

Run: uv lock --upgrade-package google-genai

Do NOT modify any other dependency groups (gpu, dev, etc.).
  </action>
  <verify>python3 -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); thumbs=d['project']['optional-dependencies'].get('thumbnails',[]); assert not any('aiplatform' in x or 'diffusers' in x or 'quanto' in x for x in thumbs), f'Legacy deps found: {thumbs}'; print(f'thumbnails extras: {thumbs} — clean')"</verify>
  <done>[thumbnails] group contains no Imagen/FLUX/Vertex packages. google-genai is confirmed in core deps. Lock file updated.</done>
</task>

<task type="auto">
  <name>Task 3: Verify and fix analyze stage + UI wiring</name>
  <files>src/podcast_pipeline/stages/analyze.py
src/podcast_pipeline/ui/app.py</files>
  <action>
### analyze.py (src/podcast_pipeline/stages/analyze.py)

Read _generate_ai_thumbnails() (~line 360-446). It constructs a ThumbnailRequest like:

    request = ThumbnailRequest(
        prompts=prompts,
        output_dir=output_dir,
        images_per_prompt=gen_config.images_per_prompt,
        width=gen_config.width,
        height=gen_config.height,
        model=gen_config.model,
        branding_profile=self._active_branding,
    )

This should work as-is after the refactor because:
- project_id and location were never passed here (they defaulted in ThumbnailRequest)
- model now comes from ThumbnailGenerationConfig which defaults to gemini-2.5-flash-image

VERIFY: Confirm no project_id or location kwargs are passed. If they are, remove them.
VERIFY: No other imports from thumbnails.py reference removed symbols (IMAGEN4, FLUX, etc.).

No changes expected — but read and confirm.

### app.py (src/podcast_pipeline/ui/app.py)

Find the AI Thumbnail Generation toggle in the production sidebar (~line 2606):

    help="Generate AI thumbnails via Imagen 4 or FLUX during analysis.",

UPDATE this help string to:

    help="Generate AI thumbnails via Gemini Vision during analysis.",

Search the entire file for any other strings mentioning "Imagen", "FLUX", "Vertex",
"diffusers", or "VRAM" and update or remove them.

SAFETY: Do NOT modify Brand Studio CRUD, caption controls, sync slider, sound kit toggles,
or any review-state persistence logic.
  </action>
  <verify>uv run ruff check src/podcast_pipeline/stages/analyze.py src/podcast_pipeline/ui/app.py && uv run mypy src/podcast_pipeline/stages/analyze.py</verify>
  <done>Analyze stage passes correct params to Gemini ThumbnailService. UI help text updated. No stale Imagen/FLUX references remain in consumer code.</done>
</task>

<task type="auto">
  <name>Task 4: Overhaul test mocks and run full test suite</name>
  <files>tests/test_thumbnails.py
tests/test_pipeline.py</files>
  <action>
### tests/test_thumbnails.py — MAJOR OVERHAUL

Read the 09-11-RESEARCH.md "Mock Pattern for Tests" section for the canonical mock approach.

**REMOVE these test classes entirely:**
- TestVramPreflight (4 tests) — VRAM is irrelevant for cloud API
- TestImagen4Availability (2 tests) — replaced by Gemini availability tests
- TestFluxAvailability (2 tests) — FLUX removed entirely

**ADD new test class TestGeminiAvailability:**
- test_unavailable_when_api_key_missing: patch os.environ to remove GEMINI_API_KEY, assert _gemini_available() returns (False, ...)
- test_unavailable_when_sdk_missing: patch the google.genai import to fail, assert (False, ...)
- test_available_when_key_and_sdk_present: set GEMINI_API_KEY env var, assert (True, "")

**UPDATE TestBackendSelection (4 tests):**
- test_gemini_selected_when_api_key_available: patch _gemini_available -> (True, ""), assert backend == ThumbnailBackend.GEMINI
- test_none_backend_when_gemini_unavailable: patch _gemini_available -> (False, "no key"), assert backend == ThumbnailBackend.NONE and "No thumbnail backend" or similar in reason
- REMOVE: test_flux_fallback_when_imagen4_unavailable (no fallback in single-backend)
- REMOVE: test_none_backend_when_flux_vram_insufficient (no VRAM concept)

**UPDATE TestCacheHit:**
- Change model string from "imagen-4.0-generate-001" to "gemini-2.5-flash-image" in all cache key computations
- Change ThumbnailBackend.IMAGEN4 references to ThumbnailBackend.GEMINI
- Update ThumbnailRequest constructor calls: remove project_id, update model default

**UPDATE TestDegradedMode:**
- Patch _gemini_available instead of _imagen4_available + _flux_available

**UPDATE TestArtifactMetadata:**
- Replace ThumbnailBackend.FLUX and ThumbnailBackend.IMAGEN4 with ThumbnailBackend.GEMINI

**UPDATE TestIntegrationRegressions:**
- Change all "imagen-4.0-generate-001" model strings to "gemini-2.5-flash-image"
- Replace ThumbnailBackend.IMAGEN4/FLUX references with ThumbnailBackend.GEMINI
- Replace _imagen4_available/_flux_available/check_vram_preflight patches with _gemini_available
- REMOVE: test_backend_failover_imagen4_to_flux_on_explicit_routing (no fallback)

**UPDATE TestBrandingOverlay:**
- Change "imagen-4.0-generate-001" model in test_service_applies_branding_on_cache_hit to "gemini-2.5-flash-image"
- Update ThumbnailBackend.IMAGEN4 to ThumbnailBackend.GEMINI

**UPDATE TestPromptTruncation:**
- Change ThumbnailBackend.IMAGEN4 to ThumbnailBackend.GEMINI

### tests/test_pipeline.py

Search for ThumbnailBackend.IMAGEN4 references and replace with ThumbnailBackend.GEMINI.
In TestAnalyzeStageAIThumbnailGeneration.test_generate_ai_thumbnails_writes_ai_image_path_to_frames:
- Update ThumbnailBackend.IMAGEN4 to ThumbnailBackend.GEMINI (~2 occurrences)

### FINAL GATE — Run full test suite:

    uv run pytest tests/ -q

ALL tests must pass. Zero failures. Zero errors. If any test from plans 09-01 through 09-10
breaks, investigate the root cause and fix the test mock/assertion — do NOT modify the
production code of those earlier plans.
  </action>
  <verify>uv run ruff check tests/test_thumbnails.py tests/test_pipeline.py && uv run mypy tests/test_thumbnails.py && uv run pytest tests/ -q</verify>
  <done>All Imagen/FLUX test mocks replaced with Gemini Vision mocks. Full test suite passes with zero failures and zero errors.</done>
</task>

</tasks>

<verification>
After all 4 tasks complete, verify the following holistically:

1. grep -r "imagen\|IMAGEN\|Imagen" src/ tests/ — must return ZERO matches
2. grep -r "FLUX\|flux\|diffusers\|optimum.quanto\|torchao" src/ tests/ — must return ZERO matches (excluding __pycache__)
3. grep -r "google.cloud.aiplatform\|google-cloud-aiplatform\|Vertex" src/ tests/ pyproject.toml — must return ZERO matches
4. grep -r "check_vram\|_free_vram\|_unload_gpu" src/ tests/ — must return ZERO matches
5. grep -r "gemini-2.5-flash-image\|gemini-3-pro-image-preview" src/podcast_pipeline/utils/thumbnails.py — must return matches (confirms new models are wired)
6. uv run pytest tests/ -q — ALL tests pass
7. uv run ruff check . — no lint errors
8. uv run mypy src/ — no type errors

Thumbnail generation is exclusively Gemini Vision-powered. All existing Phase 9 features (FFmpeg toolkit, MCP server, Claude provider, codec profiles, branding, captions, sync, sound kits, Brand Studio UI) remain intact.
</verification>

<success_criteria>
- thumbnails.py contains zero references to Imagen, FLUX, Vertex AI, diffusers, optimum-quanto, VRAM preflight, or google-cloud-aiplatform
- thumbnails.py uses gemini-2.5-flash-image for generation and gemini-3-pro-image-preview for vision audit via google.genai SDK
- Image bytes accessed via part.inline_data.data (NOT part.as_image which requires PIL)
- Prompt-hash caching (compute_cache_key + _cache_path) prevents duplicate generation
- Branding overlay (_apply_branding_overlay) preserved unchanged
- ThumbnailGenerationConfig.model defaults to "gemini-2.5-flash-image"
- pyproject.toml [thumbnails] group has no legacy packages
- analyze.py and app.py consume Gemini-based ThumbnailService without stale references
- All tests pass: uv run pytest tests/ -q with 100% pass rate
- Plans 09-01 through 09-10 are functionally unmodified
</success_criteria>

<output>
After completion, create `.planning/phases/09-automated-branding-captions-and-multi-track-sync/09-11-SUMMARY.md`
</output>
