---
phase: 05.1-streamlit-video-platform-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/podcast_pipeline/export_targets.py
  - src/podcast_pipeline/stages/review.py
  - tests/test_review.py
autonomous: true

must_haves:
  truths:
    - "Review-state export targets are normalized against one canonical key set, so malformed or unknown keys cannot silently drive render behavior"
    - "Legacy jobs and new jobs both retain safe default export intent (`youtube`, `spotify`) when no valid explicit selection exists"
    - "Approve flows persist deterministic, deduplicated platform keys in review artifacts"
  artifacts:
    - path: "src/podcast_pipeline/export_targets.py"
      provides: "Canonical export-target registry and normalization utilities shared across UI/CLI/review flows"
      min_lines: 70
    - path: "src/podcast_pipeline/stages/review.py"
      provides: "Review-decisions integration that consumes canonical target normalization"
      min_lines: 70
    - path: "tests/test_review.py"
      provides: "Regression coverage for export-target normalization, fallback defaults, and review persistence behavior"
      min_lines: 80
  key_links:
    - from: "src/podcast_pipeline/export_targets.py"
      to: "src/podcast_pipeline/stages/review.py"
      via: "shared `normalize_export_platforms()` contract used by review state load/save and `approve_review()`"
      pattern: "normalize_export_platforms|DEFAULT_EXPORT_PLATFORMS|SUPPORTED_EXPORT_PLATFORMS"
    - from: "src/podcast_pipeline/stages/review.py"
      to: "review/review_state.json"
      via: "approved export platform keys serialized from normalized review decisions"
      pattern: "export_platforms|model_dump_json|approve_review"
---

<objective>
Create the platform-selection source of truth before UI and CLI wiring.

Purpose: Prevent cross-surface drift by centralizing valid export keys, defaults, labels, and normalization behavior.
Output: Canonical registry module, review-stage normalization integration, and tests proving backward-compatible persistence.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-streamlit-video-platform-ui/05.1-RESEARCH.md
@src/podcast_pipeline/stages/review.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build canonical export-target registry + normalization utilities</name>
  <files>src/podcast_pipeline/export_targets.py</files>
  <action>Create a dedicated module that defines supported platform keys, defaults (`youtube`, `spotify`), display metadata (audio/video/package grouping), and a normalization helper that trims, deduplicates, validates, and optionally returns dropped invalid keys for diagnostics.</action>
  <verify>uv run pytest tests/test_review.py -q -k "normalize_export_platforms or supported_export_platforms"</verify>
  <done>One shared registry and normalization API exists for all export selection surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Wire review decision read/write and approve flow to canonical normalization</name>
  <files>src/podcast_pipeline/stages/review.py</files>
  <action>Update `ReviewDecisions` and `approve_review()` so persisted/exported platforms always pass through canonical normalization, preserve defaults for empty/invalid selections, and maintain backward compatibility for existing `review_state.json` files containing unknown keys.</action>
  <verify>uv run pytest tests/test_review.py -q -k "approve_review and export_platforms"</verify>
  <done>Review artifacts persist only valid export target keys with deterministic fallback behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add review-level regression coverage for normalization and fallback contracts</name>
  <files>tests/test_review.py</files>
  <action>Add tests for dedupe/trim behavior, unknown-key dropping, legacy empty/invalid key fallback to defaults, and `approve_review()` persistence guarantees. Include at least one test proving new video keys (`spotify_video`, `apple_video`, `apple_hls`) survive normalization unchanged.</action>
  <verify>uv run pytest tests/test_review.py -q</verify>
  <done>Review normalization behavior is enforced by deterministic tests covering both legacy and new video target workflows.</done>
</task>

</tasks>

<verification>
Contract checks: one canonical key registry exists, review persistence normalizes keys safely, and defaults remain stable for legacy jobs.
</verification>

<success_criteria>
- Shared export-target registry and normalization helper are implemented.
- Review stage and approve flow persist only valid, deduplicated platform keys.
- Legacy/malformed review-state platform data degrades safely to default targets.
- Regression tests lock normalization and video-target key behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-streamlit-video-platform-ui/05.1-01-SUMMARY.md`
</output>
