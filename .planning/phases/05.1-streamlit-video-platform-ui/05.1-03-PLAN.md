---
phase: 05.1-streamlit-video-platform-ui
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/podcast_pipeline/cli.py
  - tests/test_cli.py
autonomous: true

must_haves:
  truths:
    - "CLI `approve --platforms` accepts valid platform keys (including video targets) and rejects unknown keys with actionable error messages"
    - "CLI and Streamlit approval flows use the same canonical platform-key contract, preventing drift"
    - "Default approval behavior remains backward-compatible when no explicit platform list is provided"
  artifacts:
    - path: "src/podcast_pipeline/cli.py"
      provides: "Approve command parsing/validation wired to canonical export target registry"
      min_lines: 80
    - path: "tests/test_cli.py"
      provides: "CLI regression tests for valid/invalid platform key parsing and default fallback behavior"
      min_lines: 80
  key_links:
    - from: "src/podcast_pipeline/export_targets.py"
      to: "src/podcast_pipeline/cli.py"
      via: "shared normalization/validation for `approve --platforms` parsing"
      pattern: "normalize_export_platforms|SUPPORTED_EXPORT_PLATFORMS"
    - from: "src/podcast_pipeline/cli.py"
      to: "src/podcast_pipeline/stages/review.py"
      via: "validated platform list passed into `approve_review()`"
      pattern: "approve_review|platforms|Review approved"
---

<objective>
Lock CLI parity with Streamlit export-target behavior.

Purpose: Ensure operators get the same validated platform contract from command-line and UI workflows.
Output: Canonical-key CLI parsing/validation and regression tests for invalid-key failure contracts.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-streamlit-video-platform-ui/05.1-RESEARCH.md
@.planning/phases/05.1-streamlit-video-platform-ui/05.1-01-PLAN.md
@src/podcast_pipeline/cli.py
@tests/test_cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate `approve --platforms` input using shared platform normalization</name>
  <files>src/podcast_pipeline/cli.py</files>
  <action>Refactor approve-platform parsing so comma-separated input is normalized by the canonical helper (trim, dedupe, validate). Preserve existing default behavior for omitted input while allowing new keys (`spotify_video`, `apple_video`, `apple_hls`).</action>
  <verify>uv run pytest tests/test_cli.py -q -k "approve and platform"</verify>
  <done>Approve command accepts valid platform keys consistently with Streamlit/review behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add explicit invalid-key error contract for operator diagnostics</name>
  <files>src/podcast_pipeline/cli.py</files>
  <action>When unknown keys are supplied, fail fast before job mutation with a clear message listing invalid keys and supported keys. Keep error exit behavior deterministic (`Exit(1)`) and avoid silently dropping unsupported CLI input.</action>
  <verify>uv run pytest tests/test_cli.py -q -k "invalid platform or unsupported platform"</verify>
  <done>CLI rejects unknown platform keys with actionable diagnostics and no silent fallback for explicit invalid input.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests for CLI/default/invalid platform parsing</name>
  <files>tests/test_cli.py</files>
  <action>Add tests for default approve behavior, valid mixed audio/video platform parsing, dedupe/trim normalization, and explicit invalid key failure paths. Ensure tests prove CLI behavior matches canonical supported key set.</action>
  <verify>uv run pytest tests/test_cli.py -q</verify>
  <done>CLI platform parsing/validation behavior is covered and resistant to regressions.</done>
</task>

</tasks>

<verification>
CLI checks: valid keys pass, invalid keys fail loudly, and defaults remain stable.
</verification>

<success_criteria>
- CLI approve flow uses canonical export-target validation.
- Unknown keys return explicit actionable errors before review mutation.
- New video targets are accepted by CLI platform parsing.
- Regression tests enforce CLI parity with shared platform contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-streamlit-video-platform-ui/05.1-03-SUMMARY.md`
</output>
