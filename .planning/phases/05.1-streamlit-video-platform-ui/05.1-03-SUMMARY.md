---
phase: 05.1-streamlit-video-platform-ui
plan: 03
subsystem: cli
tags: [cli, approve-flow, export-platforms, validation, regression-tests]
requires:
  - phase: 05.1-streamlit-video-platform-ui
    provides: "Canonical export-target registry + Streamlit normalization contract"
provides:
  - "CLI approve platform parsing wired to canonical normalization"
  - "Actionable invalid-key errors with explicit supported-key list"
  - "Backward-compatible default behavior when --platforms is omitted"
  - "Regression coverage for valid/invalid/empty explicit platform parsing"
affects: [phase-verification, cross-surface-contract, review-approval]
tech-stack:
  added: []
  patterns:
    - "CLI parser delegates platform validation to shared canonical helper"
    - "Explicit user input errors fail loudly; omission uses stable defaults"
key-files:
  created: []
  modified:
    - src/podcast_pipeline/cli.py
    - tests/test_cli.py
key-decisions:
  - "Moved approve-platform parsing into `_parse_approve_platforms()` for deterministic validation and reuse in tests."
  - "Unknown explicit keys now hard-fail with supported-key diagnostics instead of silent dropping."
  - "Omitted `--platforms` preserves legacy defaults via canonical `DEFAULT_EXPORT_PLATFORMS`."
patterns-established:
  - "Cross-surface platform key contract is enforced identically across CLI and Streamlit"
  - "Operator-visible validation errors are emitted before review-state mutation"
duration: 2m
completed: 2026-02-20
---

# Phase 5.1 Plan 03: CLI Approval Parity Summary

**CLI `approve --platforms` now enforces the same canonical platform-key contract as Streamlit, rejects unsupported explicit keys with actionable diagnostics, and preserves default behavior when omitted.**

## Performance

- **Duration:** 2m
- **Started:** 2026-02-20T01:01:35Z
- **Completed:** 2026-02-20T01:02:17Z
- **Tasks:** 3
- **Files modified:** 2

## Accomplishments
- Added canonical CLI platform parsing/validation with explicit invalid-key rejection.
- Preserved backward-compatible defaults (`youtube`, `spotify`) for omitted `--platforms`.
- Added regression tests covering valid video keys, invalid keys, and explicit empty-input failure behavior.

## Task Commits

1. **Task 1: Validate `approve --platforms` input using shared platform normalization** - `cd8d7d0` (feat)
2. **Task 2: Add explicit invalid-key error contract for operator diagnostics** - `3219fb8` (test)
3. **Task 3: Add regression tests for CLI/default/invalid platform parsing** - `89126c7` (test)

## Files Created/Modified
- `src/podcast_pipeline/cli.py` - Canonical platform parser and fail-fast diagnostics for `approve --platforms`.
- `tests/test_cli.py` - Regression tests for defaults, video-key acceptance, and invalid/empty explicit platform errors.

## Decisions Made
- Parse and validate platform keys before loading job state so invalid CLI input fails immediately.
- Treat explicit invalid input as an operator error (exit 1), not as implicit fallback behavior.
- Keep omission semantics unchanged to avoid breaking existing approve workflows.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Adjusted malformed pytest `-k` expression in plan verify command**
- **Found during:** Task 2 verification
- **Issue:** Provided expression `invalid platform or unsupported platform` is invalid pytest syntax.
- **Fix:** Ran equivalent valid expression with explicit boolean clauses.
- **Files modified:** None (execution fix only)
- **Verification:** Filtered CLI tests passed.
- **Committed in:** N/A (verification command adjustment only)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** No code scope impact; verification command syntax corrected.

## Issues Encountered
- `ruff-format` auto-formatted `cli.py` during commit hooks; restaged and committed formatted output.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Phase 5.1 execution is complete (`3/3` plans).
- Ready for phase goal verification against must-haves and phase-level state updates.

---
*Phase: 05.1-streamlit-video-platform-ui*
*Completed: 2026-02-20*
