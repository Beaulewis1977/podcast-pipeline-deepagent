---
phase: 05.1-streamlit-video-platform-ui
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/podcast_pipeline/ui/app.py
  - tests/test_ui.py
  - tests/test_ui_app.py
autonomous: true

must_haves:
  truths:
    - "Operators can explicitly select `spotify_video`, `apple_video`, and optional `apple_hls` from Streamlit export controls"
    - "Streamlit export selections round-trip safely through review-state persistence without key drift or stale widget state"
    - "`apple_hls` is presented with truthful artifact-only/provider-mediated guidance (no direct upload promise)"
  artifacts:
    - path: "src/podcast_pipeline/ui/app.py"
      provides: "Export panel wired to canonical platform registry with normalized persistence and guidance copy"
      min_lines: 140
    - path: "tests/test_ui.py"
      provides: "Helper-level tests for export platform persistence behavior in Streamlit flows"
      min_lines: 70
    - path: "tests/test_ui_app.py"
      provides: "UI regression tests for export target visibility, normalization, and persisted selection behavior"
      min_lines: 90
  key_links:
    - from: "src/podcast_pipeline/export_targets.py"
      to: "src/podcast_pipeline/ui/app.py"
      via: "UI options and defaults are rendered from shared registry metadata"
      pattern: "EXPORT_TARGETS|DEFAULT_EXPORT_PLATFORMS|normalize_export_platforms"
    - from: "src/podcast_pipeline/ui/app.py"
      to: "review/review_state.json"
      via: "`update_export_platforms()` persists normalized selections consumed by render"
      pattern: "update_export_platforms|ReviewDecisions|review_state.json"
---

<objective>
Make Streamlit export UX fully truthful for Phase 5 video platform capabilities.

Purpose: Surface new video targets in operator-facing controls while preserving robust persistence and clear workflow boundaries.
Output: Export panel driven by canonical registry, normalized state persistence, and UI-focused regression tests.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-streamlit-video-platform-ui/05.1-RESEARCH.md
@.planning/phases/05.1-streamlit-video-platform-ui/05.1-01-PLAN.md
@src/podcast_pipeline/ui/app.py
@tests/test_ui.py
@tests/test_ui_app.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drive Streamlit export options from canonical platform registry</name>
  <files>src/podcast_pipeline/ui/app.py</files>
  <action>Replace hardcoded export platform tuples in `render_export_panel()` with canonical registry-driven options. Ensure `spotify_video`, `apple_video`, and `apple_hls` are visible, grouped/readable, and default behavior remains `youtube,spotify` for first-run flows.</action>
  <verify>uv run pytest tests/test_ui_app.py -q -k "export and platform and video"</verify>
  <done>Streamlit export UI reliably exposes all supported targets from one shared source of truth.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize persisted selections and surface safe guidance/warnings</name>
  <files>src/podcast_pipeline/ui/app.py</files>
  <action>Normalize platform values when loading and saving review state (`update_export_platforms` + panel load path). If persisted invalid keys are encountered, drop them safely and show concise operator feedback. Include explicit `apple_hls` helper text clarifying artifact packaging/provider-mediated publishing boundary.</action>
  <verify>uv run pytest tests/test_ui.py tests/test_ui_app.py -q -k "update_export_platforms or review_state"</verify>
  <done>UI persistence stays deterministic for legacy/malformed data and communicates workflow boundaries truthfully.</done>
</task>

<task type="auto">
  <name>Task 3: Add Streamlit regression tests for option visibility and round-trip persistence</name>
  <files>tests/test_ui.py
tests/test_ui_app.py</files>
  <action>Add tests covering new video targets in rendered options, default fallback behavior, normalized save/load round trips, and invalid-key dropping behavior. Include at least one assertion that `apple_hls` boundary guidance text is present in export UX output.</action>
  <verify>uv run pytest tests/test_ui.py tests/test_ui_app.py -q</verify>
  <done>UI behavior for new video targets is protected by repeatable tests and cannot silently regress.</done>
</task>

</tasks>

<verification>
UI checks: new video targets are selectable, persisted selections remain normalized, and boundary guidance is explicit.
</verification>

<success_criteria>
- Streamlit export panel includes `spotify_video`, `apple_video`, and `apple_hls`.
- Export selections persist through normalized review-state round trips.
- Invalid persisted keys are handled safely with operator-visible guidance.
- UI regression tests cover the new target visibility and persistence behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-streamlit-video-platform-ui/05.1-02-SUMMARY.md`
</output>
