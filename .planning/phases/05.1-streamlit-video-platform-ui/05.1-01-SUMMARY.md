---
phase: 05.1-streamlit-video-platform-ui
plan: 01
subsystem: review
tags: [export-targets, review-state, validation, pydantic, regression-tests]
requires:
  - phase: 05-video-podcast-platforms
    provides: "Video platform keys and packaging contracts (`spotify_video`, `apple_video`, `apple_hls`)"
provides:
  - "Canonical export target registry with shared key metadata"
  - "Deterministic export-platform normalization helper used by review flow"
  - "Review-state normalization/fallback contract for legacy or malformed platform keys"
  - "Regression coverage for normalization and approve persistence"
affects: [05.1-02, 05.1-03, streamlit-ui, cli-approve]
tech-stack:
  added: []
  patterns:
    - "Single source of truth for export platform keys"
    - "Model-bound normalization for persisted review data"
key-files:
  created:
    - src/podcast_pipeline/export_targets.py
    - tests/test_review.py
  modified:
    - src/podcast_pipeline/stages/review.py
key-decisions:
  - "Centralized export platform key contract in `export_targets.py` so Streamlit/CLI/review share one registry."
  - "Normalized `ReviewDecisions.export_platforms` at model-validation boundaries to prevent malformed persisted keys from driving behavior."
  - "Kept fallback defaults anchored to `youtube,spotify` when no valid explicit selection exists."
patterns-established:
  - "Platform-key normalization is applied before persistence and approval side effects"
  - "Review-state reads are validated through `ReviewDecisions` rather than raw JSON consumption"
duration: 3m
completed: 2026-02-20
---

# Phase 5.1 Plan 01: Canonical Export Target Registry + Review Normalization Summary

**Canonical export-target normalization now enforces one key contract for review persistence, legacy fallback defaults, and video platform key parity.**

## Performance

- **Duration:** 3m
- **Started:** 2026-02-20T00:54:00Z
- **Completed:** 2026-02-20T00:56:28Z
- **Tasks:** 3
- **Files modified:** 3

## Accomplishments
- Added `export_targets.py` with canonical platform metadata, supported/default constants, and deterministic normalization.
- Wired review-stage model/approve flow to normalize persisted/exported platform keys with safe fallback semantics.
- Added deterministic regression tests for normalization, invalid-key dropping, legacy fallback, and video-key preservation.

## Task Commits

1. **Task 1: Build canonical export-target registry + normalization utilities** - `fa9c050` (feat)
2. **Task 2: Wire review decision read/write and approve flow to canonical normalization** - `e953aa5` (feat)
3. **Task 3: Add review-level regression coverage for normalization and fallback contracts** - `cf2eec4` (test)

## Files Created/Modified
- `src/podcast_pipeline/export_targets.py` - Canonical export target registry and `normalize_export_platforms()` contract.
- `src/podcast_pipeline/stages/review.py` - `ReviewDecisions` export key normalization and approve-flow integration.
- `tests/test_review.py` - Regression suite for canonical key behavior and fallback safety.

## Decisions Made
- Use a dedicated registry module instead of scattered hardcoded tuples to prevent cross-surface key drift.
- Normalize at model boundaries (not ad-hoc call sites) so legacy review-state payloads are corrected consistently.
- Preserve existing default intent (`youtube`, `spotify`) when explicit selections are absent or entirely invalid.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added precise typing overloads for normalization helper**
- **Found during:** Task 2 commit verification
- **Issue:** Mypy treated `normalize_export_platforms()` as union return type in review flow assignments.
- **Fix:** Added `Literal` overload signatures so `include_invalid=False` resolves to `list[str]`.
- **Files modified:** `src/podcast_pipeline/export_targets.py`
- **Verification:** Pre-commit mypy hook passed on subsequent commit.
- **Committed in:** `e953aa5` (part of Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Blocking fix only; no scope expansion.

## Issues Encountered
- Conventional-commit hook rejected `05.1` scope format; switched commit scope to `05-1-01` for compliance.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Shared export target contract is ready for Streamlit export panel wiring (`05.1-02`).
- CLI parity work (`05.1-03`) can reuse the same normalization + supported key set.

---
*Phase: 05.1-streamlit-video-platform-ui*
*Completed: 2026-02-20*
