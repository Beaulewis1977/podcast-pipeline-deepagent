---
phase: 09.12-streamlit-ui-gap-closure
plan: "03"
subsystem: ui
tags: [streamlit, brand-studio, sound-kit, stingers, auto-ducking, file-upload, branding]

# Dependency graph
requires:
  - phase: 09.12-streamlit-ui-gap-closure
    plan: "01"
    provides: ReviewDecisions.auto_duck_enabled (bool) field added to model
  - phase: 09.12-streamlit-ui-gap-closure
    plan: "02"
    provides: Production sidebar pattern established; caption/sync controls reference

provides:
  - Production sidebar has separate "Enable Stingers" checkbox (mapped to decisions.sound_kit_enabled)
  - Production sidebar has separate "Enable Auto-Ducking" checkbox (mapped to decisions.auto_duck_enabled)
  - Brand Studio Visual Assets expander has logo file uploader (PNG/SVG/JPG/JPEG) above logo path text input
  - Brand Studio Visual Assets expander has font file uploader (TTF/OTF/WOFF/WOFF2) above font path text input
  - Uploaded files saved to branding_dir/assets/ and path text inputs auto-populate

affects:
  - render.py can now read decisions.auto_duck_enabled for granular stinger+ducking control
  - Brand Studio operator workflow: drag-drop logo/font instead of manually typing paths

# Tech tracking
tech-stack:
  added: []
  patterns:
    - File uploader + text input combo: uploader saves to assets_dir, text input receives saved path as default
    - assets_dir resolved from branding_dir parameter (already passed to _render_brand_studio_editor) — no extra config call needed
    - Uploader write_bytes() is idempotent on reruns — same file re-written is harmless

key-files:
  created: []
  modified:
    - src/podcast_pipeline/ui/app.py

key-decisions:
  - "Enable Stingers maps to decisions.sound_kit_enabled (existing field) — render stage uses this to gate stinger mixing"
  - "Enable Auto-Ducking maps to decisions.auto_duck_enabled (new field from 09.12-01) — wired to UI persistence only; render-side consumption deferred"
  - "File uploaders use branding_dir (function parameter) not get_config() — avoids redundant config load inside expander"
  - "P3 stretch goals (GAP-5B per-job stinger overrides, GAP-6B platform overrides form) deferred — not trivially implementable without additional ReviewDecisions fields"

# Metrics
duration: 3min
completed: 2026-02-24
---

# Phase 9.12 Plan 03: Split Sound Kit Controls + Brand Studio File Uploaders Summary

**Split "Enable Sound Kit" into "Enable Stingers" + "Enable Auto-Ducking" checkboxes, and add logo/font file uploaders to Brand Studio — closing GAP-5A and GAP-6A.**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-02-24T22:29:03Z
- **Completed:** 2026-02-24T22:32:08Z
- **Tasks:** 2 (both committed atomically)
- **Files modified:** 1

## Accomplishments

- GAP-5A closed: Single "Enable Sound Kit" checkbox replaced with two independent checkboxes — "Enable Stingers" (widget key `prod_stingers_enabled_{job_id}`, persists to `decisions.sound_kit_enabled`) and "Enable Auto-Ducking" (widget key `prod_auto_duck_enabled_{job_id}`, persists to `decisions.auto_duck_enabled`). Old `prod_sound_kit_{job_id}` key removed.
- GAP-6A closed: Brand Studio Visual Assets expander now has `st.file_uploader` for logo (PNG/SVG/JPG/JPEG) and font (TTF/OTF/WOFF/WOFF2). Uploaded files are saved to `branding_dir/assets/` and the corresponding path text inputs auto-populate with the saved path. Existing manual text inputs remain fully functional.
- 1041 tests pass with no regressions (1 pre-existing failure in test_pose_match.py unrelated to UI changes)

## Task Commits

Each task was committed atomically:

1. **Task 1: Split sound kit into Enable Stingers + Enable Auto-Ducking checkboxes (GAP-5A)** - `ce28bcb` (feat)
2. **Task 2: Add logo/font file uploaders to Brand Studio Visual Assets (GAP-6A)** - `fcc9a70` (feat)

## Files Created/Modified

- `src/podcast_pipeline/ui/app.py` - Sound kit checkbox split + Brand Studio file uploaders

## Decisions Made

- "Enable Stingers" maps to `decisions.sound_kit_enabled` (existing render-wired field) so stinger mixing in render is immediately controlled by the new UI
- "Enable Auto-Ducking" maps to `decisions.auto_duck_enabled` (added in 09.12-01) — UI persistence only; render-side granular ducking control is deferred to a future iteration (current `_mix_stingers` applies ducking when stingers are present)
- `assets_dir` derived from `branding_dir` function parameter rather than calling `get_config()` again inside the expander — avoids redundant config load
- P3 stretch goals deferred: GAP-5B (per-job stinger path overrides) and GAP-6B (platform overrides interactive form) require additional ReviewDecisions fields or deeper profile mutation — not trivially implementable

## Deviations from Plan

None - plan executed exactly as written. Both P3 stretch goals (GAP-5B, GAP-6B) were explicitly marked optional and are deferred as documented in key-decisions above.

## Issues Encountered

- None. Ruff and mypy passed on first attempt for both tasks.

## User Setup Required

None.

## Next Phase Readiness

- All 7 identified UI gaps are now closed (GAP-1 through GAP-7 across plans 01-03)
- Phase 9.12 is complete: 3/3 plans done
- `decisions.sound_kit_enabled` and `decisions.auto_duck_enabled` are both persisted and available for render-side consumption
- Brand Studio file upload workflow reduces operator friction for branding asset setup

## Self-Check: PASSED

- FOUND: src/podcast_pipeline/ui/app.py (modified)
- FOUND commit: ce28bcb (Task 1)
- FOUND commit: fcc9a70 (Task 2)
- Grep `prod_stingers_enabled`: FOUND at line 2629
- Grep `prod_auto_duck_enabled`: FOUND at line 2636
- Grep `brand_studio_logo_upload`: FOUND at line 2395
- Grep `brand_studio_font_upload`: FOUND at line 2434
- Grep `assets_dir`: FOUND at lines 2389, 2390, 2399, 2438
- Old key `prod_sound_kit`: NOT FOUND (removed)

---
*Phase: 09.12-streamlit-ui-gap-closure*
*Completed: 2026-02-24*
