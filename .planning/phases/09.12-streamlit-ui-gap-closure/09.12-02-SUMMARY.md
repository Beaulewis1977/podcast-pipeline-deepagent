---
phase: 09.12-streamlit-ui-gap-closure
plan: "02"
subsystem: ui
tags: [streamlit, settings, provider-selection, captions, sync, aspect-ratio, production-sidebar]

# Dependency graph
requires:
  - phase: 09.12-streamlit-ui-gap-closure
    plan: "01"
    provides: ReviewDecisions.caption_aspect_ratio (str | None) and auto_duck_enabled (bool) fields; render.py wired to use caption_aspect_ratio override

provides:
  - Settings page has interactive Analysis Provider selectbox (Claude/Gemini/Kimi) — session-only mutation of config.models.provider
  - Settings API Keys panel shows ANTHROPIC_API_KEY status alongside Gemini/Kimi/YouTube
  - Production sidebar caption aspect ratio selectbox (Auto/16:9/9:16/1:1) persisted to ReviewDecisions.caption_aspect_ratio
  - Production sidebar sync artifact display: auto-detected offset_ms and confidence shown as st.metric above slider
  - Low-confidence sync warning when sync_artifact.low_confidence is true
  - Manual sync slider range extended from +/-2000ms to +/-5000ms per original Phase 9 spec

affects:
  - 09.12-03 (sound kit UI wiring — auto_duck_enabled; can now reference caption controls pattern)
  - Any future providers added to SUPPORTED_MODEL_PROVIDERS appear automatically in Settings selectbox

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Session-only config mutation via config.models.provider = selected_provider (no file write)
    - Module-level _ASPECT_RATIO_OPTIONS / _ASPECT_RATIO_LABELS constants avoid closure issues with lambda in format_func
    - Conditional selectbox (only shown when parent toggle is enabled) clears persisted value to None on disable
    - sync_artifact.json read via existing _read_metadata_json helper — artifact display requires no new infrastructure

key-files:
  created: []
  modified:
    - src/podcast_pipeline/ui/app.py
    - tests/test_ui_app.py

key-decisions:
  - "Provider selectbox mutates st.session_state config for current session only — no config.yaml write; caption explains how to persist permanently via env var or config.yaml"
  - "_ASPECT_RATIO_OPTIONS defined at module level (not inside render function) to avoid Streamlit lambda closure issues during reruns"
  - "Caption aspect ratio cleared to None when captions_enabled is False — prevents stale override from silently affecting future enabled state"

patterns-established:
  - "Conditional child widgets pattern: show child selectbox only when parent checkbox is True, clear child value to None on parent disable"
  - "Sync artifact display follows read-if-exists pattern using _read_metadata_json — gracefully absent when sync stage not yet run"

# Metrics
duration: 2min
completed: 2026-02-24
---

# Phase 9.12 Plan 02: Settings Provider Select + Production Sidebar Caption/Sync Controls Summary

**Claude/Gemini/Kimi provider selectbox in Settings, caption aspect ratio selectbox in Production sidebar, sync artifact metrics display, and slider range extended to +/-5000ms — closing GAP-1, GAP-3A, GAP-3B, and GAP-4.**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-02-24T22:23:29Z
- **Completed:** 2026-02-24T22:25:25Z
- **Tasks:** 2 (both committed atomically)
- **Files modified:** 2

## Accomplishments

- GAP-1 closed: Settings page AI Models expander now has an interactive Analysis Provider selectbox (sorted options from SUPPORTED_MODEL_PROVIDERS); Anthropic (Claude) API key status added to API Keys expander alongside Gemini/Kimi/YouTube
- GAP-4 closed: Caption aspect ratio selectbox (Auto/16:9/9:16/1:1) in Production sidebar; only visible when captions enabled; persists to ReviewDecisions.caption_aspect_ratio; cleared to None on caption disable
- GAP-3A closed: Sync artifact display (offset_ms metric, confidence metric, low_confidence warning) reads from `intermediate/sync_artifact.json` via existing _read_metadata_json helper
- GAP-3B closed: Manual sync slider range extended from +/-2000ms to +/-5000ms matching original Phase 9 specification
- 1034 tests pass with no regressions; test_production_controls_persist_caption_toggle updated to handle aspect ratio selectbox mock

## Task Commits

Each task was committed atomically:

1. **Task 1: Claude provider selectbox + Anthropic API key status (GAP-1)** - `8a0ffb5` (feat)
2. **Task 2: Caption aspect ratio selectbox + sync artifact display + slider range fix (GAP-3+GAP-4)** - `7dae4c8` (feat)

## Files Created/Modified

- `src/podcast_pipeline/ui/app.py` - Settings provider selectbox + Anthropic key status + caption aspect ratio selectbox + sync artifact display + slider range extension
- `tests/test_ui_app.py` - Updated test_production_controls_persist_caption_toggle to handle aspect ratio selectbox returning str | None

## Decisions Made

- Provider selectbox mutates `config.models.provider` in session state only — app never writes config files; a `st.caption` explains how to persist permanently via `MODELS__PROVIDER=claude` or config.yaml
- `_ASPECT_RATIO_OPTIONS` and `_ASPECT_RATIO_LABELS` defined at module level to avoid Streamlit lambda closure issues that can cause incorrect options on reruns
- Caption aspect ratio is cleared to `None` when captions are disabled — prevents stale override value from silently applying if captions are re-enabled with a different intent

## Deviations from Plan

None - plan executed exactly as written. Both tasks were already fully implemented in working tree from a prior session; Task 1 was already committed (`8a0ffb5`); Task 2 changes were staged and committed cleanly (`7dae4c8`) after ruff format auto-fix on second commit attempt.

## Issues Encountered

- ruff format reformatted one file during pre-commit hook on first commit attempt; re-staged and committed cleanly on second attempt (same pattern as 09.12-01).

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- GAP-1, GAP-3A, GAP-3B, GAP-4 all closed
- Production sidebar is feature-complete for caption controls (aspect ratio + captions_enabled both wired to ReviewDecisions)
- `auto_duck_enabled` field in ReviewDecisions is ready for Phase 09.12-03 sound kit UI wiring
- All 1034 tests pass

## Self-Check: PASSED

- FOUND: src/podcast_pipeline/ui/app.py
- FOUND: tests/test_ui_app.py
- FOUND commit: 8a0ffb5 (Task 1)
- FOUND commit: 7dae4c8 (Task 2)

---
*Phase: 09.12-streamlit-ui-gap-closure*
*Completed: 2026-02-24*
