---
phase: 04-post-release-hardening
plan: 06
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/podcast_pipeline/service/app.py
  - src/podcast_pipeline/service/supervisor.py
  - src/podcast_pipeline/service/recovery.py
  - src/podcast_pipeline/service/routes/system.py
  - tests/test_supervisor.py
  - tests/test_service_security.py
autonomous: true

must_haves:
  truths:
    - "Service endpoints require explicit authentication in production mode"
    - "Unhandled server exceptions are converted into consistent non-leaky API responses"
    - "Hung background runs are detectable and recoverable with timeout/heartbeat policies"
  artifacts:
    - path: "src/podcast_pipeline/service/app.py"
      provides: "auth and global exception middleware wiring"
      min_lines: 80
    - path: "src/podcast_pipeline/service/supervisor.py"
      provides: "timeout, heartbeat, and last-known-stage resilience logic"
      min_lines: 120
    - path: "tests/test_service_security.py"
      provides: "auth/error path regression coverage"
      min_lines: 80
  key_links:
    - from: "src/podcast_pipeline/service/app.py"
      to: "src/podcast_pipeline/service/routes/jobs.py"
      via: "middleware/dependency gate applies to lifecycle endpoints"
      pattern: "Depends|middleware|auth"
    - from: "src/podcast_pipeline/service/supervisor.py"
      to: "src/podcast_pipeline/service/recovery.py"
      via: "heartbeat/timeout metadata drives reconciliation"
      pattern: "heartbeat|timeout|reconcile"
---

<objective>
Secure and harden service runtime behavior for production operations.

Purpose: Protect job-control endpoints and make background execution failure modes observable and recoverable.
Output: Auth-protected service with supervisor/recovery hardening and tests.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-post-release-hardening/04-RESEARCH.md
@docs/reports/2026-02-12-all-features-production-gap-report.md
@src/podcast_pipeline/service/app.py
@src/podcast_pipeline/service/supervisor.py
@src/podcast_pipeline/service/recovery.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add service authentication gate and global exception handling</name>
  <files>src/podcast_pipeline/service/app.py
tests/test_service_security.py</files>
  <action>Introduce API key (or equivalent configured token) enforcement for job-control endpoints and add global exception handling middleware that returns structured errors without stack-trace leakage. Keep explicit dev override behavior documented and tested.</action>
  <verify>uv run pytest tests/test_service_security.py -q -k "auth or exception_handler"</verify>
  <done>Unauthorized requests are rejected consistently and unhandled exceptions become sanitized structured responses.</done>
</task>

<task type="auto">
  <name>Task 2: Add supervisor timeout and stage-heartbeat reliability</name>
  <files>src/podcast_pipeline/service/supervisor.py
tests/test_supervisor.py</files>
  <action>Persist `last_known_stage` and heartbeat updates during background runs, add configurable timeout/kill escalation for hung tasks, and ensure duplicate-run prevention remains intact under timeout conditions.</action>
  <verify>uv run pytest tests/test_supervisor.py -q -k "heartbeat or timeout or duplicate"</verify>
  <done>Hung runs are detected/handled deterministically and runtime metadata reflects real execution progress.</done>
</task>

<task type="auto">
  <name>Task 3: Improve reconciliation lifecycle and system observability hooks</name>
  <files>src/podcast_pipeline/service/recovery.py
src/podcast_pipeline/service/routes/system.py
tests/test_supervisor.py</files>
  <action>Add periodic reconciliation between startup and manual API calls, validate resume stage inputs against canonical stage order, and expose enough system diagnostics for operators to identify stale or orphaned runs quickly.</action>
  <verify>uv run pytest tests/test_supervisor.py -q -k "reconcile or resume_validation"</verify>
  <done>Recovery logic catches stale runtime states proactively and invalid resume stages cannot be persisted silently.</done>
</task>

</tasks>

<verification>
Service hardening checks: auth gate enforced, exception responses sanitized, and supervisor/recovery behavior resilient under stalled runs.
</verification>

<success_criteria>
- Job-control API surface is protected by auth.
- Global error handling avoids leaking internal traces.
- Supervisor and reconciliation flows are timeout-aware and test-covered.
</success_criteria>

<output>
After completion, create `.planning/phases/04-post-release-hardening/04-06-SUMMARY.md`
</output>
