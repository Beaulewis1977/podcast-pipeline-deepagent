---
phase: 04-post-release-hardening
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - src/podcast_pipeline/ui/app.py
  - tests/test_streamlit_service_client.py
  - tests/test_ui_app.py
autonomous: true

must_haves:
  truths:
    - "Streamlit actions do exactly what their labels claim"
    - "Timeline/preview metadata loads from the correct ingest output path"
    - "Marketing edits flow through review artifacts instead of bypassing workflow state"
  artifacts:
    - path: "src/podcast_pipeline/ui/app.py"
      provides: "truthful full-run, metadata, and marketing-flow behavior"
      min_lines: 120
    - path: "tests/test_streamlit_service_client.py"
      provides: "service action integration checks for Streamlit controls"
      min_lines: 60
    - path: "tests/test_ui_app.py"
      provides: "UI workflow regression coverage"
      min_lines: 60
  key_links:
    - from: "src/podcast_pipeline/ui/app.py"
      to: "src/podcast_pipeline/clients/service_client.py"
      via: "Run Full Pipeline uses run/resume API with full-stage semantics"
      pattern: "run_job|resume_job"
    - from: "src/podcast_pipeline/ui/app.py"
      to: "jobs/<job_id>/review/edit_plan.json"
      via: "marketing save/regenerate writes via review workflow"
      pattern: "edit_plan|review"
---

<objective>
Remove misleading Streamlit behaviors and enforce workflow truthfulness.

Purpose: Ensure operator actions map directly to backend behavior and persisted artifacts.
Output: Corrected UI action wiring and regression tests for critical controls.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-post-release-hardening/04-RESEARCH.md
@docs/reports/2026-02-12-all-features-production-gap-report.md
@src/podcast_pipeline/ui/app.py
@src/podcast_pipeline/clients/service_client.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix full-run action semantics and upload path consistency</name>
  <files>src/podcast_pipeline/ui/app.py
tests/test_streamlit_service_client.py</files>
  <action>Update "Run Full Pipeline" to call full pipeline execution (not ingest-only). Remove pre-job orphan directory assumptions during upload and keep job path creation aligned with service/pipeline ownership.</action>
  <verify>uv run pytest tests/test_streamlit_service_client.py -q -k "run_full or upload_path"</verify>
  <done>Clicking full-run triggers end-to-end pipeline execution semantics and upload flow no longer creates misleading orphan paths.</done>
</task>

<task type="auto">
  <name>Task 2: Correct preview metadata lookup and timeline data loading</name>
  <files>src/podcast_pipeline/ui/app.py
tests/test_ui_app.py</files>
  <action>Read ingest metadata from canonical `intermediate/metadata.json` and update timeline/preview loading logic accordingly. Keep backward compatibility only if legacy paths are explicitly detected and logged as fallback.</action>
  <verify>uv run pytest tests/test_ui_app.py -q -k "metadata or timeline"</verify>
  <done>Preview duration/timestamps load from the produced ingest artifact and timeline rendering no longer fails due to path mismatch.</done>
</task>

<task type="auto">
  <name>Task 3: Route marketing save/regenerate through review workflow</name>
  <files>src/podcast_pipeline/ui/app.py
tests/test_ui_app.py</files>
  <action>Replace direct `analysis.json` mutation for marketing save/regenerate with review-state/edit-plan-driven updates, preserving editorial workflow consistency and auditability.</action>
  <verify>uv run pytest tests/test_ui_app.py -q -k "marketing and review_flow"</verify>
  <done>Marketing edits are persisted through review workflow artifacts and no longer bypass decision state.</done>
</task>

</tasks>

<verification>
UI truth checks: full-run label correctness, metadata/timeline fidelity, and review-governed marketing persistence.
</verification>

<success_criteria>
- Streamlit actions map to real backend behavior.
- Metadata/timeline path mismatch is resolved.
- Marketing edits no longer bypass review state.
</success_criteria>

<output>
After completion, create `.planning/phases/04-post-release-hardening/04-03-SUMMARY.md`
</output>
