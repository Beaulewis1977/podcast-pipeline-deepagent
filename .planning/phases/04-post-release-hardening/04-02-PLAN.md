---
phase: 04-post-release-hardening
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/podcast_pipeline/service/schemas.py
  - src/podcast_pipeline/service/routes/jobs.py
  - src/podcast_pipeline/clients/service_client.py
  - src/podcast_pipeline/pipeline.py
  - tests/test_service_api.py
autonomous: true

must_haves:
  truths:
    - "Resume requests continue from a chosen stage through completion by default"
    - "Service run/resume endpoints expose explicit, typed request/response contracts"
    - "Client behavior matches backend capabilities, including background control and timeout policy"
  artifacts:
    - path: "src/podcast_pipeline/service/schemas.py"
      provides: "request/response schema parity for run/resume controls"
      min_lines: 50
    - path: "src/podcast_pipeline/service/routes/jobs.py"
      provides: "contract-enforced run/resume route semantics"
      min_lines: 120
    - path: "src/podcast_pipeline/clients/service_client.py"
      provides: "typed client parity with pooled transport and endpoint-aware timeouts"
      min_lines: 120
  key_links:
    - from: "src/podcast_pipeline/service/routes/jobs.py"
      to: "src/podcast_pipeline/pipeline.py"
      via: "resume route invokes continuation semantics, not single-stage rerun"
      pattern: "resume|until_stage|run"
    - from: "src/podcast_pipeline/clients/service_client.py"
      to: "/jobs/{job_id}/resume"
      via: "typed request includes background and optional until_stage"
      pattern: "resume_job|background|until_stage"
---

<objective>
Align service contracts with actual runtime behavior for run/resume flows.

Purpose: Eliminate API/client mismatch and restore truthful operational semantics.
Output: Contract-safe schemas, route handlers, and client methods for run/resume.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-post-release-hardening/04-RESEARCH.md
@docs/reports/2026-02-12-all-features-production-gap-report.md
@src/podcast_pipeline/service/routes/jobs.py
@src/podcast_pipeline/service/schemas.py
@src/podcast_pipeline/clients/service_client.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize run/resume schemas and backend contracts</name>
  <files>src/podcast_pipeline/service/schemas.py
src/podcast_pipeline/service/routes/jobs.py</files>
  <action>Add/align fields so resume accepts parity controls (`background`, optional `until_stage` where appropriate) and responses clearly communicate whether work was started, completed, or rejected. Ensure invalid stage values return structured validation errors.</action>
  <verify>uv run pytest tests/test_service_api.py -q -k "resume_schema or invalid_stage or run_schema"</verify>
  <done>Run/resume request and response payloads are typed, validated, and aligned with documented behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Implement resume-through-completion semantics</name>
  <files>src/podcast_pipeline/service/routes/jobs.py
src/podcast_pipeline/pipeline.py</files>
  <action>Change resume behavior so selecting `from_stage` resumes execution from that point through the remainder of the pipeline unless an explicit stop is provided. Preserve deterministic behavior for already-complete jobs and invalid resume requests.</action>
  <verify>uv run pytest tests/test_service_api.py -q -k "resume_to_completion or already_complete"</verify>
  <done>One resume call can continue a job end-to-end without repeated manual stage reruns.</done>
</task>

<task type="auto">
  <name>Task 3: Make service client contract-complete and robust</name>
  <files>src/podcast_pipeline/clients/service_client.py
tests/test_service_api.py</files>
  <action>Update client methods to send all supported run/resume parameters, reuse a persistent HTTPX client, apply endpoint-specific timeout values, and map key HTTP status codes to typed client exceptions.</action>
  <verify>uv run pytest tests/test_service_api.py -q -k "service_client and resume and timeout"</verify>
  <done>Client-side calls expose backend capability parity and fail with clear typed errors for common status classes.</done>
</task>

</tasks>

<verification>
Contract checks: schema parity, truthful resume semantics, and client-backend compatibility for run/resume controls.
</verification>

<success_criteria>
- Resume defaults to continuation through completion.
- Run/resume contracts are fully typed and validated.
- Service client matches backend schema and timeout behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/04-post-release-hardening/04-02-SUMMARY.md`
</output>
