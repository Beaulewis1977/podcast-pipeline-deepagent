---
phase: 04-post-release-hardening
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/podcast_pipeline/models/edit_plan.py
  - src/podcast_pipeline/models/analysis.py
  - src/podcast_pipeline/models/transcript.py
  - src/podcast_pipeline/models/job.py
  - src/podcast_pipeline/config/settings.py
  - tests/test_edit_plan.py
  - tests/test_models_validation.py
autonomous: true

must_haves:
  truths:
    - "Impossible ranges and confidence values are rejected at model boundaries"
    - "Job/runtime status fields cannot hold contradictory or out-of-bounds values"
    - "Malformed artifacts fail early with clear validation messages"
  artifacts:
    - path: "src/podcast_pipeline/models/edit_plan.py"
      provides: "validated filler/content/clip range models"
      min_lines: 80
    - path: "src/podcast_pipeline/models/analysis.py"
      provides: "clip/cut bounds and cross-field consistency validators"
      min_lines: 120
    - path: "tests/test_edit_plan.py"
      provides: "edge-case validation tests for range/pathological inputs"
      min_lines: 80
  key_links:
    - from: "src/podcast_pipeline/models/edit_plan.py"
      to: "src/podcast_pipeline/stages/render.py"
      via: "render consumes only pre-validated ranges"
      pattern: "EditPlan|ClipRange|ContentCut"
    - from: "src/podcast_pipeline/models/transcript.py"
      to: "src/podcast_pipeline/stages/transcribe.py"
      via: "confidence/duration bounds enforce artifact integrity"
      pattern: "confidence|duration"
---

<objective>
Make model validation strict enough to block impossible runtime data.

Purpose: Move error detection to data boundaries and stop silent cleanup of malformed artifacts.
Output: Strong validators plus dedicated validation regression suites.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-post-release-hardening/04-RESEARCH.md
@docs/reports/2026-02-12-all-features-production-gap-report.md
@src/podcast_pipeline/models/edit_plan.py
@src/podcast_pipeline/models/analysis.py
@src/podcast_pipeline/models/transcript.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add field and cross-field validators for edit/analysis/transcript models</name>
  <files>src/podcast_pipeline/models/edit_plan.py
src/podcast_pipeline/models/analysis.py
src/podcast_pipeline/models/transcript.py</files>
  <action>Apply `Field` bounds and `model_validator`/`field_validator` checks for non-negative timestamps, `end >= start`, confidence bounds, and consistency between string and numeric time representations where both are present.</action>
  <verify>uv run pytest tests/test_models.py -q -k "analysis or transcript"</verify>
  <done>Invalid ranges/confidence values are rejected during model creation and cannot silently flow downstream.</done>
</task>

<task type="auto">
  <name>Task 2: Harden job/config validation invariants</name>
  <files>src/podcast_pipeline/models/job.py
src/podcast_pipeline/config/settings.py
tests/test_models_validation.py</files>
  <action>Add bounds/consistency validators for `progress_percent`, job IDs, service port/host constraints, and provider/model configuration sanity checks. Ensure contradictory job state combinations are rejected with actionable errors.</action>
  <verify>uv run pytest tests/test_models_validation.py -q -k "job or config"</verify>
  <done>Job and service config models enforce valid runtime states and reject contradictory/unsafe values.</done>
</task>

<task type="auto">
  <name>Task 3: Add dedicated edit-plan/model edge-case test coverage</name>
  <files>tests/test_edit_plan.py
tests/test_models_validation.py</files>
  <action>Create coverage for negative/backward ranges, overlapping cuts, zero-duration segments, over-100 progress, invalid confidence values, and conflicting status combinations.</action>
  <verify>uv run pytest tests/test_edit_plan.py tests/test_models_validation.py -q</verify>
  <done>Validation regressions are trapped by focused tests before runtime stages execute.</done>
</task>

</tasks>

<verification>
Validation checks: impossible data cannot be instantiated, and model-level errors are explicit and test-covered.
</verification>

<success_criteria>
- Range/confidence validators are enforced across core artifacts.
- Job/config invariants block contradictory states.
- Dedicated model validation tests cover critical edge cases.
</success_criteria>

<output>
After completion, create `.planning/phases/04-post-release-hardening/04-04-SUMMARY.md`
</output>
