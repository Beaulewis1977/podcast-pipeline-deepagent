---
phase: 04-post-release-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/podcast_pipeline/pipeline.py
  - src/podcast_pipeline/models/job.py
  - src/podcast_pipeline/utils/locks.py
  - tests/test_pipeline.py
  - tests/test_job_locking.py
autonomous: true

must_haves:
  truths:
    - "Invalid stage inputs are rejected explicitly instead of being treated as success"
    - "Only one pipeline run can mutate a given job at a time"
    - "Stage transitions remain consistent even under concurrent run attempts"
  artifacts:
    - path: "src/podcast_pipeline/pipeline.py"
      provides: "strict stage validation and lock-aware run orchestration"
      min_lines: 80
    - path: "src/podcast_pipeline/utils/locks.py"
      provides: "job-scoped file lock helper"
      min_lines: 40
    - path: "tests/test_job_locking.py"
      provides: "regression tests for concurrent run guardrails"
      min_lines: 60
  key_links:
    - from: "src/podcast_pipeline/pipeline.py"
      to: "src/podcast_pipeline/utils/locks.py"
      via: "run path acquires/release per-job lock"
      pattern: "with .*job.*lock|acquire"
    - from: "src/podcast_pipeline/pipeline.py"
      to: "src/podcast_pipeline/models/job.py"
      via: "state reload and stage updates are coordinated under lock"
      pattern: "Job\.load|update_stage"
---

<objective>
Harden pipeline runtime invariants before touching higher-level behavior.

Purpose: Remove false-success and concurrency corruption paths at the orchestration core.
Output: Lock-aware pipeline runner with strict stage validation and regression coverage.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-post-release-hardening/04-RESEARCH.md
@docs/reports/2026-02-12-all-features-production-gap-report.md
@src/podcast_pipeline/pipeline.py
@src/podcast_pipeline/models/job.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce strict stage validation in pipeline entry points</name>
  <files>src/podcast_pipeline/pipeline.py
src/podcast_pipeline/models/job.py</files>
  <action>Update stage selection/dispatch so unknown stage names raise deterministic validation errors and produce failed status responses. Remove skip-and-log behavior for unknown stages. Ensure stage state transitions remain valid when bad inputs are received.</action>
  <verify>uv run pytest tests/test_pipeline.py -q -k "invalid_stage or stage_validation"</verify>
  <done>Supplying an invalid stage always returns an explicit error path and never reports successful completion.</done>
</task>

<task type="auto">
  <name>Task 2: Add per-job lock and state-sync boundaries</name>
  <files>src/podcast_pipeline/utils/locks.py
src/podcast_pipeline/pipeline.py</files>
  <action>Implement a job-scoped lock helper and acquire it for run/resume mutation paths. Reload job state at safe boundaries under lock before each stage starts, so stale in-memory state cannot silently overwrite fresher filesystem state.</action>
  <verify>uv run pytest tests/test_pipeline.py -q -k "state_reload or lock"</verify>
  <done>Concurrent runs on the same job are serialized/rejected deterministically and state writes are no longer race-prone.</done>
</task>

<task type="auto">
  <name>Task 3: Add concurrency regression tests for orchestration invariants</name>
  <files>tests/test_pipeline.py
tests/test_job_locking.py</files>
  <action>Create tests for duplicate run attempts, lock contention behavior, and unknown-stage rejection semantics. Include at least one test that simulates two near-simultaneous run requests on the same job ID.</action>
  <verify>uv run pytest tests/test_pipeline.py tests/test_job_locking.py -q</verify>
  <done>Regression tests fail without lock/validation hardening and pass once invariants are in place.</done>
</task>

</tasks>

<verification>
Core runtime checks: unknown stage inputs fail fast, duplicate runs are blocked, and job state remains coherent under contention.
</verification>

<success_criteria>
- Unknown stage requests cannot produce successful runs.
- Per-job lock prevents concurrent state mutation races.
- Orchestration regressions are covered by dedicated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/04-post-release-hardening/04-01-SUMMARY.md`
</output>
