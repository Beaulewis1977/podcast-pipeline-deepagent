---
phase: 04-post-release-hardening
plan: 05
type: execute
wave: 2
depends_on: ["04"]
files_modified:
  - src/podcast_pipeline/providers/gemini.py
  - src/podcast_pipeline/providers/kimi.py
  - src/podcast_pipeline/providers/base.py
  - src/podcast_pipeline/stages/analyze.py
  - tests/test_providers.py
autonomous: true

must_haves:
  truths:
    - "Provider parse failures surface as explicit failures, not empty success payloads"
    - "Fallback to transcript-only analysis is flagged as degraded mode"
    - "Provider retries and rate-limit handling are deterministic and test-covered"
  artifacts:
    - path: "src/podcast_pipeline/providers/gemini.py"
      provides: "strict parse failure handling, upload-cache optimization, robust retry classification"
      min_lines: 120
    - path: "src/podcast_pipeline/providers/kimi.py"
      provides: "truthful transcript-only fallback behavior and degraded-mode signaling"
      min_lines: 100
    - path: "tests/test_providers.py"
      provides: "provider runtime behavior tests (retry/fallback/parse failure)"
      min_lines: 120
  key_links:
    - from: "src/podcast_pipeline/stages/analyze.py"
      to: "src/podcast_pipeline/providers/base.py"
      via: "provider fallback chain emits degraded-mode metadata"
      pattern: "fallback|degraded"
    - from: "src/podcast_pipeline/providers/gemini.py"
      to: "jobs/<job_id>/analysis"
      via: "upload/cache and parse behavior influences analysis artifacts"
      pattern: "upload|cache|analyze"
---

<objective>
Harden provider behavior so AI analysis failures are explicit, diagnosable, and bounded.

Purpose: Prevent silent low-quality outputs and make fallback semantics transparent.
Output: Provider/runtime updates with dedicated tests for parse/retry/fallback paths.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-post-release-hardening/04-RESEARCH.md
@docs/reports/2026-02-12-all-features-production-gap-report.md
@src/podcast_pipeline/stages/analyze.py
@src/podcast_pipeline/providers/gemini.py
@src/podcast_pipeline/providers/kimi.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace silent parse fallback with strict failure contracts</name>
  <files>src/podcast_pipeline/providers/gemini.py
src/podcast_pipeline/providers/kimi.py
src/podcast_pipeline/providers/base.py</files>
  <action>Remove empty-structure fallback on parse/validation failures. Raise explicit provider errors, keep bounded retry behavior via tenacity for retryable failure classes, and preserve actionable error metadata for downstream observability.</action>
  <verify>uv run pytest tests/test_providers.py -q -k "parse_failure or retry"</verify>
  <done>Malformed provider responses no longer appear as successful low-value analysis outputs.</done>
</task>

<task type="auto">
  <name>Task 2: Add degraded-mode fallback signaling in analyze pipeline</name>
  <files>src/podcast_pipeline/stages/analyze.py
src/podcast_pipeline/providers/kimi.py</files>
  <action>When fallback provider is transcript-only, set explicit degraded-mode flags in analysis metadata and logs. Fix misleading capability documentation/comments so runtime behavior matches declared support.</action>
  <verify>uv run pytest tests/test_providers.py -q -k "degraded_mode or fallback"</verify>
  <done>Fallback paths are transparent to operators and downstream ranking/UX layers.</done>
</task>

<task type="auto">
  <name>Task 3: Optimize Gemini upload handling and rate-limit classification</name>
  <files>src/podcast_pipeline/providers/gemini.py
tests/test_providers.py</files>
  <action>Add job-scoped file upload ID reuse where safe, and replace fragile string-match rate-limit detection with structured exception classification. Cover cache hit/miss, upload failure, and 429 handling in tests.</action>
  <verify>uv run pytest tests/test_providers.py -q -k "upload_cache or rate_limit"</verify>
  <done>Gemini analysis avoids unnecessary repeated uploads and rate-limit handling is reliable/tested.</done>
</task>

</tasks>

<verification>
Provider checks: parse failures fail explicitly, degraded fallback is flagged, and retry/upload behavior is deterministic.
</verification>

<success_criteria>
- Silent empty-analysis fallbacks are removed.
- Transcript-only fallback is surfaced as degraded mode.
- Provider reliability logic is covered by dedicated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/04-post-release-hardening/04-05-SUMMARY.md`
</output>
