---
phase: 04-post-release-hardening
plan: 07
type: execute
wave: 3
depends_on: ["04", "06"]
files_modified:
  - src/podcast_pipeline/stages/render.py
  - src/podcast_pipeline/stages/ingest.py
  - src/podcast_pipeline/ui/app.py
  - src/podcast_pipeline/service/schemas.py
  - src/podcast_pipeline/service/routes/jobs.py
  - tests/test_render.py
  - tests/test_streamlit_service_client.py
autonomous: true

must_haves:
  truths:
    - "Render result status accurately reflects platform-level failures"
    - "Operator quality controls are passed end-to-end and affect render behavior"
    - "Render/ingest perform preflight and output verification instead of assuming success"
  artifacts:
    - path: "src/podcast_pipeline/stages/render.py"
      provides: "strict export result semantics and quality-control-driven rendering"
      min_lines: 160
    - path: "src/podcast_pipeline/service/schemas.py"
      provides: "run options carrying quality-control settings"
      min_lines: 40
    - path: "tests/test_render.py"
      provides: "render contract tests for partial failure and output verification"
      min_lines: 100
  key_links:
    - from: "src/podcast_pipeline/ui/app.py"
      to: "src/podcast_pipeline/service/routes/jobs.py"
      via: "quality controls included in run request payload"
      pattern: "quality|run_job"
    - from: "src/podcast_pipeline/stages/render.py"
      to: "jobs/<job_id>/output"
      via: "post-ffmpeg file existence checks and per-platform status map"
      pattern: "exists|errors|platform"
---

<objective>
Make render and quality-control behavior operationally truthful.

Purpose: Ensure output status and user controls accurately reflect runtime outcomes.
Output: Contract-safe render semantics plus end-to-end quality option wiring.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-post-release-hardening/04-RESEARCH.md
@docs/reports/2026-02-12-all-features-production-gap-report.md
@src/podcast_pipeline/stages/render.py
@src/podcast_pipeline/ui/app.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix render partial-failure semantics and platform result reporting</name>
  <files>src/podcast_pipeline/stages/render.py
tests/test_render.py</files>
  <action>Rework render status handling so partial platform export failures are surfaced explicitly (top-level failure or degraded state with deterministic rules). Include per-platform result details in structured output and logs.</action>
  <verify>uv run pytest tests/test_render.py -q -k "partial_failure or platform_status"</verify>
  <done>Render cannot report generic success while required platform outputs failed.</done>
</task>

<task type="auto">
  <name>Task 2: Wire quality controls from Streamlit to render execution</name>
  <files>src/podcast_pipeline/ui/app.py
src/podcast_pipeline/service/schemas.py
src/podcast_pipeline/service/routes/jobs.py
src/podcast_pipeline/stages/render.py
tests/test_streamlit_service_client.py</files>
  <action>Capture Streamlit quality control values in run payloads, validate them in service schemas, and apply them in render decision logic. Remove display-only behavior by ensuring control values materially change runtime settings.</action>
  <verify>uv run pytest tests/test_streamlit_service_client.py -q -k "quality_controls"</verify>
  <done>Changing quality controls in UI has observable impact on render execution parameters.</done>
</task>

<task type="auto">
  <name>Task 3: Add preflight and output existence checks for ingest/render</name>
  <files>src/podcast_pipeline/stages/ingest.py
src/podcast_pipeline/stages/render.py
tests/test_render.py</files>
  <action>Add disk-space and output-directory preflight checks for large ingest/render operations and assert output artifact existence after FFmpeg calls. Fail with actionable errors when preflight/output checks fail.</action>
  <verify>uv run pytest tests/test_render.py -q -k "preflight or output_exists"</verify>
  <done>Ingest/render fail early for resource issues and fail loudly when expected output files are missing.</done>
</task>

</tasks>

<verification>
Render checks: truthful status semantics, real quality-control wiring, and preflight/output guardrails.
</verification>

<success_criteria>
- Render outcome status reflects real platform export outcomes.
- UI quality settings influence backend render execution.
- Preflight and post-write checks prevent silent output failures.
</success_criteria>

<output>
After completion, create `.planning/phases/04-post-release-hardening/04-07-SUMMARY.md`
</output>
