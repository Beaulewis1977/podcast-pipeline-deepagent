---
phase: 04-post-release-hardening
plan: 10
type: execute
wave: 5
depends_on: ["01", "02", "03", "04", "05", "06", "07", "08", "09"]
files_modified:
  - tests/test_providers.py
  - tests/test_edit_plan.py
  - tests/test_supervisor.py
  - tests/test_integration.py
  - pyproject.toml
  - README.md
  - docs/desktop-distribution.md
autonomous: true

must_haves:
  truths:
    - "Runtime-critical modules have dedicated regression suites covering real failure paths"
    - "Coverage gates reflect production hardening expectations instead of permissive defaults"
    - "Documentation matches actual run/resume/auth/quality-control behavior"
  artifacts:
    - path: "tests/test_providers.py"
      provides: "provider retry/parse/fallback/degraded-mode test coverage"
      min_lines: 140
    - path: "tests/test_supervisor.py"
      provides: "background run timeout/heartbeat/recovery test coverage"
      min_lines: 120
    - path: "pyproject.toml"
      provides: "updated coverage thresholds and quality gate configuration"
      min_lines: 20
  key_links:
    - from: "tests/test_integration.py"
      to: "src/podcast_pipeline/stages/render.py"
      via: "integration checks enforce hardening behavior at runtime boundaries"
      pattern: "render|analyze|resume"
    - from: "README.md"
      to: "src/podcast_pipeline/service/app.py"
      via: "docs align with auth and run/resume contract semantics"
      pattern: "auth|resume|service"
---

<objective>
Lock in Phase 4 hardening outcomes with durable confidence gates.

Purpose: Prevent regression to false-success and weak-runtime behaviors after hardening lands.
Output: New regression suites, stricter coverage gates, and aligned operational docs.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-post-release-hardening/04-RESEARCH.md
@docs/reports/2026-02-12-all-features-production-gap-report.md
@pyproject.toml
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dedicated runtime-critical regression suites</name>
  <files>tests/test_providers.py
tests/test_edit_plan.py
tests/test_supervisor.py
tests/test_integration.py</files>
  <action>Create/expand tests for provider parse/retry/fallback behavior, edit-plan validation edge cases, supervisor timeout/recovery behavior, and integration paths that verify resume/render hardening outcomes under realistic fixtures.</action>
  <verify>uv run pytest tests/test_providers.py tests/test_edit_plan.py tests/test_supervisor.py tests/test_integration.py -q</verify>
  <done>Critical Phase 4 runtime behaviors are protected by dedicated regression suites.</done>
</task>

<task type="auto">
  <name>Task 2: Raise and segment quality gates</name>
  <files>pyproject.toml</files>
  <action>Increase overall coverage threshold and add module-level guardrails for runtime-critical areas (`stages/`, `providers/`, `service/`). Keep thresholds realistic for current fixture strategy and document rationale in comments.</action>
  <verify>uv run pytest --cov=src/podcast_pipeline --cov-report=term-missing -q</verify>
  <done>Coverage gates enforce meaningful protection for hardened runtime modules.</done>
</task>

<task type="auto">
  <name>Task 3: Align docs with hardened runtime contracts</name>
  <files>README.md
docs/desktop-distribution.md</files>
  <action>Update operator/developer docs to reflect auth requirements, resume-through-completion semantics, degraded-mode behavior, and quality-control wiring so documented behavior matches implementation reality.</action>
  <verify>rg -n "resume|auth|degraded|quality" README.md docs/desktop-distribution.md</verify>
  <done>Docs are accurate for post-hardening runtime behavior and operational expectations.</done>
</task>

</tasks>

<verification>
Confidence checks: dedicated suites cover hardened behaviors, coverage gates are raised, and docs match runtime reality.
</verification>

<success_criteria>
- Runtime-critical hardening paths are regression-tested.
- Coverage requirements are strengthened with module focus.
- Core docs reflect Phase 4 contract changes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-post-release-hardening/04-10-SUMMARY.md`
</output>
