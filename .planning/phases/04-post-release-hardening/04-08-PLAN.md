---
phase: 04-post-release-hardening
plan: 08
type: execute
wave: 4
depends_on: ["05", "07"]
files_modified:
  - src/podcast_pipeline/stages/render.py
  - src/podcast_pipeline/stages/analyze.py
  - src/podcast_pipeline/research/youtube.py
  - src/podcast_pipeline/models/analysis.py
  - tests/test_render.py
  - tests/test_research.py
autonomous: true

must_haves:
  truths:
    - "Rendered outputs include real enhancement/thumbnails rather than metadata-only placeholders"
    - "Research query generation uses transcript/topic evidence instead of weak filename fallback"
    - "Research cache behavior persists enough state to reduce repeated quota spikes"
  artifacts:
    - path: "src/podcast_pipeline/stages/render.py"
      provides: "audio enhancement and thumbnail output pipeline"
      min_lines: 180
    - path: "src/podcast_pipeline/research/youtube.py"
      provides: "persistent cache and stronger query derivation"
      min_lines: 120
    - path: "tests/test_research.py"
      provides: "cache/query derivation regression coverage"
      min_lines: 80
  key_links:
    - from: "src/podcast_pipeline/stages/analyze.py"
      to: "src/podcast_pipeline/research/youtube.py"
      via: "topic/transcript-derived query feeds research pipeline"
      pattern: "query|topics|research"
    - from: "src/podcast_pipeline/stages/render.py"
      to: "jobs/<job_id>/output"
      via: "thumbnail assets and enhanced media written as concrete artifacts"
      pattern: "thumbnail|enhance|output"
---

<objective>
Upgrade output-quality backbone after runtime correctness is in place.

Purpose: Ensure high-value media outputs and smarter research signals are materially implemented.
Output: Real enhancement/thumbnail pipeline plus stronger research query/caching behavior.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-post-release-hardening/04-RESEARCH.md
@docs/reports/2026-02-12-all-features-production-gap-report.md
@src/podcast_pipeline/stages/render.py
@src/podcast_pipeline/stages/analyze.py
@src/podcast_pipeline/research/youtube.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement concrete media enhancement and thumbnail outputs</name>
  <files>src/podcast_pipeline/stages/render.py
src/podcast_pipeline/models/analysis.py
tests/test_render.py</files>
  <action>Add a practical enhancement chain (denoise/EQ/normalization guards as configured) and real thumbnail extraction/ranking/output artifacts instead of recommendation-only metadata. Keep quality predictable with explicit fallback behavior when optional dependencies are unavailable.</action>
  <verify>uv run pytest tests/test_render.py -q -k "enhancement or thumbnail"</verify>
  <done>Render produces concrete enhanced media and thumbnail artifacts with deterministic fallback behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Strengthen transcript-driven research query derivation</name>
  <files>src/podcast_pipeline/stages/analyze.py
src/podcast_pipeline/research/youtube.py
tests/test_research.py</files>
  <action>Replace weak fallback query derivation with transcript/topic extraction heuristics and deterministic weighting rules. Ensure sparse transcript cases degrade gracefully with explicit fallback labels, not silent defaults.</action>
  <verify>uv run pytest tests/test_research.py -q -k "query_derivation or topics"</verify>
  <done>Research queries are driven by transcript/topic evidence and quality is test-verified.</done>
</task>

<task type="auto">
  <name>Task 3: Add persistent research cache strategy with coverage</name>
  <files>src/podcast_pipeline/research/youtube.py
tests/test_research.py</files>
  <action>Extend caching so repeated service restarts do not fully discard useful query/stat responses. Add TTL-aware persistence behavior and tests for cache hit/miss, stale eviction, and restart persistence semantics.</action>
  <verify>uv run pytest tests/test_research.py -q -k "cache_persist or ttl"</verify>
  <done>Research cache reduces repeated quota load across restarts and behaves predictably under TTL expiry.</done>
</task>

</tasks>

<verification>
Quality checks: real enhancement/thumbnail outputs, transcript-grounded research queries, and durable cache behavior.
</verification>

<success_criteria>
- Thumbnail/enhancement outputs exist as real artifacts.
- Research query quality improves with transcript/topic grounding.
- Cache persistence and TTL behavior are validated by tests.
</success_criteria>

<output>
After completion, create `.planning/phases/04-post-release-hardening/04-08-SUMMARY.md`
</output>
