---
phase: 05-video-podcast-platforms
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/podcast_pipeline/stages/render.py
  - tests/test_render.py
autonomous: true

must_haves:
  truths:
    - "`spotify_video` and `apple_video` renders apply configured codec/profile/level settings in FFmpeg arguments when codec-compatible"
    - "Spotify video outputs fail when stream topology or timing constraints violate documented requirements (single video + single audio, duration parity)"
    - "Render diagnostics remain truthful by surfacing compliance failures in `platform_results` and overall render status"
  artifacts:
    - path: "src/podcast_pipeline/stages/render.py"
      provides: "Video render path with compliance-aware encoding args and ffprobe-backed post-render checks"
      min_lines: 180
    - path: "tests/test_render.py"
      provides: "Regression coverage for render argument wiring and compliance failure semantics"
      min_lines: 140
  key_links:
    - from: "src/podcast_pipeline/config/settings.py"
      to: "src/podcast_pipeline/stages/render.py"
      via: "Platform spec fields mapped into `_render_video()` argument generation"
      pattern: "video_profile|video_level|pix_fmt|fps|_render_video"
    - from: "src/podcast_pipeline/stages/render.py"
      to: "tests/test_render.py"
      via: "tests assert topology/timing validation and truthful failure reporting"
      pattern: "spotify_video|ffprobe|compliance|platform_results"
---

<objective>
Wire research-defined platform compliance constraints into render behavior and stage truthfulness.

Purpose: Prevent "renders locally but fails ingest/publish" outcomes for Spotify and Apple video exports.
Output: Compliance-aware FFmpeg argument wiring plus deterministic ffprobe validation and failure reporting.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-video-podcast-platforms/05-RESEARCH.md
@.planning/phases/05-video-podcast-platforms/05-01-PLAN.md
@src/podcast_pipeline/stages/render.py
@tests/test_render.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire profile/level and cadence settings into `_render_video`</name>
  <files>src/podcast_pipeline/stages/render.py
tests/test_render.py</files>
  <action>Update `_render_video()` so `video_profile`, `video_level`, pixel format, and GOP/keyframe cadence inputs are applied only when codec-compatible. Keep behavior explicit and backward-compatible when fields are unset or unsupported.</action>
  <verify>uv run pytest tests/test_render.py -q -k "profile_flag or level_flag or gop or keyframe"</verify>
  <done>Render arguments reflect configured compliance controls without breaking existing video exports.</done>
</task>

<task type="auto">
  <name>Task 2: Add ffprobe-backed Spotify/Apple post-render compliance checks</name>
  <files>src/podcast_pipeline/stages/render.py
tests/test_render.py</files>
  <action>Add ffprobe-driven validators for `spotify_video` and `apple_video` outputs with strict Spotify topology checks (exactly one video + one audio stream, bounded A/V duration delta) and structured, actionable errors. Include conservative handling for unsupported edit-list/EDL risk and keyframe cadence drift (warn or fail according to policy defined in code/tests).</action>
  <verify>uv run pytest tests/test_render.py -q -k "spotify_video and (compliance or duration_parity or topology)"</verify>
  <done>Non-compliant outputs are rejected before render success is reported, with deterministic diagnostics.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve truthful status/error contracts under compliance failures</name>
  <files>src/podcast_pipeline/stages/render.py
tests/test_render.py</files>
  <action>Integrate validator outcomes with existing render partial-failure semantics so platform-level failures populate `platform_results`, affect overall render status correctly, and emit explicit structured logs rather than generic exceptions.</action>
  <verify>uv run pytest tests/test_render.py -q -k "partial_failure or platform_status or validation or compliance_error"</verify>
  <done>Render output contracts remain truthful when compliance checks fail on any selected platform.</done>
</task>

</tasks>

<verification>
Render checks: compliance settings flow into FFmpeg args, ffprobe catches invalid outputs, and status/error surfaces stay truthful.
</verification>

<success_criteria>
- Profile/level/cadence settings flow from config into video render args.
- Spotify/Apple validation catches non-compliant outputs before success is reported.
- Compliance failures propagate through platform and stage status contracts with actionable diagnostics.
</success_criteria>

<output>
After completion, create `.planning/phases/05-video-podcast-platforms/05-02-SUMMARY.md`
</output>
