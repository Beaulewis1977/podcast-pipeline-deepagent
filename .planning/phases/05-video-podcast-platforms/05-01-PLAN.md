---
phase: 05-video-podcast-platforms
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/podcast_pipeline/config/settings.py
  - config.yaml
  - tests/test_render.py
autonomous: true

must_haves:
  truths:
    - "Operators can select dedicated `spotify_video` and `apple_video` targets while preserving existing audio-only `spotify` and `apple` behavior"
    - "Video platform specs encode explicit compliance-critical settings (codec/profile/level/pixel format/keyframe cadence) required by render and post-render validation"
    - "Invalid platform constraints fail at config load time with actionable errors instead of surfacing late during FFmpeg execution"
  artifacts:
    - path: "src/podcast_pipeline/config/settings.py"
      provides: "Typed platform schema for research-aligned Spotify/Apple video exports"
      min_lines: 100
    - path: "config.yaml"
      provides: "Default `spotify_video` and `apple_video` export targets with conservative mezzanine-first values"
      min_lines: 50
    - path: "tests/test_render.py"
      provides: "Platform config regression tests covering defaults and validation failures"
      min_lines: 90
  key_links:
    - from: "config.yaml"
      to: "src/podcast_pipeline/config/settings.py"
      via: "`load_config()` parsing of new video-platform spec fields"
      pattern: "spotify_video|apple_video|video_profile|video_level|pix_fmt|gop"
    - from: "src/podcast_pipeline/config/settings.py"
      to: "src/podcast_pipeline/stages/render.py"
      via: "typed `PlatformSpec` constraints consumed by `_render_video()` argument construction"
      pattern: "_get_platform_spec|video_profile|video_level|fps|pix_fmt"
---

<objective>
Establish the research-led configuration contract for Spotify and Apple video export targets.

Purpose: Lock conservative, publish-oriented defaults and fail-fast schema guardrails before render-path changes.
Output: Typed platform schema, updated defaults, and regression coverage that preserve legacy audio-only behavior.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-video-podcast-platforms/05-RESEARCH.md
@src/podcast_pipeline/config/settings.py
@config.yaml
@tests/test_render.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dedicated Spotify/Apple video target defaults</name>
  <files>src/podcast_pipeline/config/settings.py
config.yaml</files>
  <action>Define `spotify_video` and `apple_video` in `PlatformSpecs` and `config.yaml` using conservative MP4/H.264/AAC defaults. Preserve existing `spotify` and `apple` audio-only targets unchanged, and keep H.265 support explicit opt-in only (not default).</action>
  <verify>uv run pytest tests/test_render.py -q -k "platform and (spotify_video or apple_video)"</verify>
  <done>Config exposes dedicated video targets for Spotify and Apple without regressing existing audio-only exports.</done>
</task>

<task type="auto">
  <name>Task 2: Add fail-fast schema validation for compliance-critical fields</name>
  <files>src/podcast_pipeline/config/settings.py
tests/test_render.py</files>
  <action>Validate `video_profile`, `video_level`, `pix_fmt`, and keyframe-related fields with codec-aware constraints (e.g., allow nullable profile/level where unsupported, reject invalid combinations for H.264 defaults). Ensure errors are actionable and tied to platform target names.</action>
  <verify>uv run pytest tests/test_render.py -q -k "video_profile or video_level or pix_fmt or keyframe"</verify>
  <done>Invalid platform constraints are rejected at config load with explicit field-level diagnostics.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for defaults and backward compatibility</name>
  <files>tests/test_render.py</files>
  <action>Add tests asserting new defaults (container/codec/profile intent, audio channels, pixel format, cadence fields) and explicit regression tests that legacy audio-only `spotify`/`apple` targets remain unchanged.</action>
  <verify>uv run pytest tests/test_render.py -q -k "platform_spec_defaults or spotify_video or apple_video or audio_only"</verify>
  <done>New video targets and legacy audio-only targets are both protected by deterministic tests.</done>
</task>

</tasks>

<verification>
Configuration checks: research-aligned video defaults parse cleanly, invalid constraints fail early, and legacy targets remain intact.
</verification>

<success_criteria>
- `load_config()` supports `spotify_video` and `apple_video` with conservative defaults.
- Compliance-critical schema fields fail fast on invalid values.
- Existing audio-only targets remain unchanged and covered by regression tests.
</success_criteria>

<output>
After completion, create `.planning/phases/05-video-podcast-platforms/05-01-SUMMARY.md`
</output>
