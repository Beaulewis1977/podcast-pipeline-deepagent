---
phase: 05-video-podcast-platforms
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - src/podcast_pipeline/config/settings.py
  - config.yaml
  - src/podcast_pipeline/stages/render.py
  - tests/test_render.py
  - README.md
autonomous: true
user_setup:
  - service: apple_podcasts_connect
    why: "Apple HLS publication is provider-mediated and requires Apple Podcasts Connect + eligible hosting-partner workflows outside this repository"
    env_vars:
      - name: APPLE_PODCASTS_CONNECT_API_KEY
        source: "Apple Podcasts Connect (when using provider-managed API key workflow)"
    dashboard_config:
      - task: "Confirm Apple video feature availability for your region/account before rollout"
        location: "https://podcasters.apple.com/support/904-availability-of-apple-podcasts-features"
      - task: "Use an eligible hosting provider and configure provider-side Apple video publishing"
        location: "https://podcasters.apple.com/partner-search"
      - task: "Follow Apple provider-mediated publish workflow for video podcasts"
        location: "https://podcasters.apple.com/support/5593-how-to-publish-video"
  - service: spotify_for_creators
    why: "Non-hosted Spotify shows still require dashboard workflow steps to attach/replace video episodes"
    dashboard_config:
      - task: "Follow Spotify hosted/non-hosted creator workflow for publishing/replacing video episodes"
        location: "https://support.spotify.com/us/creators/article/video-episodes-for-shows-not-hosted-with-spotify/"

must_haves:
  truths:
    - "Pipeline can generate optional Apple-focused HLS VOD hand-off artifacts (master playlist + variants + segments) from local inputs"
    - "Apple workflow is documented as dual-path (`apple_video` RSS enclosure and `apple_hls` provider-mediated hand-off), without claiming direct in-repo upload automation"
    - "Operator docs clearly capture Spotify hosted/non-hosted boundaries and Apple subscription limitations (audio-only)"
  artifacts:
    - path: "src/podcast_pipeline/stages/render.py"
      provides: "`apple_hls` render path with deterministic playlist/segment integrity checks"
      min_lines: 190
    - path: "src/podcast_pipeline/config/settings.py"
      provides: "Typed HLS configuration models and `apple_hls` platform defaults"
      min_lines: 110
    - path: "README.md"
      provides: "Operator-facing workflow contracts for Apple and Spotify video publication paths"
      min_lines: 45
  key_links:
    - from: "config.yaml"
      to: "src/podcast_pipeline/stages/render.py"
      via: "`apple_hls` routing into `_render_hls()` FFmpeg muxer invocation"
      pattern: "apple_hls|hls_playlist_type|master_pl_name|var_stream_map|hls_segment_filename"
    - from: "README.md"
      to: "src/podcast_pipeline/stages/render.py"
      via: "workflow documentation maps real publishing boundaries to generated artifacts"
      pattern: "spotify_video|apple_video|apple_hls|provider|hosted|subscriptions"
---

<objective>
Add optional Apple HLS packaging and close operator-workflow truthfulness gaps.

Purpose: Support Apple provider hand-off packaging while preventing overpromises about platform upload automation.
Output: Typed `apple_hls` config, deterministic HLS artifact generation/validation, and research-aligned workflow docs.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-video-podcast-platforms/05-RESEARCH.md
@.planning/phases/05-video-podcast-platforms/05-02-PLAN.md
@src/podcast_pipeline/config/settings.py
@src/podcast_pipeline/stages/render.py
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed HLS platform config models and defaults</name>
  <files>src/podcast_pipeline/config/settings.py
config.yaml
tests/test_render.py</files>
  <action>Add HLS config models (segment duration, playlist type, variant definitions, file naming) and an `apple_hls` platform spec. Keep defaults conservative, VOD-oriented, and aligned with FFmpeg HLS muxer requirements referenced in research.</action>
  <verify>uv run pytest tests/test_render.py -q -k "apple_hls or hls_config"</verify>
  <done>`apple_hls` config parses cleanly with typed validation and test coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Implement HLS render routing and artifact validation</name>
  <files>src/podcast_pipeline/stages/render.py
tests/test_render.py</files>
  <action>Add `_render_hls()` and route `apple_hls` outputs through FFmpeg HLS muxer options (`-var_stream_map`, `-master_pl_name`, `-hls_segment_filename`, `-hls_playlist_type vod`). Validate master playlist, variant playlists, and segment presence before reporting success, with explicit failure diagnostics for incomplete HLS artifacts.</action>
  <verify>uv run pytest tests/test_render.py -q -k "render_hls or master_playlist or variant_playlist"</verify>
  <done>HLS packaging succeeds only when expected playlist/segment artifacts are present.</done>
</task>

<task type="auto">
  <name>Task 3: Update workflow docs with platform constraints and operator steps</name>
  <files>README.md
tests/test_render.py</files>
  <action>Document Apple RSS vs HLS path separation, provider-mediated Apple workflow constraints, Spotify hosted/non-hosted workflow boundaries, and Apple subscription audio-only limitation. Ensure docs state this phase produces compliant artifacts only, not direct platform publish automation.</action>
  <verify>rg -n "apple_video|apple_hls|provider|hosted|Spotify|Apple|subscription" README.md</verify>
  <done>Operator documentation is aligned with official workflow constraints and avoids overpromising automation.</done>
</task>

</tasks>

<verification>
HLS checks: config validity, deterministic playlist/segment integrity, and truthful operator workflow boundaries in docs.
</verification>

<success_criteria>
- `apple_hls` target generates valid VOD master/variant outputs with deterministic checks.
- README reflects Apple dual-path and Spotify hosted/non-hosted workflow boundaries.
- Export artifact generation is clearly separated from dashboard/provider publication actions.
</success_criteria>

<output>
After completion, create `.planning/phases/05-video-podcast-platforms/05-03-SUMMARY.md`
</output>
