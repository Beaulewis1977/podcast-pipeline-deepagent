---
phase: 03-polishing-+-desktop-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/podcast_pipeline/service/app.py
  - src/podcast_pipeline/service/routes/jobs.py
  - src/podcast_pipeline/service/schemas.py
  - src/podcast_pipeline/service/supervisor.py
  - src/podcast_pipeline/cli.py
  - tests/test_service_api.py
autonomous: true

must_haves:
  truths:
    - "A local API can create jobs, start runs, and return status for existing jobs"
    - "Job execution can continue while clients poll progress and stage state"
    - "Clients see consistent job status across repeated refreshes and reconnects"
  artifacts:
    - path: "src/podcast_pipeline/service/app.py"
      provides: "FastAPI app and lifespan wiring"
      min_lines: 60
    - path: "src/podcast_pipeline/service/routes/jobs.py"
      provides: "HTTP routes for create/run/status/list/resume"
      min_lines: 120
    - path: "src/podcast_pipeline/service/supervisor.py"
      provides: "Background pipeline run orchestration with heartbeat metadata"
      min_lines: 80
    - path: "tests/test_service_api.py"
      provides: "Contract tests for service endpoints"
      min_lines: 80
  key_links:
    - from: "src/podcast_pipeline/service/routes/jobs.py"
      to: "src/podcast_pipeline/pipeline.py"
      via: "route handlers call Pipeline create/load/run flows"
      pattern: "Pipeline\\(|pipeline\\."
    - from: "src/podcast_pipeline/service/supervisor.py"
      to: "jobs/*/state.json"
      via: "supervisor reads and reconciles job state for polling and resume"
      pattern: "state\\.json|Job\\.load|update_stage_progress"
---

<objective>
Build a reusable backend job-runner service for both Streamlit and desktop clients.

Purpose: Establish one contract and one execution engine before UI migration.
Output: A FastAPI service module with durable job lifecycle routes and tests.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-polishing-+-desktop-distribution/03-RESEARCH.md
@src/podcast_pipeline/pipeline.py
@src/podcast_pipeline/models/job.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement FastAPI service surface for job lifecycle</name>
  <files>src/podcast_pipeline/service/app.py
src/podcast_pipeline/service/routes/jobs.py
src/podcast_pipeline/service/schemas.py</files>
  <action>Create a FastAPI app with lifespan startup and route modules for `GET /health`, `POST /jobs`, `POST /jobs/{job_id}/run`, `GET /jobs/{job_id}`, `GET /jobs`, and `POST /jobs/{job_id}/resume`. Reuse existing `Pipeline` and `Job` logic from `src/podcast_pipeline` (do not fork business logic into routes). Define strict request/response schemas for stable client consumption.</action>
  <verify>pytest tests/test_service_api.py -q -k "health or jobs_contract"</verify>
  <done>Service starts, endpoints return typed JSON payloads, and create/run/status/list/resume routes are implemented against existing pipeline logic.</done>
</task>

<task type="auto">
  <name>Task 2: Add background run supervision and runtime metadata</name>
  <files>src/podcast_pipeline/service/supervisor.py
src/podcast_pipeline/service/routes/jobs.py</files>
  <action>Implement non-blocking execution helpers so route handlers can start long-running pipeline tasks without blocking request threads. Persist runtime metadata (pid, started_at, heartbeat, last_known_stage) to a runtime file under each job directory for crash/restart reconciliation. Guard against duplicate concurrent runs for same job ID.</action>
  <verify>pytest tests/test_service_api.py -q -k "background_run or duplicate_run_guard"</verify>
  <done>Starting a job returns immediately with accepted/running state, supervisor heartbeat is persisted, and duplicate run attempts for same job are rejected with a deterministic response.</done>
</task>

<task type="auto">
  <name>Task 3: Wire CLI entrypoint for backend service mode</name>
  <files>src/podcast_pipeline/cli.py
src/podcast_pipeline/service/app.py
tests/test_service_api.py</files>
  <action>Add a CLI command (for example `podcast-pipeline service`) that starts the FastAPI server with host/port options. Ensure command uses the same config loading and logging setup as existing CLI commands and can be launched by desktop sidecar process without interactive prompts.</action>
  <verify>podcast-pipeline service --host 127.0.0.1 --port 8787 --help</verify>
  <done>CLI exposes a service command suitable for sidecar startup and docs/tests cover command invocation and service availability assumptions.</done>
</task>

</tasks>

<verification>
Service verification must prove contract stability for both UI clients: health check works, job lifecycle routes are callable, and background runs do not block request-response flow.
</verification>

<success_criteria>
- FastAPI service module exists and starts via CLI command.
- Job lifecycle endpoints exist with typed schemas and tests.
- Background execution metadata is persisted for future resume logic.
</success_criteria>

<output>
After completion, create `.planning/phases/03-polishing-+-desktop-distribution/03-01-SUMMARY.md`
</output>
