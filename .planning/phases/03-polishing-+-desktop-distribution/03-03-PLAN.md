---
phase: 03-polishing-+-desktop-distribution
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/podcast_pipeline/clients/service_client.py
  - src/podcast_pipeline/ui/app.py
  - src/podcast_pipeline/cli.py
  - src/podcast_pipeline/config/settings.py
  - tests/test_streamlit_service_client.py
autonomous: true

must_haves:
  truths:
    - "Streamlit users can create jobs, run jobs, and view status through backend APIs"
    - "Streamlit status/progress values match backend job responses"
    - "Switching Streamlit to service mode does not regress core review workflow actions"
  artifacts:
    - path: "src/podcast_pipeline/clients/service_client.py"
      provides: "Typed HTTP client for backend service contract"
      min_lines: 120
    - path: "src/podcast_pipeline/ui/app.py"
      provides: "Streamlit actions routed through service client"
      min_lines: 80
    - path: "tests/test_streamlit_service_client.py"
      provides: "UI/service integration behavior tests"
      min_lines: 80
  key_links:
    - from: "src/podcast_pipeline/ui/app.py"
      to: "src/podcast_pipeline/clients/service_client.py"
      via: "button actions call service client methods"
      pattern: "ServiceClient|create_job|run_job|get_job"
    - from: "src/podcast_pipeline/clients/service_client.py"
      to: "src/podcast_pipeline/service/routes/jobs.py"
      via: "endpoint paths and payload contracts match backend routes"
      pattern: "/jobs|/health|resume"
---

<objective>
Migrate Streamlit UI to the backend service contract created in Plan 01.

Purpose: Ensure one backend implementation is reused by both desktop and Streamlit.
Output: Streamlit actions routed through typed service client with regression coverage.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-polishing-+-desktop-distribution/03-RESEARCH.md
@.planning/phases/03-polishing-+-desktop-distribution/03-01-SUMMARY.md
@src/podcast_pipeline/ui/app.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed Streamlit backend service client</name>
  <files>src/podcast_pipeline/clients/service_client.py
src/podcast_pipeline/config/settings.py</files>
  <action>Implement a typed HTTP client for backend routes (health, create, run, list, job detail, resume) with explicit timeout/retry behavior. Add config fields for backend URL/port so Streamlit and desktop use the same endpoint conventions. Fail with clear typed exceptions on transport or contract errors.</action>
  <verify>pytest tests/test_streamlit_service_client.py -q -k "client_contract or retry_behavior"</verify>
  <done>Service client supports full job lifecycle route set and can be configured via project settings.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor Streamlit actions to use service client</name>
  <files>src/podcast_pipeline/ui/app.py
src/podcast_pipeline/clients/service_client.py</files>
  <action>Replace direct `Pipeline(...)` orchestration calls inside Streamlit actions with service-client calls while preserving UI behavior. Ensure create, run, refresh, and progress/status views read service responses. Keep UX stable but remove duplicated orchestration logic from UI code paths.</action>
  <verify>pytest tests/test_streamlit_service_client.py -q -k "streamlit_actions"</verify>
  <done>Streamlit can create and run jobs through backend service without direct stage orchestration calls in UI handlers.</done>
</task>

<task type="auto">
  <name>Task 3: Add service-mode compatibility in CLI and regression tests</name>
  <files>src/podcast_pipeline/cli.py
tests/test_streamlit_service_client.py</files>
  <action>Ensure CLI UX remains coherent with service-backed flows by adding explicit status/help messaging when service endpoint is unavailable and by documenting recovery behavior. Add regression tests covering unavailable backend, validation failures, and successful end-to-end service calls from UI helper functions.</action>
  <verify>pytest tests/test_streamlit_service_client.py -q</verify>
  <done>Regression suite covers service-backed UI path and failure handling; Streamlit behavior remains usable when backend is down.</done>
</task>

</tasks>

<verification>
Migration verification must confirm Streamlit and desktop both target the same backend API contract and no direct orchestration remains inside UI action handlers.
</verification>

<success_criteria>
- Typed service client exists and is configurable.
- Streamlit job operations are routed through service client.
- Regression tests cover success + backend-unavailable scenarios.
</success_criteria>

<output>
After completion, create `.planning/phases/03-polishing-+-desktop-distribution/03-03-SUMMARY.md`
</output>
