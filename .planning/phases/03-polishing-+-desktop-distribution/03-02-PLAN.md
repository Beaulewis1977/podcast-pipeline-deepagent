---
phase: 03-polishing-+-desktop-distribution
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - desktop/package.json
  - desktop/src-tauri/Cargo.toml
  - desktop/src-tauri/src/main.rs
  - desktop/src-tauri/tauri.conf.json
  - desktop/src-tauri/capabilities/default.json
  - desktop/src/lib/backend.ts
  - desktop/src/App.tsx
autonomous: true

must_haves:
  truths:
    - "Desktop app starts and can launch the packaged backend sidecar process"
    - "Desktop UI can call backend API and display connection status"
    - "Packaged desktop startup does not fail due to sidecar permission errors"
  artifacts:
    - path: "desktop/src-tauri/tauri.conf.json"
      provides: "externalBin and bundling configuration for backend sidecar"
      min_lines: 40
    - path: "desktop/src-tauri/src/main.rs"
      provides: "Rust commands for sidecar lifecycle and app startup hooks"
      min_lines: 100
    - path: "desktop/src/lib/backend.ts"
      provides: "Frontend client for backend health/job API calls"
      min_lines: 60
    - path: "desktop/src/App.tsx"
      provides: "Minimal desktop shell showing backend readiness"
      min_lines: 80
  key_links:
    - from: "desktop/src-tauri/tauri.conf.json"
      to: "desktop/src-tauri/src/main.rs"
      via: "sidecar name in externalBin matches Rust sidecar launch code"
      pattern: "externalBin|sidecar"
    - from: "desktop/src/App.tsx"
      to: "desktop/src/lib/backend.ts"
      via: "UI startup health check and job API calls"
      pattern: "health|jobs|fetch"
---

<objective>
Create the Tauri v2 desktop shell and wire it to the Phase 01 backend contract.

Purpose: Establish desktop runtime surface and process control needed for distribution.
Output: Tauri project scaffold with sidecar lifecycle control and a connected UI shell.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-polishing-+-desktop-distribution/03-RESEARCH.md
@.planning/phases/03-polishing-+-desktop-distribution/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold desktop Tauri workspace and base app shell</name>
  <files>desktop/package.json
desktop/src-tauri/Cargo.toml
desktop/src/App.tsx</files>
  <action>Initialize a Tauri v2 desktop workspace under `desktop/` with a minimal frontend shell and build scripts. Keep configuration isolated from Python root tooling. Ensure the frontend can render a status panel and is ready to call backend APIs via typed helper functions.</action>
  <verify>pnpm --dir desktop install && pnpm --dir desktop tauri build --no-bundle</verify>
  <done>Desktop project builds in no-bundle mode and frontend shell renders with no runtime errors.</done>
</task>

<task type="auto">
  <name>Task 2: Implement backend sidecar lifecycle in Rust</name>
  <files>desktop/src-tauri/src/main.rs
desktop/src-tauri/tauri.conf.json
desktop/src-tauri/capabilities/default.json</files>
  <action>Configure backend sidecar in `externalBin`, add explicit shell capability permissions, and implement Rust commands for start/stop/status lifecycle control. Sidecar command naming must match Tauri v2 requirements and support both dev and bundled execution flows.</action>
  <verify>cargo check --manifest-path desktop/src-tauri/Cargo.toml</verify>
  <done>Desktop process can start backend sidecar, report running status, and shut it down cleanly through Rust commands.</done>
</task>

<task type="auto">
  <name>Task 3: Wire frontend backend client and health handshake</name>
  <files>desktop/src/lib/backend.ts
desktop/src/App.tsx
desktop/src-tauri/src/main.rs</files>
  <action>Create a frontend service client that calls backend health/job endpoints and integrates with lifecycle commands. On app startup, UI must request sidecar start, wait for health readiness, and display clear connected/degraded status states.</action>
  <verify>pnpm --dir desktop test || pnpm --dir desktop lint</verify>
  <done>Opening desktop app shows backend health state and API calls are routed to local backend service successfully.</done>
</task>

</tasks>

<verification>
Desktop verification should prove end-to-end startup path: app launches, sidecar starts, health endpoint succeeds, and UI indicates connected state.
</verification>

<success_criteria>
- Tauri workspace exists and builds.
- Sidecar lifecycle is controlled by Rust commands with capabilities configured.
- Desktop UI can call backend API and present readiness status.
</success_criteria>

<output>
After completion, create `.planning/phases/03-polishing-+-desktop-distribution/03-02-SUMMARY.md`
</output>
