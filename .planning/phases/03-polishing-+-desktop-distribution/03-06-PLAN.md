---
phase: 03-polishing-+-desktop-distribution
plan: 06
type: execute
wave: 4
depends_on: ["04", "05"]
files_modified:
  - .github/workflows/desktop-release.yml
  - desktop/src-tauri/tauri.conf.json
  - desktop/scripts/smoke-test-desktop.sh
  - desktop/scripts/smoke-test-desktop.ps1
  - README.md
  - docs/desktop-distribution.md
autonomous: true
user_setup:
  - service: apple_codesign
    why: "Required for trusted notarized macOS distribution"
    env_vars:
      - name: APPLE_CERTIFICATE
        source: "Apple Developer account export"
      - name: APPLE_CERTIFICATE_PASSWORD
        source: "Certificate export password"
      - name: APPLE_ID
        source: "Apple Developer account"
  - service: windows_codesign
    why: "Required for trusted signed Windows installers"
    env_vars:
      - name: WINDOWS_CERTIFICATE
        source: "Code-signing certificate"
      - name: WINDOWS_CERTIFICATE_PASSWORD
        source: "Certificate password"

must_haves:
  truths:
    - "Windows/macOS/Linux installers are generated from the same release workflow"
    - "Release artifacts include backend sidecar and required runtime binaries"
    - "A clean machine smoke flow validates app startup and backend connectivity"
  artifacts:
    - path: ".github/workflows/desktop-release.yml"
      provides: "Cross-platform build and bundle workflow"
      min_lines: 120
    - path: "desktop/scripts/smoke-test-desktop.sh"
      provides: "Linux/macOS smoke validation script"
      min_lines: 50
    - path: "desktop/scripts/smoke-test-desktop.ps1"
      provides: "Windows smoke validation script"
      min_lines: 50
    - path: "docs/desktop-distribution.md"
      provides: "Installer, artifact, and release runbook"
      min_lines: 80
  key_links:
    - from: ".github/workflows/desktop-release.yml"
      to: "desktop/src-tauri/tauri.conf.json"
      via: "workflow bundle targets map to tauri config outputs"
      pattern: "bundle|windows|macos|linux|tauri"
    - from: "desktop/scripts/smoke-test-desktop.sh"
      to: "desktop/src/lib/backend.ts"
      via: "smoke checks verify backend health from installed app path"
      pattern: "health|backend|installer"
---

<objective>
Produce distributable installers and release validation for Windows, macOS, and Linux.

Purpose: Meet Phase 03 distribution goal with reproducible release outputs.
Output: CI release workflow, smoke-test scripts, and distribution runbook.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-polishing-+-desktop-distribution/03-RESEARCH.md
@.planning/phases/03-polishing-+-desktop-distribution/03-04-SUMMARY.md
@.planning/phases/03-polishing-+-desktop-distribution/03-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cross-platform desktop release workflow</name>
  <files>.github/workflows/desktop-release.yml
desktop/src-tauri/tauri.conf.json</files>
  <action>Create a GitHub Actions workflow that builds and bundles desktop artifacts for Linux, macOS, and Windows using Tauri bundle commands. Ensure workflow includes sidecar preparation and collects installer artifacts with clear naming/versioning per platform.</action>
  <verify>actionlint .github/workflows/desktop-release.yml || rg -n "strategy:|matrix:|windows|macos|ubuntu" .github/workflows/desktop-release.yml</verify>
  <done>Release workflow defines platform matrix, builds installers, and uploads artifacts for all three desktop targets.</done>
</task>

<task type="auto">
  <name>Task 2: Add clean-machine smoke test scripts for installer validation</name>
  <files>desktop/scripts/smoke-test-desktop.sh
desktop/scripts/smoke-test-desktop.ps1
.github/workflows/desktop-release.yml</files>
  <action>Create smoke scripts that validate installed app startup, sidecar backend launch, and health endpoint readiness on each platform. Integrate smoke scripts into release workflow post-build steps so broken installers fail CI quickly.</action>
  <verify>rg -n "health|installer|sidecar|exit" desktop/scripts/smoke-test-desktop.sh desktop/scripts/smoke-test-desktop.ps1</verify>
  <done>Smoke checks run from CI for each platform target and fail release job when startup/health requirements are not met.</done>
</task>

<task type="auto">
  <name>Task 3: Document release/distribution runbook and fallback paths</name>
  <files>docs/desktop-distribution.md
README.md</files>
  <action>Document installer outputs, signing/notarization prerequisites, smoke validation interpretation, and release troubleshooting (missing sidecar, asset resolution failures, resume issues). Keep instructions CLI-driven and aligned with generated workflow artifacts.</action>
  <verify>rg -n \"desktop release|installer|smoke|resume\" README.md docs/desktop-distribution.md</verify>
  <done>Repository docs contain a clear, executable release runbook for producing and validating cross-platform desktop installers.</done>
</task>

</tasks>

<verification>
Distribution verification must prove all three platform installer artifacts are generated and at least one smoke test per platform passes in CI.
</verification>

<success_criteria>
- Release workflow builds installers for Windows/macOS/Linux.
- Smoke scripts validate startup + backend health from packaged app context.
- Distribution docs cover release process and recovery/troubleshooting steps.
</success_criteria>

<output>
After completion, create `.planning/phases/03-polishing-+-desktop-distribution/03-06-SUMMARY.md`
</output>
