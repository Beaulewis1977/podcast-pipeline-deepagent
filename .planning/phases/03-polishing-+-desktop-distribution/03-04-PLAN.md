---
phase: 03-polishing-+-desktop-distribution
plan: 04
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - desktop/src-tauri/tauri.conf.json
  - desktop/src-tauri/build.rs
  - desktop/scripts/prepare-sidecars.mjs
  - src/podcast_pipeline/service/assets.py
  - src/podcast_pipeline/service/routes/system.py
  - tests/test_asset_resolution.py
autonomous: true

must_haves:
  truths:
    - "Bundled app can resolve FFmpeg and backend sidecar binaries without manual setup"
    - "Model artifacts can be optionally pre-fetched and cached with progress visibility"
    - "Installer builds fail early with actionable errors when required binaries are missing"
  artifacts:
    - path: "desktop/scripts/prepare-sidecars.mjs"
      provides: "target-triple naming and sidecar preparation script"
      min_lines: 70
    - path: "desktop/src-tauri/tauri.conf.json"
      provides: "bundle resource and externalBin definitions for sidecars"
      min_lines: 50
    - path: "src/podcast_pipeline/service/assets.py"
      provides: "runtime binary/model path resolution helpers"
      min_lines: 100
    - path: "src/podcast_pipeline/service/routes/system.py"
      provides: "system endpoints for model warmup/download status"
      min_lines: 80
  key_links:
    - from: "desktop/scripts/prepare-sidecars.mjs"
      to: "desktop/src-tauri/tauri.conf.json"
      via: "prepared filenames match externalBin entries"
      pattern: "externalBin|targetTriple"
    - from: "src/podcast_pipeline/service/routes/system.py"
      to: "src/podcast_pipeline/service/assets.py"
      via: "download/warmup endpoints call asset resolver functions"
      pattern: "resolve_|model|ffmpeg"
---

<objective>
Package runtime assets (FFmpeg + model artifacts) for deterministic desktop execution.

Purpose: Remove manual binary/model setup from clean-machine install path.
Output: Sidecar preparation, bundle config, and runtime asset resolver endpoints.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-polishing-+-desktop-distribution/03-RESEARCH.md
@.planning/phases/03-polishing-+-desktop-distribution/03-01-SUMMARY.md
@.planning/phases/03-polishing-+-desktop-distribution/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deterministic sidecar preparation for platform binaries</name>
  <files>desktop/scripts/prepare-sidecars.mjs
desktop/src-tauri/build.rs
desktop/src-tauri/tauri.conf.json</files>
  <action>Create a build-time script that validates and renames backend/FFmpeg binaries to Tauri target-triple naming conventions, then fails early if required binaries are missing. Wire script into desktop build workflow and ensure `externalBin` entries align exactly with generated names.</action>
  <verify>node desktop/scripts/prepare-sidecars.mjs --check && pnpm --dir desktop tauri build --no-bundle</verify>
  <done>Build pipeline produces correctly named sidecars for target platform and bundle config points to valid binaries.</done>
</task>

<task type="auto">
  <name>Task 2: Implement runtime asset resolver in backend service</name>
  <files>src/podcast_pipeline/service/assets.py
src/podcast_pipeline/service/routes/system.py</files>
  <action>Add resolver helpers that locate packaged FFmpeg/backend binaries and model cache directories using environment + app-data aware paths. Expose system endpoints for checking asset readiness and triggering optional model warmup/download actions without blocking job APIs.</action>
  <verify>pytest tests/test_asset_resolution.py -q</verify>
  <done>Service can report asset readiness, resolve binary paths safely, and start optional model warmup/download with explicit status responses.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests for asset and model readiness flows</name>
  <files>tests/test_asset_resolution.py
src/podcast_pipeline/service/routes/system.py</files>
  <action>Write tests for path resolution edge cases (missing binaries, wrong names, bad env overrides) and model readiness endpoints (already cached vs missing model). Ensure failures are explicit and actionable for installer troubleshooting.</action>
  <verify>pytest tests/test_asset_resolution.py -q -k "missing_binary or model_ready or model_warmup"</verify>
  <done>Test suite catches broken bundle naming and model readiness regressions before release builds.</done>
</task>

</tasks>

<verification>
Packaging verification must prove binaries are discoverable in packaged layouts and optional model readiness can be checked/prepared via backend service endpoints.
</verification>

<success_criteria>
- Sidecar/FFmpeg naming and bundle inclusion are deterministic.
- Backend resolves runtime asset locations without hardcoded OS paths.
- Optional model warmup/download endpoint is available and tested.
</success_criteria>

<output>
After completion, create `.planning/phases/03-polishing-+-desktop-distribution/03-04-SUMMARY.md`
</output>
