---
phase: 03-polishing-+-desktop-distribution
plan: 05
type: execute
wave: 3
depends_on: ["01", "02", "03"]
files_modified:
  - src/podcast_pipeline/service/recovery.py
  - src/podcast_pipeline/service/routes/jobs.py
  - desktop/src-tauri/src/recovery.rs
  - desktop/src/lib/recovery.ts
  - src/podcast_pipeline/ui/app.py
  - tests/test_job_recovery.py
autonomous: true

must_haves:
  truths:
    - "In-progress jobs can be discovered and resumed after desktop app restart"
    - "Recovery decisions are based on persisted job state and runtime journal reconciliation"
    - "Both desktop and Streamlit can trigger resume through the same backend contract"
  artifacts:
    - path: "src/podcast_pipeline/service/recovery.py"
      provides: "Backend recovery/reconciliation logic"
      min_lines: 100
    - path: "desktop/src-tauri/src/recovery.rs"
      provides: "Desktop startup recovery hooks and sidecar reattach logic"
      min_lines: 90
    - path: "desktop/src/lib/recovery.ts"
      provides: "Frontend recovery status handling and resume actions"
      min_lines: 70
    - path: "tests/test_job_recovery.py"
      provides: "Resume/recovery regression tests"
      min_lines: 100
  key_links:
    - from: "desktop/src-tauri/src/recovery.rs"
      to: "src/podcast_pipeline/service/routes/jobs.py"
      via: "startup hook calls resume/status APIs for incomplete jobs"
      pattern: "resume|jobs|status"
    - from: "src/podcast_pipeline/service/recovery.py"
      to: "jobs/*/state.json"
      via: "reconciliation reads persisted stage status and runtime journal"
      pattern: "state\\.json|runtime|heartbeat"
---

<objective>
Implement crash recovery and resume orchestration across backend, desktop, and Streamlit.

Purpose: Satisfy restart-resume reliability requirement without introducing state drift.
Output: Shared recovery logic and startup resume flow for desktop and Streamlit clients.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-polishing-+-desktop-distribution/03-RESEARCH.md
@.planning/phases/03-polishing-+-desktop-distribution/03-01-SUMMARY.md
@.planning/phases/03-polishing-+-desktop-distribution/03-02-SUMMARY.md
@.planning/phases/03-polishing-+-desktop-distribution/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add backend reconciliation and resume service logic</name>
  <files>src/podcast_pipeline/service/recovery.py
src/podcast_pipeline/service/routes/jobs.py</files>
  <action>Implement recovery functions that reconcile runtime journal data with canonical `state.json` stage state. Add API support for listing resumable jobs and resuming jobs idempotently. Ensure stale/inconsistent runtime entries are corrected automatically based on canonical stage truth.</action>
  <verify>pytest tests/test_job_recovery.py -q -k "reconcile or resumable"</verify>
  <done>Backend can identify resumable jobs after simulated interruption and expose deterministic resume API responses.</done>
</task>

<task type="auto">
  <name>Task 2: Add desktop startup recovery and resume flow</name>
  <files>desktop/src-tauri/src/recovery.rs
desktop/src/lib/recovery.ts
desktop/src/App.tsx</files>
  <action>Add desktop startup hooks that query resumable jobs, reattach/restart sidecar as needed, and surface recovery status to the frontend. Frontend should present resumable jobs and trigger backend resume endpoints without requiring direct state-file manipulation.</action>
  <verify>pnpm --dir desktop test || pnpm --dir desktop lint</verify>
  <done>Desktop startup can detect interrupted jobs and expose a working resume path through UI actions.</done>
</task>

<task type="auto">
  <name>Task 3: Expose Streamlit resume entrypoint and regression coverage</name>
  <files>src/podcast_pipeline/ui/app.py
tests/test_job_recovery.py</files>
  <action>Update Streamlit to display resumable jobs using backend API and trigger resume requests. Add regression tests that simulate interrupted jobs and verify both desktop/Streamlit-facing backend behaviors remain consistent.</action>
  <verify>pytest tests/test_job_recovery.py -q</verify>
  <done>Streamlit can resume interrupted jobs through backend APIs and tests cover reconciliation + resume workflows.</done>
</task>

</tasks>

<verification>
Recovery verification must include a forced interruption simulation followed by app restart and successful resume of at least one in-progress job.
</verification>

<success_criteria>
- Backend reconciliation logic resolves runtime/canonical state drift.
- Desktop startup detects resumable jobs and can resume.
- Streamlit exposes resume actions through same backend contract.
</success_criteria>

<output>
After completion, create `.planning/phases/03-polishing-+-desktop-distribution/03-05-SUMMARY.md`
</output>
