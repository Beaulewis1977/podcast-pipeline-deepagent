---
phase: 01-wiring-+-stability
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - config.yaml
  - pyproject.toml
  - src/podcast_pipeline/utils/ffmpeg.py
autonomous: true

must_haves:
  truths:
    - "Config defaults are aligned between config.yaml and settings.py."
    - "FFmpeg metadata parsing no longer uses eval for FPS."
    - "Dependencies include all runtime imports used in render and research stages."
  artifacts:
    - path: config.yaml
      provides: "Aligned model defaults"
      min_lines: 10
    - path: pyproject.toml
      provides: "Updated runtime dependencies"
      min_lines: 10
    - path: src/podcast_pipeline/utils/ffmpeg.py
      provides: "Safe FPS parsing"
      min_lines: 20
  key_links:
    - from: src/podcast_pipeline/utils/ffmpeg.py
      to: fractions.Fraction
      via: "safe fps parse"
      pattern: "Fraction\(.*r_frame_rate"
---

<objective>
Align config defaults, fix unsafe ffmpeg metadata parsing, and ensure runtime dependencies are declared.

Purpose: Eliminate config drift and remove unsafe parsing in core media utilities.
Output: Consistent model defaults, safe FPS parsing, and updated dependencies.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@config.yaml
@pyproject.toml
@src/podcast_pipeline/utils/ffmpeg.py
@src/podcast_pipeline/stages/render.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align config.yaml model defaults with settings</name>
  <files>config.yaml</files>
  <action>
Update config.yaml to match settings.py defaults (e.g., gemini-2.5-flash-latest and kimi-k2.5). Keep comments accurate about supported models and avoid deprecated values.
  </action>
  <verify>rg -n "model:" config.yaml</verify>
  <done>config.yaml model defaults match settings.py and documented recommendations.</done>
</task>

<task type="auto">
  <name>Task 2: Declare missing runtime dependencies</name>
  <files>pyproject.toml</files>
  <action>
Add runtime dependencies required by imports (e.g., soundfile for render loudness normalization if not already declared). Avoid adding unused packages.
  </action>
  <verify>rg -n "soundfile" pyproject.toml</verify>
  <done>Runtime dependencies cover all imports used in stages and utils.</done>
</task>

<task type="auto">
  <name>Task 3: Replace eval-based FPS parsing</name>
  <files>src/podcast_pipeline/utils/ffmpeg.py</files>
  <action>
Replace eval() usage for r_frame_rate with a safe parser (fractions.Fraction or split on “/”). Ensure missing or malformed values fall back to 0 safely.
  </action>
  <verify>rg -n "eval\(" src/podcast_pipeline/utils/ffmpeg.py</verify>
  <done>No eval remains in ffmpeg metadata parsing; FPS parsing is safe.</done>
</task>

</tasks>

<verification>
- config.yaml and settings.py defaults are aligned.
- ffmpeg metadata parsing uses safe, non-eval logic.
- dependencies declared for all runtime imports.
</verification>

<success_criteria>
- Config drift removed
- eval removed from ffmpeg helper
- Missing dependencies declared
</success_criteria>

<output>
After completion, create `.planning/phases/01-wiring-+-stability/01-04-SUMMARY.md`
</output>
