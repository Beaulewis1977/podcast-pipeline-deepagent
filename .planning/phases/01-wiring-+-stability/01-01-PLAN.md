---
phase: 01-wiring-+-stability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/podcast_pipeline/models/job.py
  - src/podcast_pipeline/stages/base.py
  - src/podcast_pipeline/pipeline.py
  - src/podcast_pipeline/ui/app.py
autonomous: true

must_haves:
  truths:
    - "Creating a job from Streamlit yields one canonical job ID used by pipeline and filesystem."
    - "Stage progress is persisted in job state and visible in the UI stage status."
  artifacts:
    - path: src/podcast_pipeline/pipeline.py
      provides: "create_job supports optional job_id and preserves input path"
      min_lines: 20
    - path: src/podcast_pipeline/models/job.py
      provides: "JobStage includes progress fields and update helper"
      min_lines: 20
    - path: src/podcast_pipeline/ui/app.py
      provides: "UI job creation uses pipeline.create_job and shows stage progress"
      min_lines: 60
  key_links:
    - from: src/podcast_pipeline/ui/app.py
      to: Pipeline.create_job
      via: "pipeline.create_job(..., job_id=...)"
      pattern: "create_job\(.*job_id"
    - from: src/podcast_pipeline/stages/base.py
      to: jobs/<job_id>/state.json
      via: "job.save with progress updates"
      pattern: "job.save\(.*jobs_dir"
---

<objective>
Fix job creation to use a single job ID end-to-end and add stage progress reporting that the Streamlit UI can display.

Purpose: Eliminate duplicate job directories and enable non-blocking progress visibility.
Output: Updated job model, pipeline create_job flow, and UI stage status with progress indicators.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Key files
@src/podcast_pipeline/models/job.py
@src/podcast_pipeline/stages/base.py
@src/podcast_pipeline/pipeline.py
@src/podcast_pipeline/ui/app.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add progress fields + update helpers to job state</name>
  <files>src/podcast_pipeline/models/job.py, src/podcast_pipeline/stages/base.py</files>
  <action>
Add progress tracking to JobStage (e.g., progress_percent: int, progress_message: str | None) and a helper on Job to update stage progress without changing status. Use it inside Stage.execute or as a small utility so stages can update progress and immediately persist state via job.save. Keep defaults backward-compatible and update model_post_init if needed.
  </action>
  <verify>python -m pytest tests/test_pipeline.py -q</verify>
  <done>Job state JSON includes progress fields and can be updated while stages are running.</done>
</task>

<task type="auto">
  <name>Task 2: Use a single job ID in Streamlit job creation</name>
  <files>src/podcast_pipeline/pipeline.py, src/podcast_pipeline/ui/app.py</files>
  <action>
Update Pipeline.create_job to accept an optional job_id override (and use it if provided). In Streamlit new job form, generate the job_id once, save the uploaded file under that job directory, and call pipeline.create_job with the same job_id so state.json and input file live in the same folder. Avoid duplicate job directories.
  </action>
  <verify>python -m pytest tests/test_pipeline.py -q</verify>
  <done>Creating a job in the UI results in exactly one job directory with state.json and input/raw.*.</done>
</task>

<task type="auto">
  <name>Task 3: Display stage progress in the UI status row</name>
  <files>src/podcast_pipeline/ui/app.py</files>
  <action>
Update render_stage_status to show per-stage progress percent/message if present (e.g., a small progress bar or text under the status badge). Ensure it gracefully handles missing progress data and still renders stage status icons.
  </action>
  <verify>Manual UI check: open Streamlit UI and confirm stage status shows progress fields when present.</verify>
  <done>Stage status shows progress percent/message without breaking the existing layout.</done>
</task>

</tasks>

<verification>
- Job creation in UI yields a single canonical job ID (no duplicate directories).
- Stage progress values are persisted in state.json and visible in UI status.
</verification>

<success_criteria>
- Pipeline.create_job accepts optional job_id and uses it consistently
- JobStage includes progress fields with safe defaults
- Streamlit UI shows progress for running stages
</success_criteria>

<output>
After completion, create `.planning/phases/01-wiring-+-stability/01-01-SUMMARY.md`
</output>
