---
phase: 01-wiring-+-stability
plan: 05
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - src/podcast_pipeline/stages/render.py
autonomous: true

must_haves:
  truths:
    - "Render outputs reflect approved cuts from edit_plan.json (not full-length by default)."
    - "Selected clip ranges are exported as short-form clips."
  artifacts:
    - path: src/podcast_pipeline/stages/render.py
      provides: "Edit-plan-driven render and clip exports"
      min_lines: 80
  key_links:
    - from: src/podcast_pipeline/stages/render.py
      to: jobs/<job_id>/review/edit_plan.json
      via: "load edit plan"
      pattern: "edit_plan.json"
    - from: src/podcast_pipeline/stages/render.py
      to: output/clips
      via: "clip export loop"
      pattern: "output / \"clips\""
---

<objective>
Apply edit_plan cuts during render and generate short-form clip exports from approved ranges.

Purpose: Ensure render reflects review decisions and produces clip outputs.
Output: Trim/concat render for full exports and per-clip exports in output/clips.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/podcast_pipeline/stages/render.py
@src/podcast_pipeline/stages/review.py
@src/podcast_pipeline/models/edit_plan.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Load edit_plan.json and build trim/concat filters</name>
  <files>src/podcast_pipeline/stages/render.py</files>
  <action>
Load review/edit_plan.json when present. Build a filter_complex that trims video/audio to keep ranges (derived from cut ranges) and concatenates them into a single stream for full-episode exports. If no cuts exist, fall back to current full-length render. Remove unused numpy import if still present.
  </action>
  <verify>python -m pytest tests/test_render.py -q</verify>
  <done>Full-episode exports reflect approved cuts when edit_plan.json contains cut ranges.</done>
</task>

<task type="auto">
  <name>Task 2: Export short-form clips from edit plan</name>
  <files>src/podcast_pipeline/stages/render.py</files>
  <action>
For each clip range in edit_plan.json, generate a clip file under output/clips (e.g., clip_01.mp4). Use FFmpeg trim on the original input (or already-cut stream if required) and apply platform specs if appropriate. Include clip outputs in StageResult.outputs so they show in the UI.
  </action>
  <verify>Manual check: render stage outputs include output/clips/clip_*.mp4 when clips are selected.</verify>
  <done>Short-form clips are exported for each approved clip range.</done>
</task>

</tasks>

<verification>
- Full exports use edit_plan cut ranges.
- Clip exports generated from selected ranges.
</verification>

<success_criteria>
- Render uses edit_plan.json when available
- output/clips contains generated clip files
</success_criteria>

<output>
After completion, create `.planning/phases/01-wiring-+-stability/01-05-SUMMARY.md`
</output>
