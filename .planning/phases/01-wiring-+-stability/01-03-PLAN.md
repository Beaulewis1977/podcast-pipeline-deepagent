---
phase: 01-wiring-+-stability
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/podcast_pipeline/stages/transcribe.py
autonomous: true

must_haves:
  truths:
    - "Multi-track inputs produce merged, speaker-labeled transcripts by default."
    - "Combined filler cut list reflects all tracks."
  artifacts:
    - path: src/podcast_pipeline/stages/transcribe.py
      provides: "Default multi-track transcription path"
      min_lines: 40
  key_links:
    - from: src/podcast_pipeline/stages/transcribe.py
      to: transcribe_multi_track
      via: "track count detection"
      pattern: "transcribe_multi_track"
---

<objective>
Make multi-track transcription the default when multiple audio streams exist and ensure merged transcript + filler cuts are produced.

Purpose: Provide speaker-labeled transcripts and accurate filler cuts for multi-host podcasts.
Output: Transcribe stage routes multi-track inputs through transcribe_multi_track by default.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/podcast_pipeline/stages/transcribe.py
@src/podcast_pipeline/stages/ingest.py
@src/podcast_pipeline/utils/ffmpeg.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Detect audio track count and route to multi-track transcription</name>
  <files>src/podcast_pipeline/stages/transcribe.py</files>
  <action>
In TranscribeStage.run, detect audio track count (using metadata.json or _detect_audio_tracks on the raw input). If >1, call transcribe_multi_track and return its StageResult; otherwise, keep the single-track flow. Preserve existing outputs (transcript.json, filler_cuts.json, transcripts/*.srt/.vtt) and log the chosen path.
  </action>
  <verify>python -m pytest tests/test_pipeline.py -q</verify>
  <done>Multi-track inputs default to transcribe_multi_track; single-track inputs keep current flow.</done>
</task>

<task type="auto">
  <name>Task 2: Validate merged transcript and filler cut outputs</name>
  <files>src/podcast_pipeline/stages/transcribe.py</files>
  <action>
Ensure transcribe_multi_track outputs merged transcript.json with speaker labels and combined filler_cuts.json sorted by time. Confirm transcript exports still exist and contain speaker labels (SRT at minimum).
  </action>
  <verify>Manual check: inspect transcript.json/filler_cuts.json on a multi-track job.</verify>
  <done>Merged transcript includes speaker labels and filler_cuts.json aggregates all tracks.</done>
</task>

</tasks>

<verification>
- Multi-track audio runs the multi-track path by default.
- Merged transcript and filler cuts are produced for multi-track jobs.
</verification>

<success_criteria>
- Default multi-track path is enabled
- Outputs include merged transcript + combined filler cuts
</success_criteria>

<output>
After completion, create `.planning/phases/01-wiring-+-stability/01-03-SUMMARY.md`
</output>
